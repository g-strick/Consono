# pnpm 11: skip interactive TTY prompt when purging node_modules in non-interactive shells
confirm-module-purge=false

# pnpm 11: approve esbuild build scripts
ignore-dep-scripts=false

# pnpm 11 moved auto-install-peers into pnpm-workspace.yaml (autoInstallPeers) — .npmrc no longer reads it here.
