---
phase: 02-review-loop-home-states
plan: 03
type: execute
wave: 2
depends_on: ['02-01']
files_modified:
  - apps/mobile/app/review/index.tsx
autonomous: false
requirements: [RVEW-01, RVEW-02, RVEW-03, RVEW-04, RVEW-05, STRK-01, STRK-02]
must_haves:
  truths:
    - 'Review intro and done screens show the real streak count and state, not the placeholder 12/13'
    - 'The streak chip animates at-risk (intro) -> continued (done) via the existing 280ms fill on session completion'
    - "Completing a session invalidates ['home','summary'] so the streak/today stats refresh"
    - 'The review front offers a single slow (turtle) replay control that plays the current audio at ~0.7x, replacing the 0.7x/1.0x/1.2x label'
    - 'Default audio still plays at full speed; slow is per-tap, not sticky'
    - 'Front shows no word/image (audio only); reveal shows image/word/gender/sentence/rating buttons; intro shows count/breakdown/est-time; done uses cobalt surface (all already built — preserved while wiring)'
  artifacts:
    - path: 'apps/mobile/app/review/index.tsx'
      provides: 'Review screen wired to real streak + per-tap slow audio replay'
  key_links:
    - from: 'apps/mobile/app/review/index.tsx'
      to: 'api.getHomeSummary'
      via: "useQuery(['home','summary'])"
      pattern: "queryKey: \\['home', 'summary'\\]"
    - from: 'exitReview'
      to: "['home','summary'] cache"
      via: 'queryClient.invalidateQueries'
      pattern: "invalidateQueries\\(\\{ queryKey: \\['home', 'summary'\\]"
    - from: 'front slow control'
      to: 'playAudioAtRate(url, 0.7)'
      via: 'expo-av setRateAsync'
      pattern: 'setRateAsync'
---

<objective>
Wire real streak data and a Duolingo-style slow-audio replay control into the review loop. Phase 1.1 reskinned all four review phases (intro / audio-only front / reveal & rate / cobalt done); this plan removes the streak placeholder and the static speed label without re-laying-out any screen.

Purpose: STRK-01/STRK-02 need the real streak count flowing into the intro (at-risk) and done (continued) chips so the existing 280ms fill animation celebrates a real streak tick. RVEW-05 is revised (D-09/D-10) to a single per-tap turtle replay at ~0.7x instead of a 3-way speed picker; `users.audio_speed` stays untouched.

Output: A review screen whose streak chips show real values, whose completion refreshes Home's summary, and whose front has a working slow-replay button.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-review-loop-home-states/02-CONTEXT.md
@.planning/phases/02-review-loop-home-states/02-PATTERNS.md
@apps/mobile/app/review/index.tsx
@wireframes/Wireframes v6.html

<interfaces>
<!-- From Plan 01 (apps/mobile/src/lib/api.ts) — import, do not redefine. -->
api.getHomeSummary(): Promise<HomeSummary>
HomeSummary.streak = { count: number; active: boolean; reviewedToday: boolean }

<!-- Current placeholders in apps/mobile/app/review/index.tsx -->

line 19 const STREAK_PLACEHOLDER = 12;
line 64-67 exitReview() invalidates only ['cards','due']
line 77-86 playAudio(url) — createAsync + playAsync (default rate)
line 170 <StreakChip count={STREAK_PLACEHOLDER} state="at-risk" /> (intro)
line 230 <StreakChip count={STREAK_PLACEHOLDER + 1} state="continued" /> (done)
line 302-323 76px gold play button (TouchableOpacity shape to copy for the turtle control)
line 352-354 <Mono ...>tap to replay · 0.7× · 1.0× · 1.2×</Mono> (front, replace)

StreakChip props: { count: number; state: 'inactive'|'at-risk'|'continued' } (no change to the component)
expo-av Audio.Sound supports `await sound.setRateAsync(rate, shouldCorrectPitch)` before playAsync.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire real streak into intro + done chips, refresh Home on completion</name>
  <files>apps/mobile/app/review/index.tsx</files>
  <read_first>
    - apps/mobile/app/review/index.tsx (full file — useQuery 36-39, exitReview 64-67, intro chip 170, done chip 230, STREAK_PLACEHOLDER 19)
    - apps/mobile/src/lib/api.ts (getHomeSummary, HomeSummary.streak from Plan 01)
    - apps/mobile/src/components/StreakChip.tsx (280ms fill animation; props unchanged)
    - .planning/phases/02-review-loop-home-states/02-CONTEXT.md (D-04 — wire real count, replace count+1 literal, fire fill on done)
  </read_first>
  <action>
    Add a home-summary query to ReviewScreen: useQuery({ queryKey: ['home','summary'], queryFn: () => api.getHomeSummary(), staleTime: 30_000 }), aliasing data as homeSummary. Derive const streakCount = homeSummary?.streak.count ?? 0. Remove the STREAK_PLACEHOLDER constant (line 19). Intro chip (line 170): set count={streakCount} and state from real data — state={homeSummary?.streak.reviewedToday ? 'continued' : 'at-risk'} (D-04: if they already reviewed today the chip is already continued; otherwise at-risk pre-completion). Done chip (line 230): set count={streakCount} state="continued" (drop the `+ 1` literal — the real post-session count comes from the server after the cache refreshes). In exitReview() add queryClient.invalidateQueries({ queryKey: ['home','summary'] }) alongside the existing ['cards','due'] invalidation so Home and the next streak read reflect the completed session. Do not change any layout/styles — only the chip props, the removed constant, and the added query/invalidation.
  </action>
  <verify>
    <automated>corepack pnpm --filter mobile typecheck</automated>
  </verify>
  <acceptance_criteria>
    - review/index.tsx contains `queryKey: ['home', 'summary']`
    - STREAK_PLACEHOLDER constant is removed (no remaining reference)
    - intro StreakChip uses count={streakCount} and a data-derived state (no literal 12)
    - done StreakChip uses count={streakCount} state="continued" (no `+ 1`)
    - exitReview contains `invalidateQueries({ queryKey: ['home', 'summary'] })`
    - mobile typecheck passes
  </acceptance_criteria>
  <done>Intro/done chips show the real streak; completing a session refreshes ['home','summary']; no placeholder remains.</done>
</task>

<task type="auto">
  <name>Task 2: Slow (turtle) replay control on the front (revised RVEW-05)</name>
  <files>apps/mobile/app/review/index.tsx</files>
  <read_first>
    - apps/mobile/app/review/index.tsx (playAudio 77-86, front gold play button 302-323, speed label 352-354)
    - .planning/phases/02-review-loop-home-states/02-CONTEXT.md (D-09 single slow turtle ~0.7x not a picker, D-10 per-replay not sticky, users.audio_speed untouched)
    - wireframes/Wireframes v6.html (Review front layout — place the turtle within the existing front, no re-layout)
  </read_first>
  <action>
    Add a sibling callback playAudioAtRate(url: string, rate: number) next to playAudio (lines 77-86): same body (unloadAsync -> createAsync -> assign soundRef) but call `await sound.setRateAsync(rate, true)` (shouldCorrectPitch=true) before playAsync; keep the try/catch (audio failure non-fatal). Replace the static label at lines 352-354 (`tap to replay · 0.7× · 1.0× · 1.2×`) with a single turtle replay control: a TouchableOpacity (copy the play-button TouchableOpacity shape, but a compact inline control, not the 76px circle) whose onPress calls `currentCard.sentence_audio_url && playAudioAtRate(currentCard.sentence_audio_url, 0.7)`, containing a Mono surface={nightSurface} tone="faint" label like "🐢 slow". This is per-tap (D-10) — do NOT add sticky state or read users.audio_speed. Default full-speed playback (the gold play button and the front auto-play effect) is unchanged. Optionally, if cheap, also expose the same turtle on the reveal sentence audio — front is the priority; do it only if it does not require restructuring the reveal layout.
  </action>
  <verify>
    <automated>corepack pnpm --filter mobile typecheck</automated>
  </verify>
  <acceptance_criteria>
    - review/index.tsx defines playAudioAtRate and calls `setRateAsync(` with shouldCorrectPitch true
    - the `0.7× · 1.0× · 1.2×` static label string no longer exists in the file
    - the front renders a TouchableOpacity whose onPress calls playAudioAtRate(currentCard.sentence_audio_url, 0.7)
    - no reference to users.audio_speed and no sticky/global slow state was added
    - the default gold play button still calls playAudio (full speed) — unchanged
    - mobile typecheck passes
  </acceptance_criteria>
  <done>Front has a working per-tap turtle replay at ~0.7x; full-speed default is preserved; audio_speed untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify review streak + slow audio on device</name>
  <what-built>Review intro/done chips now show the real streak count and animate at-risk -> continued via the existing 280ms fill; completing a session refreshes Home's summary; the front has a per-tap turtle button that replays the current audio at ~0.7x (replacing the static speed label). Front stays audio-only, reveal/intro/done layouts unchanged.</what-built>
  <how-to-verify>
    1. Start API + app. Open a review session.
    2. Intro: the streak chip shows your real count (not 12). If you have not reviewed today it reads at-risk (outline); if you already did, it reads continued (RVEW-03, STRK-01).
    3. Front: confirm audio-only — no word, no image (RVEW-01). Tap the gold play button: audio plays at full speed. Tap the 🐢 slow control: the same audio replays noticeably slower (~0.7x) with corrected pitch (RVEW-05 revised, D-09/D-10).
    4. Reveal: image, word + gender, sentence, single sentence-audio control, and FSRS rating buttons all present (RVEW-02).
    5. Finish the session: done screen is the cobalt surface; the streak chip is continued (filled) with the real count and the 280ms fill plays — no scale/bounce (RVEW-04, STRK-02, D-04).
    6. Return Home: streak and today stats reflect the just-finished session (cache invalidation).
  </how-to-verify>
  <resume-signal>Type "approved" or describe anything wrong (wrong count, no fill animation, slow not slower, layout shifted).</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                  | Description                                         |
| --------------------------------------------------------- | --------------------------------------------------- |
| mobile review UI → API (GET /home/summary, POST /reviews) | Client reads scoped streak data and submits reviews |

## STRIDE Threat Register

| Threat ID | Category               | Component                         | Disposition | Mitigation Plan                                                                                                                  |
| --------- | ---------------------- | --------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------- |
| T-02-08   | Information Disclosure | streak data shown in review chips | mitigate    | Streak comes from the server-scoped /home/summary (cards.user_id enforced in Plan 01). Client trusts no client-supplied user id. |
| T-02-09   | Tampering              | playAudioAtRate rate argument     | accept      | Rate is a hardcoded 0.7 constant, not user/network input; expo-av clamps playback rate. No injection surface.                    |
| T-02-04   | Spoofing               | user identity                     | accept      | Auth stubbed (V0_USER_ID) per established v0 posture; real auth + RLS in Phase 6.                                                |
| T-02-SC   | Tampering              | npm installs                      | mitigate    | No new packages — expo-av and @tanstack/react-query already present. No install task.                                            |

</threat_model>

<verification>
- `corepack pnpm --filter mobile typecheck` passes.
- Device walkthrough (Task 3): real streak count, at-risk->continued 280ms fill, working turtle slow replay, audio-only front, cobalt done.
</verification>

<success_criteria>

- Intro/done chips show the real streak; the 280ms fill fires on completion (STRK-01, STRK-02, D-04).
- Session completion invalidates ['home','summary'].
- Front has a per-tap ~0.7x turtle replay; default playback stays full speed; users.audio_speed untouched (RVEW-05 revised, D-09, D-10).
- The four review phases keep their v6 layouts (RVEW-01..04 preserved while wiring).
  </success_criteria>

<output>
Create `.planning/phases/02-review-loop-home-states/02-03-SUMMARY.md` when done.
</output>
