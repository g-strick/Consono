---
phase: 03-streak-detail-design-fidelity
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - apps/mobile/app/streak/index.tsx
autonomous: true
requirements: [STRK-03, STRK-04, STRK-05, STRK-06]

must_haves:
  truths:
    - "Streak detail shows correct real stats for month/year/lifetime; the period toggle instant-swaps without a refetch because all three periods arrive in one payload"
    - "Personal bests render real maximal-run data per D-04, top-5 + current badge per D-05, including 1-day runs per D-06; empty data => single current-row entry ('0 days','today')"
    - "Hero/stat-grid retention shows True FSRS retention per D-09 (the screen does not relabel or reconcile it with Home accuracy)"
    - "Stat windows match the heatmap above them per D-07; month stat-sub copy reads 'this month' per the D-08 copy deviation while year/lifetime subs are unchanged"
    - "Heatmap cells use the lib's quartile levels (0-3 → 4-stop ramp) and today is outlined never filled; new-user state renders all heat-0 with zeros inline (no skeleton/error UI)"
  artifacts:
    - path: "apps/mobile/app/streak/index.tsx"
      provides: "Streak detail screen wired to real getStreakStats data"
      contains: "useQuery"
  key_links:
    - from: "apps/mobile/app/streak/index.tsx"
      to: "api.getStreakStats"
      via: "useQuery queryKey ['streak','stats']"
      pattern: "getStreakStats"
    - from: "apps/mobile/app/streak/index.tsx"
      to: "StreakStats payload"
      via: "nullish-coalescing zero defaults while loading"
      pattern: "\\?\\?"
---

<objective>
Replace every static placeholder constant in the streak detail screen (HERO, STATS, RATING,
YEAR_LEVELS, YEAR_MONTHS, MONTH_DAYS, MONTH_TODAY, REVIEWS, BESTS) with real data derived from
a single `api.getStreakStats()` TanStack Query. Layout, components, styles, and the period
toggle are LOCKED — only the data source changes.

Purpose: Make the streak screen real (STRK-03..06). The period toggle stays instant because the
query returns all three periods at once; zeros render while loading and on error (no skeleton).

Output: Wired `apps/mobile/app/streak/index.tsx`. No new components, no layout/palette changes.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-streak-detail-design-fidelity/03-CONTEXT.md
@.planning/phases/03-streak-detail-design-fidelity/03-PATTERNS.md
@.planning/phases/03-streak-detail-design-fidelity/03-UI-SPEC.md

<interfaces>
From apps/mobile/src/lib/api.ts (Plan 02 output):
```typescript
export interface StreakStats { hero; month; year; lifetime; bests; }  // full shape in api.ts
export const api = { getStreakStats(): Promise<StreakStats>; };
```

Existing TanStack Query pattern — apps/mobile/app/(tabs)/index.tsx:
```typescript
const { data } = useQuery({ queryKey: ['streak','stats'], queryFn: () => api.getStreakStats(), staleTime: 30_000 });
const value = data?.field ?? 0;  // zero-while-loading
```

Components already in the screen (do NOT modify their APIs, only feed real props):
StatTile, YearHeatmap, MonthHeatmap, RatingDistribution, Card, Body/Mono/Num,
inline ReviewsChart (lines ~317), inline LifetimeBars (lines ~376).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire query + hero + 2×2 stat grid + period toggle to real data</name>
  <files>apps/mobile/app/streak/index.tsx</files>
  <read_first>
    - apps/mobile/app/streak/index.tsx (FULL — placeholder constants lines 33–172, JSX consumers, StyleSheet; keep all layout/styles)
    - apps/mobile/app/(tabs)/index.tsx (useQuery + zero-while-loading pattern, lines ~57–82)
    - apps/mobile/src/lib/api.ts (StreakStats interface + getStreakStats, Plan 02)
    - .planning/phases/03-streak-detail-design-fidelity/03-UI-SPEC.md (Copywriting Contract, Stat grid table, Data States: zeros-while-loading no skeleton)
    - .planning/phases/03-streak-detail-design-fidelity/03-CONTEXT.md (D-07 windows, D-08 month copy, D-09 retention)
  </read_first>
  <action>
    Add `useQuery({ queryKey: ['streak','stats'], queryFn: () => api.getStreakStats(), staleTime: 1000 * 30 })` to StreakScreen. Remove the HERO and STATS placeholder constants. Derive hero from `streakStats?.hero ?? { streak: 0, longestAllTime: 0, retentionAllTime: 0 }`; render the hero meta line as "{streak} day streak · longest {longestAllTime} · retention {retentionAllTime}%" using the existing Num/Mono primitives (keep tone=brand on the Num — the number IS the data per UI-SPEC Data States). Build the per-period stat tiles from `streakStats?.[period]` with nullish-zero defaults: longest streak (value + sub: month='this month' per D-08, year=longestDates range string, lifetime='all time'), retention% (sub: month='this month' per D-08, year='past year', lifetime='all time'), reviews (sub: month='this month' per D-08, year='past year', lifetime='all time'), days active (sub: month='of {daysInMonth}', year='past year', lifetime='since {firstReviewDate month year}'). Format retention as `{Math.round(retention*100)}%` or `{retention}%` to match exactly what the route emits (check api.ts comment). Format large review counts with thousands separators via `toLocaleString()`. Keep the existing period toggle (setPeriod) and 2×2 grid markup/styles untouched — only the values change. Do NOT add a skeleton or error component: zeros render until data resolves (UI-SPEC Data States).
  </action>
  <verify>
    <automated>cd apps/mobile && corepack pnpm exec tsc --noEmit 2>&1 | grep -i "streak/index" | head; echo "tsc-checked"</automated>
  </verify>
  <acceptance_criteria>
    - streak/index.tsx contains `queryKey: ['streak', 'stats']` and `api.getStreakStats()`
    - HERO and STATS placeholder constants are deleted (grep `const HERO =` and `const STATS:` return 0 matches)
    - month stat subs read "this month" (D-08); year/lifetime subs unchanged ("past year"/"all time") — grep "this month" returns >=1, and no month sub reads "past 30 days"
    - retention display derives from streakStats (not a hardcoded 85/88) — grep returns no literal "85%"/"88%" in body
    - mobile `tsc --noEmit` reports no new errors in streak/index.tsx
  </acceptance_criteria>
  <done>Hero, stat grid, and period toggle reflect real month/year/lifetime data with D-07/D-08/D-09 semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Wire heatmaps, reviews chart, rating distribution, and personal bests to real data</name>
  <files>apps/mobile/app/streak/index.tsx</files>
  <read_first>
    - apps/mobile/app/streak/index.tsx (FULL — YEAR_LEVELS/genLevel lines 73–119, REVIEWS lines 122–165, BESTS lines 167+, inline ReviewsChart ~317, LifetimeBars ~376; YearHeatmap/MonthHeatmap/RatingDistribution call sites)
    - apps/mobile/src/components/Heatmap.tsx (YearHeatmap/MonthHeatmap props — levels array shape, today index)
    - apps/mobile/src/components/RatingDistribution.tsx (expected ratingCounts/segments props)
    - .planning/phases/03-streak-detail-design-fidelity/03-UI-SPEC.md (Activity section, Reviews chart, Rating distribution, Personal bests, Empty-state table)
    - .planning/phases/03-streak-detail-design-fidelity/03-CONTEXT.md (D-04/05/06 bests, D-07 heatmap windows, Discretion: today outlined)
  </read_first>
  <action>
    Remove YEAR_LEVELS/genLevel/YEAR_TODAY/YEAR_MONTHS/MONTH_DAYS/MONTH_TODAY/REVIEWS/BESTS placeholder constants. Derive the YearHeatmap levels array (371 cells) from `streakStats.year` perDay/heatLevels: map each trailing-53-week day-key to its 0-3 level (default 0 / heat-0 when absent); compute the today index as the last cell and keep it outlined (existing today-cell styling — never filled). Derive MonthHeatmap from `streakStats.month` (calendar-grid leading empty slots + per-day levels, today outlined). Feed the inline ReviewsChart and LifetimeBars from `streakStats[period].perMonth` (year/lifetime) and month perDay/perMonth — pass `bars` (normalized 0-1 heights) and `labels` props; the last bar stays brand-colored per UI-SPEC. Feed RatingDistribution from `streakStats[period].ratingCounts` (again/hard/good/easy) — empty (all zero) renders the paperSoft2 empty bar + "0%" legend per UI-SPEC. Build the personal-bests rows from `streakStats?.bests`: map each to rank/Num days/date-range, render the "current" badge when `current` is true; when bests is empty or undefined, render the single current-row entry rank "→", "0 days", "today" per D-06 + UI-SPEC empty state so the card is never blank. All component JSX/styles stay as-is — only data sources change.
  </action>
  <verify>
    <automated>cd apps/mobile && corepack pnpm exec tsc --noEmit 2>&1 | grep -i "streak/index" | head; echo "tsc-checked"</automated>
  </verify>
  <acceptance_criteria>
    - YEAR_LEVELS, genLevel, MONTH_DAYS, REVIEWS, BESTS placeholder constants deleted (grep each returns 0 matches)
    - Heatmap levels derive from streakStats (grep `heatLevels` or `perDay` referenced in screen)
    - Personal bests render `streakStats?.bests` and conditionally show the "current" badge (grep `current` referenced)
    - Empty-data path renders one bests row (no crash when bests is []/undefined) — code uses `?? [{...current row}]`
    - mobile `tsc --noEmit` reports no new errors in streak/index.tsx
  </acceptance_criteria>
  <done>Heatmaps, reviews chart, rating distribution, and personal bests all driven by real getStreakStats data with correct empty-state behavior.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Streak detail screen fully wired to real month/year/lifetime aggregation: hero, 2×2 stat grid, year/month heatmaps, lifetime bars, reviews chart, rating distribution, and personal bests — period toggle instant-swaps all of them.</what-built>
  <how-to-verify>
    1. Start the API (`corepack pnpm --filter @portuguese-app/api dev`) and the mobile app; open the streak detail screen (tap the streak chip on Home).
    2. Confirm the hero number, "longest", and "retention %" reflect your real review history (not 13/17/85).
    3. Toggle month → year → lifetime: stat tiles, heatmap, reviews chart, and rating distribution all change instantly with no spinner; month subs read "this month".
    4. Confirm the heatmap "today" cell is outlined (cobalt border), not filled; intensity varies with your activity.
    5. Confirm personal bests lists real runs longest-first with a "current" badge on your ongoing streak.
    6. (Optional, fresh DB) Confirm a 0-review user shows zeros inline, all-heat-0 map, and a single "0 days / today" bests row — never a blank card.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks wrong (which period / which element).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client display of data already user-scoped by the API (Plan 02, T-03-02). No user input, no writes, no new network surface beyond the one read query. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | streak/index.tsx render | accept | Screen renders only the payload the API already scoped to the current user; no cross-user data is reachable client-side. Read-only display, no PII beyond the user's own counts. |
</threat_model>

<verification>
- All placeholder constants removed; `grep -nE "const (HERO|STATS|RATING|YEAR_LEVELS|MONTH_DAYS|REVIEWS|BESTS)" apps/mobile/app/streak/index.tsx` returns 0 matches
- `getStreakStats` and `queryKey: ['streak', 'stats']` present
- mobile `tsc --noEmit` clean for streak/index.tsx
- Human checkpoint approved (real data + instant toggle + empty-state)
</verification>

<success_criteria>
The streak detail screen displays correct real stats for month/year/lifetime (STRK-03), a real activity heatmap (STRK-04), a real rating distribution (STRK-05), and real personal bests (STRK-06), with the locked v6 layout untouched.
</success_criteria>

<output>
Create `.planning/phases/03-streak-detail-design-fidelity/03-03-SUMMARY.md` when done.
</output>
