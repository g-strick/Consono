---
phase: 03-streak-detail-design-fidelity
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - apps/mobile/app/(tabs)/cards/index.tsx
  - apps/mobile/app/(tabs)/add/index.tsx
  - apps/mobile/app/(tabs)/index.tsx
  - apps/mobile/app/review/index.tsx
  - apps/mobile/app/streak/index.tsx
  - .planning/phases/03-streak-detail-design-fidelity/03-DSGN02-CHANGELOG.md
autonomous: true
requirements: [DSGN-02]

must_haves:
  truths:
    - "Audit scope is app-wide and fix-everything per D-01: every cobalt-on-light body-copy violation across home, review, add wizard, library/cards, and streak is found and fixed; no re-UAT of Phase 1/2 screens (D-01)"
    - "Violation definition follows D-02: cobalt (#1F3494 / colors.brand / colors.brandFill) is permitted only on Num, Action, key-action Body tone='brand', active toggle states, and the UI-SPEC Color call sites; any other body cobalt-on-light is a violation"
    - "A written changelog (every file:line changed, before→after) is emitted as the traceability artifact per D-03, since re-UAT is skipped"
    - "Auditing streak/index.tsx happens AFTER Plan 03 rewrites it, so any cobalt Plan 03 introduced is caught in the same pass"
  artifacts:
    - path: ".planning/phases/03-streak-detail-design-fidelity/03-DSGN02-CHANGELOG.md"
      provides: "DSGN-02 audit changelog (file:line, before→after) per D-03"
      contains: "before"
  key_links:
    - from: "audited screens"
      to: "apps/mobile/src/components/Type.tsx textForSurface system"
      via: "tone prop / textColors instead of hardcoded cobalt"
      pattern: "tone=\"brand\"|textColors"
---

<objective>
Run the app-wide DSGN-02 surface-text-rule audit (D-01: fix everything). Find every body-copy
use of cobalt (#1F3494 / colors.brand / colors.brandFill) on a light surface that is NOT a Num,
Action, key-action `Body tone="brand"`, active toggle, or an enumerated UI-SPEC call site, and
replace it with the surface-aware tone system (`tone` prop / `textColors`). Emit a written
changelog (D-03) as the traceability artifact — re-UAT is intentionally skipped (D-01).

Purpose: Enforce "text follows surface, not brand" everywhere (DSGN-02). Runs last (wave 4) so it
also audits the streak screen Plan 03 just rewrote.

Output: tone-corrected screens + `03-DSGN02-CHANGELOG.md`.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-streak-detail-design-fidelity/03-CONTEXT.md
@.planning/phases/03-streak-detail-design-fidelity/03-PATTERNS.md
@.planning/phases/03-streak-detail-design-fidelity/03-UI-SPEC.md

<interfaces>
The correct surface-aware system — apps/mobile/src/lib/theme.ts + apps/mobile/src/components/Type.tsx:
```typescript
function textForSurface(surface: Surface, tone: TextTone = 'primary'): string;  // theme.ts
// textColors.light = { primary:'#000000', muted:'rgba(0,0,0,0.60)', faint:'rgba(0,0,0,0.42)', brand:'#1F3494' }
// <Num> default tone='brand'  (cobalt OK)
// <Action> default tone='brand'  (cobalt OK)
// <Body tone="brand"> explicit key-action  (cobalt OK)
```

Known violation seeds from PATTERNS.md (verify + fix; find others too):
- app/(tabs)/cards/index.tsx lines ~82,110 : `color: '#1F3494'` on gender/state inline text → use textColors.light.brand or Chip/Body tone="brand"
- app/(tabs)/cards/index.tsx line ~45 : active-filter border cobalt (border OK; verify text inside uses tone)
- app/(tabs)/add/index.tsx line ~1093 : `<Body size={34} style={{ color: colors.brand }}>` → use `<Body tone="brand">` not inline override
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scan all mobile screens for cobalt-on-light body-copy violations and record them</name>
  <files>.planning/phases/03-streak-detail-design-fidelity/03-DSGN02-CHANGELOG.md</files>
  <read_first>
    - apps/mobile/src/lib/theme.ts (textColors table, colors.brand/brandFill, textForSurface)
    - apps/mobile/src/components/Type.tsx (useTextColor, default tones for Num/Action/Body)
    - .planning/phases/03-streak-detail-design-fidelity/03-UI-SPEC.md (Color section call-site whitelist, Surface Text Rule tables, Audit Checklist)
    - .planning/phases/03-streak-detail-design-fidelity/03-PATTERNS.md (DSGN-02 violation seed table + audit execution approach)
  </read_first>
  <action>
    Grep every mobile screen/component for text-color cobalt usage: search for `color: colors.brand`, `color: colors.brandFill`, `color: '#1F3494'`, and `color: '#2E5BC8'` across apps/mobile/app/** and apps/mobile/src/components/**. For EACH hit classify per D-02: is the element a Num, Action, explicit `tone="brand"` key action, active toggle state, or one of the UI-SPEC "Color" whitelist call sites (stat numbers, hero number, nav back/share, current badge, axis last-tick, today border, heatmap fills)? If yes → NOT a violation (record as "allowed, no change"). If it is body copy on a light/paperSoft/brandTint surface and not whitelisted → it is a violation. Exclude `backgroundColor`/`borderColor`/`ActivityIndicator color` (not text). Create 03-DSGN02-CHANGELOG.md listing every hit as a row: `file:line | element | classification (violation|allowed) | planned fix`. Confirm the three PATTERNS.md seeds and add any others found.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-streak-detail-design-fidelity/03-DSGN02-CHANGELOG.md && grep -c "|" .planning/phases/03-streak-detail-design-fidelity/03-DSGN02-CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - 03-DSGN02-CHANGELOG.md exists with one row per cobalt-text hit, each classified violation|allowed
    - The three PATTERNS.md seed locations (cards ~82/110, add ~1093) appear in the changelog
    - Every row names a file:line and a planned fix (or "allowed, no change")
  </acceptance_criteria>
  <done>Complete inventory of cobalt-text usages classified per D-02, recorded in the changelog.</done>
</task>

<task type="auto">
  <name>Task 2: Apply fixes to all violations and record before→after in the changelog</name>
  <files>apps/mobile/app/(tabs)/cards/index.tsx, apps/mobile/app/(tabs)/add/index.tsx, apps/mobile/app/(tabs)/index.tsx, apps/mobile/app/review/index.tsx, apps/mobile/app/streak/index.tsx, .planning/phases/03-streak-detail-design-fidelity/03-DSGN02-CHANGELOG.md</files>
  <read_first>
    - .planning/phases/03-streak-detail-design-fidelity/03-DSGN02-CHANGELOG.md (Task 1 inventory — the violation list to fix)
    - apps/mobile/src/components/Type.tsx (Body/Mono/Num tone API to migrate to)
    - apps/mobile/app/streak/index.tsx (FULL — re-audit AFTER Plan 03's rewrite; catch any cobalt Plan 03 added)
    - .planning/phases/03-streak-detail-design-fidelity/03-UI-SPEC.md (Surface Text Rule — correct replacement tones)
  </read_first>
  <action>
    For each row classified "violation" in the changelog, replace the hardcoded cobalt body text with the surface-aware system: prefer the `tone` prop on the Type primitive (`<Body tone="primary">` for default body, `<Body tone="muted">`/`tone="faint"` for secondary, `<Body tone="brand">` only if it is a genuine key action) over inline `style={{ color }}` overrides; for non-primitive `<Text>` elements use `textColors.light.primary/muted/faint`. Specifically fix add/index.tsx ~1093 `<Body size={34} style={{ color: colors.brand }}>` → `<Body size={34} tone="brand">` (or tone="primary" if it is not a key action — judge by UI-SPEC). Fix cards/index.tsx gender/state inline cobalt text → tone system or Chip/Body tone="brand" per UI-SPEC. Re-audit streak/index.tsx now that Plan 03 rewrote it and fix any cobalt Plan 03 introduced on non-Num/non-Action body copy. Leave border/background/ActivityIndicator cobalt untouched (allowed). For every change append a before→after line to the changelog (`file:line | before: <snippet> | after: <snippet>`). Do not change layout, spacing, sizes, or any whitelisted cobalt — text color only.
  </action>
  <verify>
    <automated>cd apps/mobile && corepack pnpm exec tsc --noEmit 2>&1 | grep -iE "cards/index|add/index|review/index|streak/index|\(tabs\)/index" | head; echo "tsc-checked"</automated>
  </verify>
  <acceptance_criteria>
    - No body-copy cobalt-on-light remains: `grep -rnE "color: '#1F3494'|color: '#2E5BC8'|color: colors\.brand(Fill)?" apps/mobile/app` returns only whitelisted (Num/Action/key-action/border/background) hits, each cross-referenced "allowed" in the changelog
    - add/index.tsx no longer contains `<Body size={34} style={{ color: colors.brand }}>` (grep returns 0)
    - 03-DSGN02-CHANGELOG.md contains a before→after line for every fixed violation (D-03)
    - mobile `tsc --noEmit` reports no new errors in the touched screens
  </acceptance_criteria>
  <done>All DSGN-02 violations fixed via the tone system; every change traced in the changelog (D-03). No re-UAT (D-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Cosmetic text-color refactor. No data flow, no trust boundary, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | text-color edits across screens | accept | Pure presentational changes; no logic, data, or auth touched. No new packages. Type-checked after edits to catch any accidental breakage. |
</threat_model>

<verification>
- `grep -rnE "color: '#1F3494'|color: colors\.brand" apps/mobile/app` — remaining hits all whitelisted in changelog
- 03-DSGN02-CHANGELOG.md has before→after for each fix (D-03)
- mobile `tsc --noEmit` clean for all touched screens
</verification>

<success_criteria>
All body text app-wide follows surface rules — no cobalt-on-white body copy anywhere (DSGN-02 success criterion #3) — and the changelog provides full file:line traceability without re-UAT (D-01/D-03).
</success_criteria>

<output>
Create `.planning/phases/03-streak-detail-design-fidelity/03-04-SUMMARY.md` when done.
</output>
