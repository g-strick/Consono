---
phase: 03-streak-detail-design-fidelity
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/api/src/lib/streakStats.ts
  - apps/api/src/lib/streakStats.test.ts
  - apps/api/src/lib/homeSummary.ts
autonomous: true
requirements: [STRK-03, STRK-04, STRK-05, STRK-06]

must_haves:
  truths:
    - "computeRetention returns True FSRS retention per D-09 (denominator = state_before=='review' reps, numerator = rating!='again', 0 due in window => 0%)"
    - "computeBestRuns returns maximal consecutive-active-day runs ranked longest-first per D-04, top-5, with the ongoing run always flagged current per D-05"
    - "computeBestRuns accepts runs of any length including 1-day per D-06 and breaks equal-length ties by most-recent per D-05"
    - "computeDaysActive / computeReviewsInWindow window exactly by month (calendar month), year (trailing 53 weeks), lifetime per D-07"
    - "heatmap level bucketing (0-3) lives in the pure lib and is unit-tested (Claude's Discretion: quartile-based)"
  artifacts:
    - path: "apps/api/src/lib/streakStats.ts"
      provides: "Pure streak-detail aggregation functions"
      exports: ["computeRetention", "computeBestRuns", "computePerDayCount", "computeDaysActive", "computeReviewsInWindow", "computeHeatLevels"]
      min_lines: 80
    - path: "apps/api/src/lib/streakStats.test.ts"
      provides: "Vitest unit tests for all aggregation functions"
      contains: "describe('computeRetention'"
  key_links:
    - from: "apps/api/src/lib/streakStats.ts"
      to: "apps/api/src/lib/homeSummary.ts"
      via: "import { dayKeyLocal } (re-used, not duplicated)"
      pattern: "import.*dayKeyLocal.*homeSummary"
---

<objective>
Build the pure, unit-tested aggregation library for the streak detail screen. All
month/year/lifetime statistics (retention, personal-best runs, days active, reviews,
heatmap intensity levels) are computed here as side-effect-free functions so they are
fully testable with vitest, mirroring the Phase 2 `homeSummary.ts` pattern.

Purpose: Isolate streak-stats logic from the Hono route so the D-04..D-09 data-semantics
decisions are verified by tests before any wiring. This is the foundation Plan 02 (route)
and Plan 03 (screen) build on.

Output: `apps/api/src/lib/streakStats.ts` + `apps/api/src/lib/streakStats.test.ts`; one
new export added to `homeSummary.ts` if `dayKeyLocal` is not already exported.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-streak-detail-design-fidelity/03-CONTEXT.md
@.planning/phases/03-streak-detail-design-fidelity/03-PATTERNS.md

<interfaces>
<!-- Re-use these from homeSummary.ts. Do NOT duplicate date helpers. -->
From apps/api/src/lib/homeSummary.ts:
```typescript
export function dayKeyLocal(date: Date): string;   // 'YYYY-MM-DD' local-day key (D-02)
export function localDayStart(now: Date): Date;     // 00:00:00 local for now's calendar day
export function computeStreak(reviewedAtDates: Date[], now: Date): number;
```

ReviewRow input shape (from packages/db schema; D-09):
```typescript
type ReviewRow = {
  reviewed_at: Date;
  rating: 'again' | 'hard' | 'good' | 'easy';
  state_before: 'new' | 'learning' | 'review' | 'relearning';
};
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED+GREEN computeRetention (D-09) and window helpers (D-07)</name>
  <files>apps/api/src/lib/streakStats.ts, apps/api/src/lib/streakStats.test.ts, apps/api/src/lib/homeSummary.ts</files>
  <read_first>
    - apps/api/src/lib/homeSummary.ts (FULL — date helpers dayKeyLocal/localDayStart, computeStreak shape, confirm whether dayKeyLocal is already exported)
    - apps/api/src/lib/homeSummary.test.ts (FULL — vitest describe/it/expect pattern + daysAgoFrom helper to copy)
    - .planning/phases/03-streak-detail-design-fidelity/03-CONTEXT.md (decisions D-07, D-09)
    - .planning/phases/03-streak-detail-design-fidelity/03-PATTERNS.md (streakStats.ts section, function signatures)
  </read_first>
  <behavior>
    - computeRetention: 0 reviews in window => 0. Only state_before=='review' reps count toward denominator (excludes new/learning/relearning). Numerator = those with rating!='again'. retention = recalled_due/total_due as a fraction (route formats to %).
    - computeDaysActive(reviewedAtDates, windowStart, windowEnd): count of distinct local-day-keys with >=1 review inside [windowStart, windowEnd].
    - computeReviewsInWindow(reviewedAtDates, windowStart, windowEnd): count of reviews whose reviewed_at falls inside the window.
    - Window boundaries are inclusive of windowStart's local day through windowEnd (now). Month = first-of-calendar-month → now; year = now minus 53 weeks → now; lifetime = earliest review → now. (Window computation lives in the route; the lib functions just take explicit start/end Dates.)
  </behavior>
  <action>
    Create apps/api/src/lib/streakStats.ts with the module header noting D-02 local-midnight boundary and that it imports dayKeyLocal from ./homeSummary.js (do NOT duplicate it — if homeSummary.ts does not already export dayKeyLocal, add `export` to it). Implement computeRetention(reviews: ReviewRow[], windowStart: Date, windowEnd: Date): number, computeDaysActive(reviewedAtDates: Date[], windowStart: Date, windowEnd: Date): number, and computeReviewsInWindow(reviewedAtDates: Date[], windowStart: Date, windowEnd: Date): number. For retention: denominator counts reviews where state_before === 'review' AND reviewed_at within window; numerator subset where rating !== 'again'; return 0 when denominator is 0 (never divide by zero). Write streakStats.test.ts first (RED) using vitest describe/it/expect and a daysAgoFrom(now, n) helper copied from homeSummary.test.ts; cover: zero-due-returns-0, only-review-state-counted, again-excluded-from-numerator, window inclusion/exclusion edges. No fenced code in production file beyond implementation; no DB or Hono imports.
  </action>
  <verify>
    <automated>corepack pnpm --filter @portuguese-app/api exec vitest run src/lib/streakStats.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - streakStats.test.ts exits 0 with computeRetention, computeDaysActive, computeReviewsInWindow describe blocks all green
    - computeRetention returns 0 (not NaN) when no state_before=='review' reps fall in the window
    - streakStats.ts contains no `import.*db` and no `import.*hono` (grep -v '^#' returns 0 matches)
    - dayKeyLocal is imported from './homeSummary.js' in streakStats.ts (not redefined)
  </acceptance_criteria>
  <done>Retention (D-09) and window-count helpers (D-07) implemented and green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: RED+GREEN computeBestRuns (D-04/05/06), computePerDayCount + computeHeatLevels (D-07 + Discretion)</name>
  <files>apps/api/src/lib/streakStats.ts, apps/api/src/lib/streakStats.test.ts</files>
  <read_first>
    - apps/api/src/lib/homeSummary.ts (computeStreak — the consecutive-active-day walk to model for run detection)
    - apps/api/src/lib/streakStats.ts (the functions added in Task 1, to extend the same file)
    - .planning/phases/03-streak-detail-design-fidelity/03-CONTEXT.md (decisions D-04, D-05, D-06; Claude's Discretion: quartile heatmap bucketing)
    - .planning/phases/03-streak-detail-design-fidelity/03-PATTERNS.md (computeBestRuns signature; equal-length tie rule)
  </read_first>
  <behavior>
    - computeBestRuns(reviewedAtDates, now): returns runs of maximal consecutive active days (active day = local-day-key with >=1 review). Ranked longest-first (D-04). Runs of any length incl. 1 day qualify (D-06). Return at most top-5 PLUS the ongoing/current run always present and flagged current:true even if not top-5 (D-05). Equal-length ties: most-recent ranks higher (D-05). Each entry: { days, start: Date, end: Date, current: boolean }. No reviews => empty array (route renders the single current-row empty entry per UI-SPEC).
    - computePerDayCount(reviewedAtDates): Map<localDayKey, count>.
    - computeHeatLevels(perDay: Map<string,number>): map each day-key to an intensity 0-3 via quartile-based bucketing over the non-zero counts (0 count => level 0; remaining counts split into 3 quartile buckets => levels 1/2/3). Single distinct non-zero value => level 3.
  </behavior>
  <action>
    Extend streakStats.ts with computeBestRuns(reviewedAtDates: Date[], now: Date): Array<{ days: number; start: Date; end: Date; current: boolean }>, computePerDayCount(reviewedAtDates: Date[]): Map<string, number>, and computeHeatLevels(perDay: Map<string, number>): Map<string, 0|1|2|3>. Detect runs by sorting distinct active day-keys ascending and grouping consecutive calendar days (use Date arithmetic on day-keys, NOT string compare). Mark a run current when its end day-key === dayKeyLocal(now) OR === dayKeyLocal(yesterday) consistent with computeStreak's grace semantics — match computeStreak's definition of an active ongoing streak. Rank longest-first, ties by latest end date; take top 5, then ensure the current run is included (append if it was truncated). For computeHeatLevels use quartile thresholds over the sorted non-zero counts. Add RED tests first covering: empty=>[], single current run flagged, top-5 ordering, current-always-included-when-not-top-5, equal-length-tie-by-recency, 1-day-run qualifies, heat levels bucket 0/1/2/3 correctly.
  </action>
  <verify>
    <automated>corepack pnpm --filter @portuguese-app/api exec vitest run src/lib/streakStats.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - streakStats.test.ts exits 0 with computeBestRuns, computePerDayCount, computeHeatLevels describe blocks green
    - A test asserts current:true on the ongoing run even when 6+ longer historical runs exist (D-05)
    - A test asserts equal-length runs order most-recent-first (D-05)
    - A test asserts a 1-day run appears in results (D-06)
    - computeHeatLevels returns only values in {0,1,2,3}
  </acceptance_criteria>
  <done>Personal-best runs (D-04/05/06), per-day counts, and heatmap level bucketing implemented and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure functions, no I/O, no untrusted input crosses here. Inputs are already-fetched, already-scoped Date/ReviewRow arrays supplied by the route (Plan 02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | streakStats.ts pure functions | accept | No DB access, no network, no user_id handling in this layer. Scoping is enforced upstream in the route (Plan 02, T-03-02). Pure math over arrays the route already scoped. |
</threat_model>

<verification>
- `corepack pnpm --filter @portuguese-app/api exec vitest run src/lib/streakStats.test.ts` exits 0
- `grep -c "import" apps/api/src/lib/streakStats.ts` shows only the homeSummary date-helper import (no db/hono)
</verification>

<success_criteria>
All six exported functions (computeRetention, computeBestRuns, computePerDayCount, computeDaysActive, computeReviewsInWindow, computeHeatLevels) are implemented as pure functions and covered by passing vitest tests encoding D-04..D-09.
</success_criteria>

<output>
Create `.planning/phases/03-streak-detail-design-fidelity/03-01-SUMMARY.md` when done.
</output>
