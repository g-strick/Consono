---
phase: 03-streak-detail-design-fidelity
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/lib/useNightSurface.ts
  - apps/mobile/src/lib/useNightSurface.test.ts
autonomous: true
requirements: [DSGN-01]

must_haves:
  truths:
    - "DSGN-01 OLED night trigger (built in Phase 1.1) is verified, not rebuilt: night reviews auto-switch to the OLED surface (system dark mode AND hour >= 19 or < 6) and otherwise stay light"
    - "The trigger condition is extracted into a pure, unit-tested predicate so the DSGN-01 success criterion is provable with an automated test (no subjective 'looks right')"
    - "The streak detail screen is confirmed light-surface-only (OLED scope is the review loop, not the whole app) per the UI-SPEC OLED scope note"
  artifacts:
    - path: "apps/mobile/src/lib/useNightSurface.test.ts"
      provides: "Unit test proving the OLED night-trigger truth table"
      contains: "oled"
    - path: "apps/mobile/src/lib/useNightSurface.ts"
      provides: "Exported pure predicate for the OLED trigger condition"
      exports: ["isOledSurface", "useNightSurface"]
  key_links:
    - from: "apps/mobile/app/review/index.tsx"
      to: "useNightSurface"
      via: "surface = useNightSurface() applied to review surfaces"
      pattern: "useNightSurface"
---

<objective>
Verify DSGN-01: the OLED night-review trigger built in Phase 1.1 (`useNightSurface`) actually
fires for night reviews and renders the OLED surface, and does NOT for daytime/light-mode. The
trigger predicate (system dark mode AND hour in [19:00, 06:00)) is currently buried in a private
`isNightHour()`; this plan extracts it into an exported pure function and proves the truth table
with a unit test. No behavior change is expected — this converts "already built, just audit" into
an automated, regression-proof acceptance criterion.

Purpose: Close DSGN-01 with real, testable verification (the trap: do not silently drop it).

Output: exported `isOledSurface(colorScheme, hour)` predicate + `useNightSurface.test.ts`.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-streak-detail-design-fidelity/03-CONTEXT.md
@.planning/phases/03-streak-detail-design-fidelity/03-UI-SPEC.md

<interfaces>
Current — apps/mobile/src/lib/useNightSurface.ts:
```typescript
function isNightHour(): boolean { const hour = new Date().getHours(); return hour >= 19 || hour < 6; }
export function useNightSurface(): Surface { /* dark && isNightHour() ? 'oled' : 'light' */ }
```
UI-SPEC OLED scope: review screen only; streak detail is light-surface always.
review/index.tsx already applies `const nightSurface = useNightSurface()` to its surfaces.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract pure isOledSurface predicate and unit-test the DSGN-01 truth table</name>
  <files>apps/mobile/src/lib/useNightSurface.ts, apps/mobile/src/lib/useNightSurface.test.ts</files>
  <read_first>
    - apps/mobile/src/lib/useNightSurface.ts (FULL — isNightHour logic, useNightSurface, the dark+night rule)
    - apps/mobile/app/review/index.tsx (lines ~45, ~161+ — how nightSurface is consumed, to confirm the wiring is intact)
    - apps/api/src/lib/homeSummary.test.ts (vitest describe/it/expect style to mirror — no @types/jest)
    - .planning/phases/03-streak-detail-design-fidelity/03-UI-SPEC.md (OLED surface section + OLED scope note: review only, streak light)
  </read_first>
  <behavior>
    - isOledSurface('dark', 19) === true; isOledSurface('dark', 23) === true; isOledSurface('dark', 5) === true; isOledSurface('dark', 0) === true.
    - isOledSurface('dark', 18) === false; isOledSurface('dark', 6) === false; isOledSurface('dark', 12) === false (daytime, even in dark mode).
    - isOledSurface('light', 23) === false; isOledSurface(null, 23) === false (light mode at night stays light).
    - useNightSurface() returns the Surface string ('oled' | 'light') by delegating to isOledSurface(useColorScheme(), new Date().getHours()).
  </behavior>
  <action>
    Refactor apps/mobile/src/lib/useNightSurface.ts to export a pure predicate `isOledSurface(colorScheme: 'light' | 'dark' | null | undefined, hour: number): boolean` that returns `colorScheme === 'dark' && (hour >= 19 || hour < 6)`. Re-implement `useNightSurface()` to call `isOledSurface(useColorScheme(), new Date().getHours())` and return 'oled' when true else 'light' — preserving existing behavior and the existing import sites in review/index.tsx (no signature change to useNightSurface). Create apps/mobile/src/lib/useNightSurface.test.ts using vitest describe/it/expect (mirror homeSummary.test.ts; no @types/jest) covering the full truth table in <behavior>: night+dark=>oled across boundary hours (19/23/5/0), daytime=>light (18/6/12), light-mode-night=>light, null-scheme=>light. Do NOT add a manual OLED toggle (deferred). Do NOT change the review screen's surface wiring beyond confirming it still compiles.
  </action>
  <verify>
    <automated>cd apps/mobile && corepack pnpm exec vitest run src/lib/useNightSurface.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - useNightSurface.test.ts exits 0 with the full truth table green (boundary hours 19/06 correct)
    - useNightSurface.ts exports `isOledSurface` and still exports `useNightSurface` with the same signature
    - review/index.tsx still imports and uses useNightSurface (grep confirms the wiring is intact — DSGN-01 trigger reaches the review surface)
    - A test explicitly asserts isOledSurface('light', 23) === false and isOledSurface('dark', 12) === false
  </acceptance_criteria>
  <done>DSGN-01 OLED night-trigger condition is verified by an automated truth-table test; review-screen wiring confirmed intact; streak screen unaffected (light-only).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure presentational predicate (color scheme + hour → surface). No data flow, no input validation, no trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | useNightSurface refactor | accept | Behavior-preserving extraction of a pure predicate, covered by a unit test that locks the truth table. No new packages, no logic/data/auth change. |
</threat_model>

<verification>
- `cd apps/mobile && corepack pnpm exec vitest run src/lib/useNightSurface.test.ts` exits 0
- `grep -n "isOledSurface" apps/mobile/src/lib/useNightSurface.ts` returns the export
- `grep -n "useNightSurface" apps/mobile/app/review/index.tsx` confirms the trigger still wired to the review surface
</verification>

<success_criteria>
DSGN-01 is verified, not assumed: night reviews auto-switch to the OLED theme (success criterion #4) and the trigger condition is locked by an automated test; the streak screen remains light-surface-only per the UI-SPEC OLED scope.
</success_criteria>

<output>
Create `.planning/phases/03-streak-detail-design-fidelity/03-05-SUMMARY.md` when done.
</output>
