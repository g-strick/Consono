---
phase: 06-auth-cloud-sync
plan: 06
type: execute
wave: 4
depends_on: ['06-04', '06-05']
files_modified:
  - scripts/migrate-audio-to-storage.ts
  - apps/api/src/routes/cards.ts
autonomous: false
requirements: [AUTH-03]

must_haves:
  truths:
    - 'Every existing audio_clips.storage_url is an https:// Supabase Storage URL (no local paths remain)'
    - 'New cards (POST /cards) and edited sentences (PATCH /cards/:id) store https:// Storage URLs, not local paths'
    - 'Cards routes emit the absolute storage_url as audio_url/sentence_audio_url (no /audio/:hash construction)'
  artifacts:
    - path: 'scripts/migrate-audio-to-storage.ts'
      provides: 'One-time backfill: local-path clips → Storage upload → storage_url update'
      contains: 'uploadAudio'
    - path: 'apps/api/src/routes/cards.ts'
      provides: 'Write-path calls uploadAudio; read-path JOINs audio_clips and emits storage_url'
      contains: 'uploadAudio'
  key_links:
    - from: 'apps/api/src/routes/cards.ts read-path'
      to: 'audio_clips.storage_url'
      via: 'leftJoin on sentence_audio_clip_hash + audio_clip_hash, emit storage_url'
      pattern: "leftJoin\\(audio_clips"
    - from: 'apps/api/src/routes/cards.ts write-path'
      to: 'uploadAudio'
      via: 'readFileSync(synthesize result path) → uploadAudio(hash, buffer)'
      pattern: "uploadAudio\\("
---

<objective>
Complete the server-side audio cutover to Supabase Storage atomically: (1) a one-time migration script
that uploads every locally-stored clip and rewrites its storage_url to the CDN URL; (2) the cards.ts
write-path so new/edited card audio uploads to Storage; (3) the cards.ts read-path so responses emit
the absolute storage_url instead of building `/audio/:hash` paths.

Purpose: AUTH-03. RESEARCH "Don't Hand-Roll" key insight + Pitfall 6 warn that data migration alone is
insufficient — the read-path construction and write-path must change together or audio breaks. All three
server-side pieces ship in this one plan so the cutover is atomic. The mobile `BASE`-prepend removal
(plan 07) is the matching client half and must land immediately after.
Output: migration script (run once), updated cards.ts write+read paths.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/Consono/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/Consono/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-auth-cloud-sync/06-RESEARCH.md
@apps/api/src/routes/cards.ts
@apps/api/src/lib/audio.ts
@packages/db/src/schema.ts

<interfaces>
From apps/api/src/lib/storage.ts (plan 04): `uploadAudio(hash: string, buffer: Buffer): Promise<string>`
returns the public https:// URL; dedup-safe; throws on real failure.
From apps/api/src/lib/audio.ts: `contentHash(text, provider='narakeet', voiceId='felipe'): string`.
From ../../../mobile/src/providers/tts.js: `synthesize(text) → { audioUrl, durationMs }` where audioUrl
is a LOCAL filesystem path under audio-cache/${hash}.mp3 (RESEARCH Pattern 10 / Assumption A5).

cards.ts current state (from grep):

- Write sites: POST handler ~line 64 (synthesize sentence) inserts audio_clips with storage_url = sentenceAudio.audioUrl (a LOCAL path — must become uploadAudio result); PATCH handler ~line 208 (re-synthesize) same bug.
- Read sites (build `/audio/${hash}`): lines 130-132, 148-150, 168-170, 241-243. audio_url uses audio_clip_hash; sentence_audio_url uses sentence_audio_clip_hash. BOTH columns must be JOINed.
- audio_clips schema: content_hash (PK), storage_url, text, provider, voice_id, duration_ms.

scripts/ dir does not yet exist at repo root — create it. Migration source dir: audio-cache/${content_hash}.mp3.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update cards.ts write-path to upload audio to Storage</name>
  <files>apps/api/src/routes/cards.ts</files>
  <read_first>
    - apps/api/src/routes/cards.ts (POST handler ~line 64; PATCH handler ~line 208)
    - apps/api/src/lib/storage.ts (uploadAudio)
    - .planning/phases/06-auth-cloud-sync/06-RESEARCH.md (Pattern 10 — Option W1 wrap synthesize at call site)
  </read_first>
  <action>Apply Option W1 (RESEARCH Pattern 10): at both the POST (~line 64) and PATCH (~line 208) synthesize sites, after `const sentenceAudio = await synthesize(sentence)` and computing `const sentenceHash = contentHash(sentence)`, read the buffer with `readFileSync(sentenceAudio.audioUrl)` (import readFileSync from 'fs') and call `const storageUrl = await uploadAudio(sentenceHash, sentenceAudio buffer)`. Set the audio_clips insert's `storage_url` to `storageUrl` instead of `sentenceAudio.audioUrl`. Import uploadAudio from '../lib/storage.js'. Keep the existing onConflictDoNothing — uploadAudio is dedup-safe and returns the URL even for already-uploaded files. Do not change synthesize() (keeps audio-cache as local dedup cache, preserves the TTSProvider boundary per ADR 0005).</action>
  <verify>
    <automated>grep -c 'uploadAudio(' apps/api/src/routes/cards.ts | grep -qv '^0$' && ! grep -nE "storage_url: sentenceAudio\.audioUrl" apps/api/src/routes/cards.ts</automated>
  </verify>
  <acceptance_criteria>
    - cards.ts imports uploadAudio and readFileSync
    - Both POST and PATCH synthesize sites call uploadAudio and store the returned https:// URL
    - No remaining `storage_url: sentenceAudio.audioUrl` (local path) assignment
  </acceptance_criteria>
  <done>New and edited card audio uploads to Storage; storage_url is an https:// URL.</done>
</task>

<task type="auto">
  <name>Task 2: Update cards.ts read-path to emit storage_url directly</name>
  <files>apps/api/src/routes/cards.ts</files>
  <read_first>
    - apps/api/src/routes/cards.ts (read sites lines 130-132, 148-150, 168-170, 241-243)
    - .planning/phases/06-auth-cloud-sync/06-RESEARCH.md (Pattern 3 Option A — JOIN both clip columns)
  </read_first>
  <action>Apply RESEARCH Pattern 3 Option A. For each response that returns audio_url / sentence_audio_url (GET /cards, GET /cards/due, GET /cards/:id, and the PATCH response), LEFT JOIN audio_clips twice with distinct aliases — one on `cards.sentence_audio_clip_hash = sentenceClip.content_hash`, one on `cards.audio_clip_hash = wordClip.content_hash` — and emit `sentence_audio_url: sentenceClip.storage_url` and `audio_url: wordClip.storage_url` (the absolute https:// URLs). Remove every `` `/audio/${...}` `` template-string construction. Where queries currently use the relational-query (db.query.cards.findFirst/findMany) form, either switch to a db.select().leftJoin() builder or fetch the storage_url via a follow-up lookup keyed by the hashes — pick the select+leftJoin form for the list endpoints to avoid N+1. Confirm zero `/audio/${` constructions remain in cards.ts.</action>
  <verify>
    <automated>! grep -nE '/audio/\$\{' apps/api/src/routes/cards.ts && grep -q 'leftJoin' apps/api/src/routes/cards.ts && grep -q 'storage_url' apps/api/src/routes/cards.ts</automated>
  </verify>
  <acceptance_criteria>
    - No `` `/audio/${` `` template construction remains anywhere in cards.ts
    - Both audio_url and sentence_audio_url are sourced from audio_clips.storage_url via leftJoin
    - `make typecheck` exits 0 for apps/api
  </acceptance_criteria>
  <done>Cards routes emit absolute Storage URLs; no /audio/:hash construction remains.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Write and run the one-time audio migration script</name>
  <read_first>
    - .planning/phases/06-auth-cloud-sync/06-RESEARCH.md (Pattern 5 — migration script; Pitfall 5 — upload-then-update ordering)
    - apps/api/src/lib/storage.ts (uploadAudio)
    - packages/db/src/schema.ts (audio_clips columns)
  </read_first>
  <what-built>Claude writes scripts/migrate-audio-to-storage.ts and runs it. It selects audio_clips where storage_url LIKE '/%' (local paths), reads audio-cache/${content_hash}.mp3, uploads via uploadAudio, and updates storage_url to the returned https:// URL — only after the upload succeeds (Pitfall 5). The human confirms audio plays from a cloud URL on a real device.</what-built>
  <how-to-verify>
    1. Claude runs the script (e.g. `pnpm tsx scripts/migrate-audio-to-storage.ts`) and shows the per-clip "Migrated <hash> → https://..." output.
    2. Claude runs the SQL check: `SELECT count(*) FROM audio_clips WHERE storage_url LIKE '/%'` — must return 0.
    3. On a device/simulator, open a card and confirm sentence audio plays (the URL is now a Supabase CDN URL). Note: mobile BASE-prepend removal is plan 07 — if audio is doubly-prefixed, that is expected until 07 lands; verify the raw URL in the API response instead via `curl` of GET /cards.
    4. Do NOT delete audio-cache/ yet — retain until the full phase is human-verified (plan 08).
  </how-to-verify>
  <verify>
    <automated>test -f scripts/migrate-audio-to-storage.ts && grep -q 'uploadAudio' scripts/migrate-audio-to-storage.ts && grep -q "like(audio_clips.storage_url, '/%')" scripts/migrate-audio-to-storage.ts</automated>
    <human-check>SELECT count(*) FROM audio_clips WHERE storage_url LIKE '/%' returns 0; audio file at a storage_url returns 200</human-check>
  </verify>
  <resume-signal>Type "audio migrated" once no local paths remain and a storage_url resolves to a real MP3, or describe the failure</resume-signal>
  <action>Write scripts/migrate-audio-to-storage.ts per RESEARCH Pattern 5: select audio_clips WHERE storage_url LIKE '/%', for each read audio-cache/${content_hash}.mp3 via readFileSync, call uploadAudio(content_hash, buffer), then UPDATE storage_url = returned URL keyed by content_hash. Update the DB row ONLY after uploadAudio returns without throwing (Pitfall 5). Log each migration. Run the script. Verify the SQL count of local-path rows is 0.</action>
  <acceptance_criteria>
    - scripts/migrate-audio-to-storage.ts uses uploadAudio and filters `like(audio_clips.storage_url, '/%')`
    - storage_url is updated only after a successful upload (no update on throw)
    - After running: zero audio_clips rows have a storage_url starting with '/'
  </acceptance_criteria>
  <done>All existing clips migrated to Storage; no local paths remain; a migrated URL serves a real MP3.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                    |
| ---------------------- | -------------------------------------------------------------- |
| API → Supabase Storage | audio buffers uploaded server-side via service-role key        |
| migration script → DB  | rewrites storage_url; must not corrupt rows on partial failure |

## STRIDE Threat Register

| Threat ID | Category               | Component                            | Disposition | Mitigation Plan                                                                                                     |
| --------- | ---------------------- | ------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------- |
| T-06-15   | Tampering              | migration partial failure            | mitigate    | Update storage_url only after uploadAudio succeeds (Pitfall 5); upsert:false surfaces conflicts                     |
| T-06-16   | Information Disclosure | broken audio from incomplete cutover | mitigate    | Write-path + read-path + migration ship atomically in this plan; grep-gate proves no /audio/${ construction remains |
| T-06-17   | Information Disclosure | audio CDN URL                        | accept      | Public bucket, content-addressed, non-secret (RESEARCH Anti-Patterns)                                               |
| T-06-SC   | Tampering              | npm/pip/cargo installs               | accept      | No package installs (uploadAudio + supabase-js already added in plan 04)                                            |

</threat_model>

<verification>
- No `/audio/${` construction in cards.ts; both audio columns JOINed to storage_url
- Write-path calls uploadAudio at POST + PATCH sites
- Migration leaves zero local-path storage_url rows
- `make typecheck` green
</verification>

<success_criteria>

1. Existing clips migrated to Storage (no local paths)
2. New/edited card audio stored as https:// URLs
3. Cards routes emit storage_url directly
   </success_criteria>

<output>
Create `.planning/phases/06-auth-cloud-sync/06-06-SUMMARY.md` when done. Note that plan 07 must remove
the mobile BASE prepend to complete the cutover, and that GET /audio/:hash route removal is deferred to
plan 08 after full verification.
</output>
