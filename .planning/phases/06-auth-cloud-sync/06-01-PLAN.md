---
phase: 06-auth-cloud-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - .planning/phases/06-auth-cloud-sync/06-JWT-ALGO.md
autonomous: false
requirements: [AUTH-01, AUTH-03]
user_setup:
  - service: supabase
    why: "Auth (email/password), Storage (audio bucket), and JWT verification all run on the existing Supabase project"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard → Project Settings → API → Project URL"
      - name: SUPABASE_ANON_KEY
        source: "Supabase Dashboard → Project Settings → API → Project API keys → anon/public"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Project Settings → API → Project API keys → service_role (secret)"
      - name: SUPABASE_JWT_SECRET
        source: "Supabase Dashboard → Project Settings → JWT Keys → JWT Secret (HS256 path only; leave blank if ES256)"
      - name: EXPO_PUBLIC_SUPABASE_URL
        source: "Same value as SUPABASE_URL"
      - name: EXPO_PUBLIC_SUPABASE_ANON_KEY
        source: "Same value as SUPABASE_ANON_KEY"
    dashboard_config:
      - task: "Create a Storage bucket named exactly 'audio' and set it to Public"
        location: "Supabase Dashboard → Storage → New bucket → name=audio, Public bucket=ON"

must_haves:
  truths:
    - "All six Supabase env vars are present in .env (not placeholder values)"
    - "A public Storage bucket named 'audio' exists"
    - "The project's JWT signing algorithm (ES256 vs HS256) is determined and recorded"
  artifacts:
    - path: ".env.example"
      provides: "Documented placeholders for all six Supabase env vars"
      contains: "SUPABASE_SERVICE_ROLE_KEY"
    - path: ".planning/phases/06-auth-cloud-sync/06-JWT-ALGO.md"
      provides: "Recorded JWT algorithm decision (ES256 or HS256) consumed by plan 03"
      contains: "ALGORITHM:"
  key_links:
    - from: ".env"
      to: "Supabase project"
      via: "SUPABASE_URL points at the JWKS endpoint used by plan 03"
      pattern: "SUPABASE_URL="
---

<objective>
Provision all Supabase configuration that the rest of Phase 6 depends on: the six missing
environment variables, the public `audio` Storage bucket, and a deterministic decision on the
project's JWT signing algorithm (ES256 vs HS256). This is the Wave 0 unblock for every other plan.

Purpose: Every downstream plan needs these env vars and the algorithm branch. Resolving them once,
up front, prevents a 401-on-all-requests dead end (RESEARCH Pitfall 3) and a missing-bucket failure
mid-migration.
Output: Populated `.env`, documented `.env.example`, created Storage bucket, and `06-JWT-ALGO.md`
recording the algorithm.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-auth-cloud-sync/06-RESEARCH.md
@.env.example
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Add Supabase env vars and create the public audio bucket</name>
  <read_first>
    - .env.example (current documented vars)
    - .planning/phases/06-auth-cloud-sync/06-RESEARCH.md (Environment Availability table — exact var names and dashboard locations)
  </read_first>
  <what-built>
    The executor has prepared the placeholder rows in .env.example (next task). This checkpoint is
    the part Claude literally cannot do: reading secret keys from the Supabase dashboard and creating
    a Storage bucket.
  </what-built>
  <how-to-verify>
    1. Open Supabase Dashboard → Project Settings → API. Copy: Project URL, anon/public key, service_role key.
    2. Open Project Settings → JWT Keys. If the project shows asymmetric ES256 keys, note "ES256"; if it shows a JWT Secret (symmetric), copy the JWT Secret and note "HS256".
    3. Add these to the project root .env file:
       SUPABASE_URL=, SUPABASE_ANON_KEY=, SUPABASE_SERVICE_ROLE_KEY=, SUPABASE_JWT_SECRET= (only if HS256),
       EXPO_PUBLIC_SUPABASE_URL= (same as SUPABASE_URL), EXPO_PUBLIC_SUPABASE_ANON_KEY= (same as anon key).
    4. Open Dashboard → Storage → New bucket. Name it exactly `audio`. Toggle "Public bucket" ON. Create.
    5. Confirm the bucket appears in the Storage list and is marked Public.
  </how-to-verify>
  <verify>
    <automated>MISSING — secrets and bucket creation are dashboard-only; verified by the next automated task probing the JWKS endpoint</automated>
    <human-check>SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY present in .env with real values; 'audio' bucket exists and is Public</human-check>
  </verify>
  <resume-signal>Type "env added" once .env contains the six vars and the public 'audio' bucket exists, or describe what failed</resume-signal>
  <action>Human-only: read secret keys from the Supabase dashboard, write them into root .env, and create the public Storage bucket named 'audio'. Record whether JWT Keys shows ES256 (asymmetric) or a JWT Secret (HS256). The next task probes the JWKS endpoint to confirm the algorithm programmatically.</action>
  <acceptance_criteria>
    - Root .env contains non-placeholder values for SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, EXPO_PUBLIC_SUPABASE_URL, EXPO_PUBLIC_SUPABASE_ANON_KEY
    - A Storage bucket named exactly `audio` exists and is set to Public in the Supabase dashboard
  </acceptance_criteria>
  <done>Six env vars present in .env; public 'audio' bucket created.</done>
</task>

<task type="auto">
  <name>Task 2: Document env placeholders and probe the JWT signing algorithm</name>
  <files>.env.example, .planning/phases/06-auth-cloud-sync/06-JWT-ALGO.md</files>
  <read_first>
    - .env.example (append-only; preserve existing NARAKEET/PEXELS/OPENROUTER/DATABASE_URL lines)
    - .planning/phases/06-auth-cloud-sync/06-RESEARCH.md (Pattern 1 — JWKS endpoint path; Pitfall 3 — algorithm branch)
  </read_first>
  <action>Append placeholder rows to .env.example for SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_JWT_SECRET, EXPO_PUBLIC_SUPABASE_URL, EXPO_PUBLIC_SUPABASE_ANON_KEY with a comment pointing to "Supabase Dashboard → Project Settings → API". Then load .env and curl the JWKS endpoint at `${SUPABASE_URL}/auth/v1/.well-known/jwks.json`. If the response is HTTP 200 with a non-empty `keys` array, the project uses asymmetric ES256 — record ALGORITHM: ES256. If the response is 404 or an empty keys array, the project uses symmetric HS256 — record ALGORITHM: HS256. Write the result to 06-JWT-ALGO.md including a line `ALGORITHM: <ES256|HS256>` and the raw JWKS probe output as evidence. Plan 03 reads this file to choose between hono/jwk and hono/jwt.</action>
  <verify>
    <automated>test -f .planning/phases/06-auth-cloud-sync/06-JWT-ALGO.md && grep -qE '^ALGORITHM: (ES256|HS256)$' .planning/phases/06-auth-cloud-sync/06-JWT-ALGO.md && grep -q 'SUPABASE_SERVICE_ROLE_KEY' .env.example</automated>
  </verify>
  <acceptance_criteria>
    - .env.example contains a line beginning `SUPABASE_SERVICE_ROLE_KEY=` and `EXPO_PUBLIC_SUPABASE_ANON_KEY=`
    - 06-JWT-ALGO.md contains exactly one line matching `^ALGORITHM: (ES256|HS256)$`
    - 06-JWT-ALGO.md includes the raw JWKS endpoint probe output (status + body excerpt)
  </acceptance_criteria>
  <done>.env.example documents all six vars; 06-JWT-ALGO.md records the resolved algorithm with probe evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → Supabase dashboard | Secret keys (service_role, JWT secret) cross into local .env |
| .env → git | Secrets must never be committed; only .env.example placeholders are tracked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | SUPABASE_SERVICE_ROLE_KEY in .env | mitigate | Key lives only in root .env (gitignored); never in EXPO_PUBLIC_* or .env.example; .env.example carries placeholders only |
| T-06-02 | Spoofing | JWT algorithm misconfiguration | mitigate | Probe JWKS endpoint to deterministically resolve ES256 vs HS256 before any middleware is written |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan; package legitimacy handled in install plans (03, 05, 07) |
</threat_model>

<verification>
- .env contains six real Supabase values (human-verified at checkpoint)
- `audio` public bucket exists (human-verified at checkpoint)
- 06-JWT-ALGO.md records ES256 or HS256 with probe evidence
- .env.example documents all six placeholders
</verification>

<success_criteria>
1. All six Supabase env vars present in .env with real values
2. Public Storage bucket `audio` created
3. JWT algorithm resolved and recorded in 06-JWT-ALGO.md
</success_criteria>

<output>
Create `.planning/phases/06-auth-cloud-sync/06-01-SUMMARY.md` when done. Record the resolved
ALGORITHM value prominently so plan 03 can consume it.
</output>
