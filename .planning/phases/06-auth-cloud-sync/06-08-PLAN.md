---
phase: 06-auth-cloud-sync
plan: 08
type: execute
wave: 6
depends_on: ['06-07']
files_modified:
  - apps/mobile/app/(auth)/_layout.tsx
  - apps/mobile/app/(auth)/sign-in.tsx
  - apps/mobile/app/(auth)/sign-up.tsx
  - apps/mobile/app/_layout.tsx
  - apps/api/src/index.ts
  - apps/api/src/routes/audio.ts
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03]

must_haves:
  truths:
    - 'An unauthenticated user sees the sign-in screen; an authenticated user sees the tabs'
    - 'A user can sign up (email/password/display_name) and a public.users row is auto-created'
    - "After signing up, the legacy V0 user's cards belong to the real user (data cutover)"
    - 'Cards added on device A appear on device B for the same account, and audio plays from the CDN'
    - 'The legacy GET /audio/:hash route is removed'
  artifacts:
    - path: 'apps/mobile/app/(auth)/sign-in.tsx'
      provides: 'Email/password sign-in screen'
      contains: 'signInWithPassword'
    - path: 'apps/mobile/app/(auth)/sign-up.tsx'
      provides: 'Email/password/display_name sign-up screen'
      contains: 'signUp'
    - path: 'apps/mobile/app/_layout.tsx'
      provides: 'Auth-gated routing + AppState auto-refresh'
      contains: 'onAuthStateChange'
  key_links:
    - from: 'apps/mobile/app/_layout.tsx'
      to: 'supabase.auth.onAuthStateChange'
      via: 'session state drives (auth) vs (tabs) routing with a loading gate'
      pattern: 'onAuthStateChange'
---

<objective>
Ship the mobile auth experience and finish the migration: sign-in/sign-up screens, auth-gated root
routing with an AppState token-refresh listener, the one-time V0→real-user data cutover, removal of the
legacy `GET /audio/:hash` route, and the cross-device human verification that closes the phase.

Purpose: AUTH-01 (sign-up/sign-in + session), AUTH-02 (cross-device sync), AUTH-03 (cloud audio).
RESEARCH Pattern 7 (AppState), Pattern 8 (V0 cutover sequencing — runs only after a real user exists),
Pitfall 7 (navigation-loop avoidance via loading gate).
Output: (auth) route group, gated \_layout.tsx, V0 cutover, /audio route removed, device-verified.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/Consono/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/Consono/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-auth-cloud-sync/06-RESEARCH.md
@apps/mobile/app/_layout.tsx
@apps/api/src/index.ts

<interfaces>
From apps/mobile/src/lib/supabase.ts (plan 07): exported `supabase` singleton.
supabase.auth.signUp({ email, password, options: { data: { display_name } } }) — display_name is
required so the trigger COALESCE has a value (RESEARCH Pattern 8 / Pitfall 4).
supabase.auth.signInWithPassword({ email, password }).
supabase.auth.onAuthStateChange((event, session) => ...) — source of truth for routing (Pitfall 7).
supabase.auth.startAutoRefresh() / stopAutoRefresh() — toggle on AppState change (Pattern 7).

Current app/\_layout.tsx: a Stack with (tabs), review/index, streak/index, cards/[id]; fonts gate render.
Add a sibling (auth) group and conditional routing. Expo Router groups: app/(auth)/\_layout.tsx etc.
apps/api/src/index.ts mounts `app.route('/audio', audioRoute)` (line ~17) and imports audioRoute (line ~7).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the (auth) sign-in / sign-up screens</name>
  <files>apps/mobile/app/(auth)/_layout.tsx, apps/mobile/app/(auth)/sign-in.tsx, apps/mobile/app/(auth)/sign-up.tsx</files>
  <read_first>
    - apps/mobile/app/_layout.tsx (Stack + theme/Surface conventions)
    - apps/mobile/src/lib/supabase.ts (supabase singleton)
    - apps/mobile/src/lib/theme.ts (Surface + Type primitives for consistent styling)
    - .planning/phases/06-auth-cloud-sync/06-RESEARCH.md (Code Examples — signUp/signInWithPassword)
  </read_first>
  <action>Create app/(auth)/_layout.tsx as a Stack with headerShown:false for sign-in and sign-up. Create sign-in.tsx with email + password inputs calling `supabase.auth.signInWithPassword({ email, password })`, surfacing errors loudly (no silent fallback), and a link to sign-up. Create sign-up.tsx with email + password + display_name inputs calling `supabase.auth.signUp({ email, password, options: { data: { display_name } } })` — display_name is required (RESEARCH Pitfall 4). Use the app's existing theme Surface/Type primitives for styling. On success, routing is handled by the root layout's onAuthStateChange (Task 2), so screens need only call the auth methods.</action>
  <verify>
    <automated>grep -q 'signInWithPassword' apps/mobile/app/\(auth\)/sign-in.tsx && grep -q 'signUp' apps/mobile/app/\(auth\)/sign-up.tsx && grep -q 'display_name' apps/mobile/app/\(auth\)/sign-up.tsx</automated>
  </verify>
  <acceptance_criteria>
    - sign-in.tsx calls supabase.auth.signInWithPassword with email/password
    - sign-up.tsx calls supabase.auth.signUp with email/password and options.data.display_name
    - Auth errors are surfaced to the user (visible error text), not swallowed
  </acceptance_criteria>
  <done>Email/password sign-in and sign-up screens exist and call Supabase Auth.</done>
</task>

<task type="auto">
  <name>Task 2: Auth-gate the root layout + AppState refresh</name>
  <files>apps/mobile/app/_layout.tsx</files>
  <read_first>
    - apps/mobile/app/_layout.tsx (current Stack + fonts gate)
    - apps/mobile/src/lib/supabase.ts
    - .planning/phases/06-auth-cloud-sync/06-RESEARCH.md (Pattern 7 AppState; Pitfall 7 navigation loop / loading gate)
  </read_first>
  <action>In _layout.tsx: subscribe to `supabase.auth.onAuthStateChange((_event, session) => setSession(session))` and call `supabase.auth.getSession()` once on mount; keep an `isLoading` state true until the first auth event fires and render null/splash during it (Pitfall 7 — prevents auth↔tabs flicker). When session is null, render the `(auth)` group (or redirect to /(auth)/sign-in); when present, render the existing Stack with (tabs). Register the AppState listener (RESEARCH Pattern 7): on 'active' call supabase.auth.startAutoRefresh(), otherwise stopAutoRefresh(). Add a Stack.Screen entry for the `(auth)` group. Preserve the existing GestureHandlerRootView, QueryClientProvider, ThemeContext, fonts gate, and the review/streak/cards screen registrations.</action>
  <verify>
    <automated>grep -q 'onAuthStateChange' apps/mobile/app/_layout.tsx && grep -q 'startAutoRefresh' apps/mobile/app/_layout.tsx && grep -q 'GestureHandlerRootView' apps/mobile/app/_layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - _layout.tsx uses onAuthStateChange as routing source of truth with an isLoading gate
    - AppState listener calls startAutoRefresh on 'active' and stopAutoRefresh otherwise
    - Existing providers (GestureHandlerRootView, QueryClientProvider, ThemeContext) and screen registrations are preserved
    - `make typecheck` exits 0 for apps/mobile
  </acceptance_criteria>
  <done>Root layout routes by session with a loading gate; tokens auto-refresh on foreground.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the legacy GET /audio/:hash route</name>
  <files>apps/api/src/index.ts, apps/api/src/routes/audio.ts</files>
  <read_first>
    - apps/api/src/index.ts (audioRoute import line ~7; app.route('/audio', ...) line ~17)
    - apps/api/src/routes/audio.ts (route to delete)
    - .planning/phases/06-auth-cloud-sync/06-RESEARCH.md (State of the Art — /audio/:hash replaced; Pitfall 6 — delete last)
  </read_first>
  <action>Now that plans 06 + 07 make all audio URLs absolute CDN URLs, remove the legacy local-file route. Delete apps/api/src/routes/audio.ts. In index.ts remove the `import { audioRoute }` line and the `app.route('/audio', audioRoute)` registration. Confirm no remaining import references audio.ts via typecheck. (This is the "delete the route last, after verifying URL construction is gone" step from Pitfall 6.) Note: apps/api/src/lib/audio.ts — the contentHash helper — is a DIFFERENT file and must NOT be deleted; it is still used by cards.ts.</action>
  <verify>
    <automated>! test -f apps/api/src/routes/audio.ts && ! grep -q "routes/audio" apps/api/src/index.ts && test -f apps/api/src/lib/audio.ts && make typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/routes/audio.ts no longer exists
    - index.ts has no audioRoute import or /audio route registration
    - apps/api/src/lib/audio.ts (contentHash) still exists
    - `make typecheck` exits 0 for apps/api
  </acceptance_criteria>
  <done>Legacy /audio/:hash route removed; contentHash helper retained.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: V0 data cutover + cross-device verification</name>
  <read_first>
    - .planning/phases/06-auth-cloud-sync/06-RESEARCH.md (Pattern 8 sequencing; Runtime State Inventory — V0 data migration options A/B)
    - packages/db/src/schema.ts (cards/lemmas/pending_cards user_id FKs)
  </read_first>
  <what-built>
    All code is in place: auth screens, gated routing, cloud audio, per-user scoping. This checkpoint
    performs the one-time V0→real-user data cutover (which requires a real signed-up user's UUID, only
    available now) and verifies cross-device sync + cloud audio on physical devices.
  </what-built>
  <how-to-verify>
    1. On device A: sign up with the real account (email/password/display_name). Confirm sign-up succeeds (the trigger created a public.users row).
    2. Get the new user's UUID (Supabase Dashboard → Authentication → Users, or `SELECT id, email FROM auth.users`).
    3. Run the V0 cutover (RESEARCH Pattern 8 / Runtime Inventory Option A): `UPDATE cards SET user_id = '<new-uuid>' WHERE user_id = '00000000-0000-0000-0000-000000000001'` and the same for lemmas and pending_cards. Then `DELETE FROM public.users WHERE id = '00000000-0000-0000-0000-000000000001'`. (Claude can run these via the DB connection; the human confirms the UUID and approves.)
    4. On device A: confirm the migrated cards now appear under the real account and audio plays (from the Supabase CDN URL).
    5. On device B (or a reinstall/second simulator): sign in with the same account. Confirm the same cards appear (AUTH-02) and sentence audio plays from the cloud URL (AUTH-03).
    6. Add a card on device A; confirm it appears on device B after refresh.
    7. Only now, after all the above pass, the audio-cache/ directory may be archived/removed.
  </how-to-verify>
  <verify>
    <automated>MISSING — cross-device sync and physical-device audio playback cannot be automated; verified by human on two devices. The V0 cutover SQL is verified by a follow-up count: SELECT count(*) FROM cards WHERE user_id = '00000000-0000-0000-0000-000000000001' returns 0</automated>
    <human-check>Same cards visible on two devices for one account; sentence audio plays from CDN; no cards remain under the V0 UUID</human-check>
  </verify>
  <resume-signal>Type "verified" once cross-device sync + cloud audio work and the V0 cutover is complete, or describe what failed</resume-signal>
  <action>Guide the human through sign-up on device A, capture the real UUID, run the V0→real-user UPDATE/DELETE cutover (Option A), then verify on a second device that cards sync and audio plays from the CDN. Do not remove audio-cache/ until this passes.</action>
  <acceptance_criteria>
    - A real account exists and its public.users row was auto-created by the trigger
    - Zero cards/lemmas/pending_cards remain under the V0 UUID after cutover
    - The same account shows identical cards on two devices, with audio playing from the Supabase CDN
  </acceptance_criteria>
  <done>V0 data migrated to the real user; cross-device sync and cloud audio human-verified.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                       |
| -------------------------------- | ------------------------------------------------- |
| unauthenticated user → app       | must be routed to auth, never into the tabs/data  |
| V0 cutover SQL → production data | one-time destructive UPDATE/DELETE on user_id FKs |

## STRIDE Threat Register

| Threat ID | Category               | Component                     | Disposition | Mitigation Plan                                                                                                |
| --------- | ---------------------- | ----------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------- |
| T-06-21   | Elevation of Privilege | unauth access to tabs         | mitigate    | Root layout gates on session via onAuthStateChange; null session → (auth) group                                |
| T-06-22   | Tampering              | V0 cutover data loss          | mitigate    | Human confirms the real UUID before UPDATE; cutover ordered per Pattern 8; audio-cache retained until verified |
| T-06-23   | Denial of Service      | navigation flicker loop       | mitigate    | isLoading gate until first auth event (Pitfall 7)                                                              |
| T-06-24   | Information Disclosure | session refresh on background | mitigate    | stopAutoRefresh on background; startAutoRefresh on active (Pattern 7)                                          |
| T-06-SC   | Tampering              | npm/pip/cargo installs        | accept      | No new installs (mobile deps added in plan 07)                                                                 |

</threat_model>

<verification>
- onAuthStateChange gates routing; AppState refresh wired
- sign-in/sign-up call Supabase Auth with display_name
- /audio route removed; contentHash retained; typecheck green
- V0 cutover leaves zero cards under the legacy UUID; cross-device + cloud audio human-verified
</verification>

<success_criteria>

1. Unauthenticated → sign-in; authenticated → tabs
2. Sign-up creates account + profile row; data cutover moves V0 cards to the real user
3. Same account sees the same cards on a second device; audio plays from the CDN
   </success_criteria>

<output>
Create `.planning/phases/06-auth-cloud-sync/06-08-SUMMARY.md` when done. Record the real user UUID
(redacted as needed), confirm audio-cache/ disposition, and that all three AUTH success criteria pass.
</output>
