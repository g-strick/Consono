---
phase: 06-auth-cloud-sync
plan: 05
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - apps/api/src/routes/cards.ts
  - apps/api/src/routes/generate.ts
  - apps/api/src/routes/users.ts
  - apps/api/src/routes/home.ts
  - apps/api/src/routes/streak.ts
  - apps/api/src/routes/reviews.ts
  - apps/api/src/routes/reviews.test.ts
  - apps/api/src/lib/constants.ts
  - apps/api/src/seed.ts
autonomous: true
requirements: [AUTH-02]

must_haves:
  truths:
    - "Every authenticated route scopes queries to the JWT user id, not the hardcoded V0 user"
    - "POST /reviews rejects a card_id that does not belong to the authenticated user (403)"
    - "V0_USER_ID is removed from constants.ts so any remaining use is a compile error"
  artifacts:
    - path: "apps/api/src/routes/cards.ts"
      provides: "All 11 user-scoped queries use getUserId(c)"
      contains: "getUserId"
    - path: "apps/api/src/routes/reviews.ts"
      provides: "Card ownership check before review insert"
      contains: "getUserId"
    - path: "apps/api/src/lib/constants.ts"
      provides: "V0_USER_ID export removed"
  key_links:
    - from: "route handlers"
      to: "getUserId(c)"
      via: "const userId = getUserId(c) replacing V0_USER_ID"
      pattern: "getUserId\\(c\\)"
---

<objective>
Replace every `V0_USER_ID` reference across the five route files with the JWT-derived user id from
`getUserId(c)`, add the missing card-ownership check to `POST /reviews` (IDOR fix), and remove the
`V0_USER_ID` export from constants.ts so the TypeScript compiler catches any straggler.

Purpose: AUTH-02 per-user data scoping. RESEARCH Pitfall 6 warns V0_USER_ID lives in 11+ locations
across 5+ files; missing one silently scopes to the wrong user. RESEARCH Security Domain flags
reviews.ts as an IDOR hole — POST /reviews accepts any card_id without ownership verification.
Output: Five route files scoped to getUserId(c), reviews.ts ownership-checked, V0_USER_ID export deleted.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-auth-cloud-sync/06-RESEARCH.md
@apps/api/src/lib/constants.ts
@apps/api/src/routes/cards.ts
@apps/api/src/routes/reviews.ts

<interfaces>
From apps/api/src/lib/auth.ts (plan 03): `getUserId(c: Context): string` returns the JWT sub claim,
throws if absent. authMiddleware is already applied globally in index.ts, so every handler in these
route files runs only for authenticated requests; `c` is in scope inside each handler.

V0_USER_ID occurrence map (from grep):
- cards.ts: lines 81, 93, 125, 142, 162, 190, 232, 235, 262, 272 (10 occurrences) + import line 7
- generate.ts: line 29 (+ import line 7)
- users.ts: line 9 (+ import line 4)
- home.ts: lines 18, 24, 40 (+ import line 4)
- streak.ts: line 37 (+ import line 4)
- reviews.ts: NO V0_USER_ID — currently looks up card by id only (line 37); needs an ownership check.
- seed.ts: uses V0_USER_ID at lines 2,6,9 — leave as-is (seed is dev-only; see action note).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add card-ownership check to POST /reviews</name>
  <files>apps/api/src/routes/reviews.ts, apps/api/src/routes/reviews.test.ts</files>
  <read_first>
    - apps/api/src/routes/reviews.ts (current handler — card lookup at line 37)
    - apps/api/src/lib/auth.ts (getUserId signature)
    - .planning/phases/06-auth-cloud-sync/06-RESEARCH.md (Pattern 2 reviews note; Security Domain IDOR note)
  </read_first>
  <behavior>
    - POST /reviews with a card_id owned by another user returns 403 and inserts no review
    - POST /reviews with a card_id owned by the authenticated user proceeds and inserts the review
  </behavior>
  <action>In reviews.ts import getUserId from '../lib/auth.js'. Compute `const userId = getUserId(c)` at the top of the POST handler. Change the card lookup from `findFirst({ where: eq(cards.id, card_id) })` to `findFirst({ where: and(eq(cards.id, card_id), eq(cards.user_id, userId)) })` (import `and` from drizzle-orm). If the card is not found, return `c.json({ error: 'Card not found' }, 403)` before any review insert. Add reviews.test.ts asserting the 403-on-foreign-card and success-on-owned-card behaviors using a fake getUserId via vi.mock of ../lib/auth.js. Commit: `feat(06-05): reviews card-ownership IDOR fix`.</action>
  <verify>
    <automated>pnpm test -- reviews 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - reviews.ts imports getUserId and `and`
    - The card lookup includes `eq(cards.user_id, userId)`
    - Foreign card_id path returns 403 with no review insert
    - `pnpm test -- reviews` passes
  </acceptance_criteria>
  <done>POST /reviews verifies card ownership; IDOR closed; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Replace V0_USER_ID with getUserId(c) across the five route files</name>
  <files>apps/api/src/routes/cards.ts, apps/api/src/routes/generate.ts, apps/api/src/routes/users.ts, apps/api/src/routes/home.ts, apps/api/src/routes/streak.ts</files>
  <read_first>
    - apps/api/src/routes/cards.ts (10 occurrences + closures that capture user_id)
    - apps/api/src/routes/generate.ts, users.ts, home.ts, streak.ts
    - apps/api/src/lib/auth.ts (getUserId)
  </read_first>
  <action>In each of cards.ts, generate.ts, users.ts, home.ts, streak.ts: remove the `import { V0_USER_ID }` line, add `import { getUserId } from '../lib/auth.js'`, and at the top of each route handler add `const userId = getUserId(c)`. Replace every `V0_USER_ID` with `userId`. Note: several cards.ts queries use the Drizzle relational-query callback form `where: (table, { eq }) => eq(table.user_id, V0_USER_ID)` — because `userId` is captured from the handler scope, the callback closes over it correctly; keep the callback form and swap the constant for `userId`. Verify there are zero remaining `V0_USER_ID` references in apps/api/src/routes/.</action>
  <verify>
    <automated>test "$(grep -rc 'V0_USER_ID' apps/api/src/routes/ | grep -v ':0$' | wc -l)" -eq 0 && grep -rq 'getUserId(c)' apps/api/src/routes/cards.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'V0_USER_ID' apps/api/src/routes/` returns nothing
    - Each of the 5 files imports getUserId and computes `const userId = getUserId(c)` in its handler(s)
    - cards.ts relational-query callbacks reference `userId` (closure), not a literal
  </acceptance_criteria>
  <done>All five route files scoped to the JWT user; no V0_USER_ID left in routes/.</done>
</task>

<task type="auto">
  <name>Task 3: Remove V0_USER_ID export and confirm compile-clean</name>
  <files>apps/api/src/lib/constants.ts</files>
  <read_first>
    - apps/api/src/lib/constants.ts (current export)
    - apps/api/src/seed.ts (dev-only consumer)
  </read_first>
  <action>Delete the `V0_USER_ID` export from constants.ts entirely (RESEARCH Pitfall 6 — make any straggler a compile error). seed.ts still imports it: change seed.ts to define a local `const V0_USER_ID = '00000000-0000-0000-0000-000000000001'` inside seed.ts (seed is a dev-only script that pre-dates auth and may still seed the legacy row for local dev); it must NOT re-import from constants. Run `make typecheck` for apps/api and confirm zero errors — a non-zero result means a route still references the removed export.</action>
  <verify>
    <automated>! grep -q 'export const V0_USER_ID' apps/api/src/lib/constants.ts && make typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - constants.ts no longer exports V0_USER_ID
    - seed.ts defines its own local V0_USER_ID (no import from constants)
    - `make typecheck` exits 0 for apps/api
  </acceptance_criteria>
  <done>V0_USER_ID export removed; typecheck green proving no straggler references remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated request → route handler | user id must come from the verified JWT, never a hardcoded constant |
| client-supplied card_id → reviews insert | card_id is untrusted; ownership must be verified before any write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Elevation of Privilege | POST /reviews card_id | mitigate | Lookup card WHERE id = card_id AND user_id = getUserId(c); 403 if not owned |
| T-06-13 | Spoofing | residual V0_USER_ID scoping | mitigate | Remove the export so any leftover use is a TypeScript compile error |
| T-06-14 | Information Disclosure | cross-user data leak | mitigate | Every route query scoped to JWT userId; grep-gate proves zero V0_USER_ID in routes/ |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `grep -rn 'V0_USER_ID' apps/api/src/routes/` returns nothing
- `make typecheck` green for apps/api
- `pnpm test -- reviews` green
</verification>

<success_criteria>
1. All routes scope to getUserId(c)
2. POST /reviews returns 403 for a foreign card_id
3. V0_USER_ID export removed; typecheck proves no stragglers
</success_criteria>

<output>
Create `.planning/phases/06-auth-cloud-sync/06-05-SUMMARY.md` when done.
</output>
