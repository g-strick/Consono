---
phase: 06-auth-cloud-sync
plan: 04
type: tdd
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/api/src/lib/storage.ts
  - apps/api/src/lib/storage.test.ts
  - apps/api/package.json
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "uploadAudio(hash, buffer) uploads an MP3 to the public 'audio' bucket and returns its https:// CDN URL"
    - "Re-uploading an existing hash does not throw (content-addressed dedup)"
    - "A real upload failure throws loudly (no silent fallback)"
  artifacts:
    - path: "apps/api/src/lib/storage.ts"
      provides: "Supabase service-role Storage client + uploadAudio()"
      exports: ["uploadAudio"]
    - path: "apps/api/src/lib/storage.test.ts"
      provides: "Unit tests with a mocked supabase-js storage client"
      contains: "uploadAudio"
  key_links:
    - from: "apps/api/src/lib/storage.ts"
      to: "Supabase Storage 'audio' bucket"
      via: "supabase.storage.from('audio').upload + getPublicUrl"
      pattern: "from\\('audio'\\)"
---

<objective>
Add a server-side Supabase Storage module that uploads synthesized MP3 audio to the public `audio`
bucket using the service-role key and returns the public CDN URL. This is the write target for both
the one-time migration (plan 06) and the new-card audio write-path (plan 06).

Purpose: AUTH-03 requires audio in Supabase Storage instead of local file paths (RESEARCH Pattern 4).
The service-role client is for Storage only — never DB queries (ADR 0007 rule 1).
Output: `apps/api/src/lib/storage.ts` with `uploadAudio()`, unit tests, and the supabase-js dependency.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-auth-cloud-sync/06-RESEARCH.md
@apps/api/src/lib/audio.ts
@vitest.config.ts

<interfaces>
@supabase/supabase-js 2.108.2 [VERIFIED: npm registry, slopcheck OK — RESEARCH Package Legitimacy Audit]
- createClient(url, serviceRoleKey) → client
- client.storage.from('audio').upload(path, buffer, { contentType: 'audio/mpeg', upsert: false })
  → { error } where error.message === 'The resource already exists' on dedup conflict
- client.storage.from('audio').getPublicUrl(path) → { data: { publicUrl } } (synchronous, no network)
Path convention: `${hash}.mp3` where hash = audio_clips.content_hash (SHA-256 from audio.ts contentHash()).
</interfaces>
</context>

<feature>
  <name>Supabase Storage uploadAudio</name>
  <files>apps/api/src/lib/storage.ts, apps/api/src/lib/storage.test.ts</files>
  <behavior>
    - uploadAudio('abc123', buffer) calls upload('abc123.mp3', buffer, { contentType: 'audio/mpeg', upsert: false }) then returns getPublicUrl('abc123.mp3').data.publicUrl
    - When upload returns error.message === 'The resource already exists', uploadAudio swallows that specific error and still returns the public URL (content-addressed dedup)
    - When upload returns any other error, uploadAudio throws `Storage upload failed: <message>`
    - The returned URL is an absolute https:// URL
  </behavior>
  <implementation>
    Create a module-level supabase client via createClient(process.env['SUPABASE_URL']!,
    process.env['SUPABASE_SERVICE_ROLE_KEY']!). Export async uploadAudio(hash, buffer) per RESEARCH
    Pattern 4. Tests mock '@supabase/supabase-js' (vi.mock) so createClient returns a fake whose
    storage.from('audio') exposes upload() and getPublicUrl() spies. Assert: success path returns the
    mocked publicUrl; 'resource already exists' path does not throw and still returns the URL; an
    arbitrary error path throws 'Storage upload failed:'. No real network calls.
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: Install supabase-js in the API workspace</name>
  <files>apps/api/package.json</files>
  <read_first>
    - apps/api/package.json (current deps)
    - .planning/phases/06-auth-cloud-sync/06-RESEARCH.md (Package Legitimacy Audit — @supabase/supabase-js [OK]; Installation (api))
  </read_first>
  <action>Run `pnpm --filter @portuguese-app/api add @supabase/supabase-js`. @supabase/supabase-js 2.108.2 is verified on npm and slopcheck-OK in RESEARCH (Package Legitimacy Audit), so no human verification checkpoint is required. Do NOT add it to packages/db (DB uses pg driver only — ADR 0007 rule 1).</action>
  <verify>
    <automated>node -e "process.exit(require('./apps/api/package.json').dependencies['@supabase/supabase-js'] ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/package.json dependencies includes @supabase/supabase-js
    - packages/db/package.json does NOT include @supabase/supabase-js
  </acceptance_criteria>
  <done>supabase-js added to the API workspace only.</done>
</task>

<task type="tdd">
  <name>Task 2: RED — failing uploadAudio tests</name>
  <files>apps/api/src/lib/storage.test.ts</files>
  <read_first>
    - apps/api/src/lib/homeSummary.test.ts (vitest mocking style in this package)
    - .planning/phases/06-auth-cloud-sync/06-RESEARCH.md (Pattern 4)
  </read_first>
  <behavior>
    - Test: success path returns the mocked getPublicUrl().data.publicUrl
    - Test: 'The resource already exists' error does not throw and returns the URL
    - Test: any other upload error throws 'Storage upload failed:'
  </behavior>
  <action>Write storage.test.ts that vi.mock('@supabase/supabase-js') so createClient returns a fake with storage.from() → { upload, getPublicUrl } spies. Import uploadAudio from ./storage.js. Assert the three behaviors. Run and confirm FAIL (storage.ts absent). Commit: `test(06-04): add failing uploadAudio tests`.</action>
  <verify>
    <automated>pnpm test -- storage 2>&1 | grep -qiE 'fail|cannot find module|no test|error' && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - storage.test.ts imports uploadAudio and mocks @supabase/supabase-js
    - Suite fails because apps/api/src/lib/storage.ts is absent (RED)
  </acceptance_criteria>
  <done>Failing tests committed describing dedup + throw-on-error contract.</done>
</task>

<task type="tdd">
  <name>Task 3: GREEN — implement storage.ts</name>
  <files>apps/api/src/lib/storage.ts</files>
  <read_first>
    - apps/api/src/lib/storage.test.ts
    - .planning/phases/06-auth-cloud-sync/06-RESEARCH.md (Pattern 4 — exact upload + getPublicUrl)
  </read_first>
  <action>Create storage.ts with a module-level createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY) and exported async uploadAudio(hash, buffer): path = `${hash}.mp3`; call upload(path, buffer, { contentType: 'audio/mpeg', upsert: false }); if error and error.message !== 'The resource already exists' throw `Storage upload failed: ${error.message}`; return getPublicUrl(path).data.publicUrl. Run until GREEN. Commit: `feat(06-04): supabase storage uploadAudio`.</action>
  <verify>
    <automated>pnpm test -- storage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - storage.ts exports `uploadAudio` and uses `from('audio')` + `upsert: false`
    - `pnpm test -- storage` passes
    - `make typecheck` passes for apps/api
  </acceptance_criteria>
  <done>uploadAudio implemented; dedup-safe; throws loudly on real failures; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API server → Supabase Storage | Service-role key grants full Storage access; must stay server-side only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Information Disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | Read only from server env in storage.ts; never imported by mobile or EXPO_PUBLIC_* |
| T-06-10 | Tampering | silent upload failure | mitigate | Throw `Storage upload failed:` on any non-dedup error; no silent fallback |
| T-06-11 | Information Disclosure | public audio bucket | accept | Audio is content-addressed and non-secret; public bucket is intentional (RESEARCH Anti-Patterns) |
| T-06-SC | Tampering | @supabase/supabase-js install | mitigate | Pinned 2.108.2; npm-verified + slopcheck [OK] in RESEARCH; no postinstall network calls |
</threat_model>

<verification>
- `pnpm test -- storage` green
- supabase-js in apps/api only
- uploadAudio returns https:// URL, dedup-safe, throws on real errors
</verification>

<success_criteria>
1. uploadAudio uploads to public 'audio' bucket and returns CDN URL
2. Re-upload of same hash does not throw
3. Real failures throw loudly
</success_criteria>

<output>
Create `.planning/phases/06-auth-cloud-sync/06-04-SUMMARY.md` when done. Export the uploadAudio
signature for plan 06.
</output>
