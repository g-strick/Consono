---
phase: 06-auth-cloud-sync
plan: 07
type: execute
wave: 5
depends_on: ["06-06"]
files_modified:
  - apps/mobile/src/lib/supabase.ts
  - apps/mobile/src/lib/api.ts
  - apps/mobile/package.json
autonomous: true
requirements: [AUTH-01, AUTH-03]

must_haves:
  truths:
    - "The mobile app has a Supabase client whose session is encrypted on device (LargeSecureStore)"
    - "Every API request attaches the current access token as a Bearer header"
    - "audio_url / sentence_audio_url are used as-is (no BASE prepend) since they are absolute https:// URLs"
  artifacts:
    - path: "apps/mobile/src/lib/supabase.ts"
      provides: "createClient with LargeSecureStore adapter; exported supabase singleton"
      exports: ["supabase"]
    - path: "apps/mobile/src/lib/api.ts"
      provides: "request() attaches Bearer token; audio URL BASE prepend removed"
      contains: "getSession"
  key_links:
    - from: "apps/mobile/src/lib/api.ts"
      to: "supabase.auth.getSession()"
      via: "request() reads access_token and sets Authorization header"
      pattern: "Authorization.*Bearer"
---

<objective>
Add the mobile Supabase client with the LargeSecureStore session adapter, attach the access token as a
Bearer header on every API request, and remove the `${BASE}` prepend from audio URLs — completing the
audio cutover started server-side in plan 06.

Purpose: AUTH-01 needs a persisted, encrypted session on device (RESEARCH Pattern 6, Pitfall 2). The API
now requires a Bearer token (plan 03), so api.ts must attach it (Pattern 9). After plan 06 the audio
URLs are absolute https://; prepending BASE would double-prefix and break audio (Pitfall 6 / Anti-Patterns).
Output: supabase.ts singleton, api.ts Bearer attach + BASE-prepend removal, new mobile deps.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-auth-cloud-sync/06-RESEARCH.md
@apps/mobile/src/lib/api.ts

<interfaces>
@supabase/supabase-js 2.108.2, @react-native-async-storage/async-storage 3.1.1,
react-native-url-polyfill 3.0.0, aes-js 3.1.2, react-native-get-random-values 2.0.0
[ALL VERIFIED: npm registry + slopcheck OK — RESEARCH Package Legitimacy Audit]. expo-secure-store 56.0.4
already installed.

api.ts current state (from grep):
- request() helper (line ~20) wraps fetch with `${BASE}${path}` and Content-Type header — must also attach Bearer.
- BASE prepend on audio fields at lines 262-263, 272-273, 300-301 (`audio_url: c.audio_url ? \`${BASE}${c.audio_url}\` : null`).
- Line 314: `return \`${BASE}/audio/${hash}\`` — legacy audio URL builder; this path no longer applies after plan 06 (storage_url is absolute). Remove or stop using it.
- EXPO_PUBLIC_SUPABASE_URL / EXPO_PUBLIC_SUPABASE_ANON_KEY added in plan 01.

Mobile env vars must be EXPO_PUBLIC_* prefixed; SUPABASE_SERVICE_ROLE_KEY must NEVER appear here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install mobile Supabase + session deps</name>
  <files>apps/mobile/package.json</files>
  <read_first>
    - apps/mobile/package.json (current deps; confirm expo-secure-store present)
    - .planning/phases/06-auth-cloud-sync/06-RESEARCH.md (Installation (mobile); Package Legitimacy Audit — all [OK])
  </read_first>
  <action>Run `pnpm --filter @portuguese-app/mobile add @supabase/supabase-js @react-native-async-storage/async-storage react-native-url-polyfill aes-js react-native-get-random-values`. All five are npm-verified and slopcheck-[OK] in RESEARCH (Package Legitimacy Audit), so no human verification checkpoint is required. expo-secure-store is already installed. Also add `base64-js` if not present (used by LargeSecureStore encode/decode in RESEARCH Pattern 6).</action>
  <verify>
    <automated>node -e "const d=require('./apps/mobile/package.json').dependencies; ['@supabase/supabase-js','@react-native-async-storage/async-storage','react-native-url-polyfill','aes-js','react-native-get-random-values'].forEach(p=>{if(!d[p])process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/package.json dependencies include all five new packages
    - SUPABASE_SERVICE_ROLE_KEY appears nowhere in mobile code or env
  </acceptance_criteria>
  <done>Mobile Supabase + encrypted-session deps installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create the mobile Supabase client with LargeSecureStore</name>
  <files>apps/mobile/src/lib/supabase.ts</files>
  <read_first>
    - apps/mobile/src/lib/theme.ts (existing module structure/conventions in src/lib)
    - .planning/phases/06-auth-cloud-sync/06-RESEARCH.md (Pattern 6 — full LargeSecureStore implementation; Pitfall 2)
  </read_first>
  <action>Create supabase.ts per RESEARCH Pattern 6: import 'react-native-url-polyfill/auto' and 'react-native-get-random-values' at the top; implement the LargeSecureStore class (AES-256-CTR via aes-js; key in expo-secure-store, encrypted payload in AsyncStorage; base64-js for encode/decode) exposing getItem/setItem/removeItem. Export `supabase = createClient(EXPO_PUBLIC_SUPABASE_URL, EXPO_PUBLIC_SUPABASE_ANON_KEY, { auth: { storage: new LargeSecureStore(), autoRefreshToken: true, persistSession: true, detectSessionInUrl: false } })`. Read the two env vars from process.env['EXPO_PUBLIC_SUPABASE_URL'] / ['EXPO_PUBLIC_SUPABASE_ANON_KEY']. Do NOT use plain AsyncStorage as the storage adapter (Pitfall 2 — 2048-byte SecureStore limit / plaintext tokens).</action>
  <verify>
    <automated>grep -q 'LargeSecureStore' apps/mobile/src/lib/supabase.ts && grep -q 'EXPO_PUBLIC_SUPABASE_ANON_KEY' apps/mobile/src/lib/supabase.ts && ! grep -q 'SERVICE_ROLE' apps/mobile/src/lib/supabase.ts</automated>
  </verify>
  <acceptance_criteria>
    - supabase.ts exports `supabase` created with `storage: new LargeSecureStore()`
    - Uses EXPO_PUBLIC_SUPABASE_URL + EXPO_PUBLIC_SUPABASE_ANON_KEY
    - Contains no reference to SERVICE_ROLE
  </acceptance_criteria>
  <done>Encrypted-session Supabase singleton available to the mobile app.</done>
</task>

<task type="auto">
  <name>Task 3: Attach Bearer token and remove BASE prepend in api.ts</name>
  <files>apps/mobile/src/lib/api.ts</files>
  <read_first>
    - apps/mobile/src/lib/api.ts (request() ~line 20; BASE prepends at 262-263, 272-273, 300-301; line 314)
    - .planning/phases/06-auth-cloud-sync/06-RESEARCH.md (Pattern 9 — request() Bearer attach; Anti-Patterns — BASE double-prefix)
  </read_first>
  <action>In api.ts: (1) import { supabase } from './supabase'; make request() async-read `const { data: { session } } = await supabase.auth.getSession()` and add `...(session?.access_token ? { Authorization: \`Bearer ${session.access_token}\` } : {})` into the fetch headers (RESEARCH Pattern 9). (2) Remove the `${BASE}` prepend from the three audio-field sites (lines ~262-263, 272-273, 300-301) — set `audio_url: c.audio_url ?? null` and `sentence_audio_url: c.sentence_audio_url ?? null`, because plan 06 makes these absolute https:// URLs (Anti-Patterns: double-prefix). (3) Remove or stop using the legacy `\`${BASE}/audio/${hash}\`` builder at line ~314 (the storage_url is now absolute). BASE is still used for the API path itself in request() — keep that. Confirm no audio field is wrapped in `${BASE}`.</action>
  <verify>
    <automated>grep -q 'Bearer' apps/mobile/src/lib/api.ts && ! grep -nE 'audio_url: c\.audio_url \? `\$\{BASE\}' apps/mobile/src/lib/api.ts && ! grep -nE 'sentence_audio_url: c\.sentence_audio_url \? `\$\{BASE\}' apps/mobile/src/lib/api.ts</automated>
  </verify>
  <acceptance_criteria>
    - request() reads supabase.auth.getSession() and sets `Authorization: Bearer <token>` when a session exists
    - No audio_url / sentence_audio_url field is prefixed with `${BASE}`
    - `${BASE}` is still used to build the API request path in request()
    - `make typecheck` exits 0 for apps/mobile
  </acceptance_criteria>
  <done>Every request carries a Bearer token; audio URLs used as absolute; cutover client-half complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device storage → app | session token must be encrypted at rest, not plaintext |
| mobile app → API | every request must present the verified Bearer token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-18 | Information Disclosure | session token at rest | mitigate | LargeSecureStore: AES-256 payload in AsyncStorage, key in expo-secure-store (Pitfall 2) |
| T-06-19 | Spoofing | unauthenticated API call | mitigate | request() attaches Bearer from supabase.auth.getSession() (Pattern 9) |
| T-06-20 | Information Disclosure | broken audio from double-prefix | mitigate | Remove ${BASE} prepend on absolute storage URLs (Anti-Patterns) |
| T-06-SC | Tampering | 5 mobile package installs | mitigate | All pinned + npm-verified + slopcheck [OK] in RESEARCH; no suspicious postinstall scripts |
</threat_model>

<verification>
- supabase.ts uses LargeSecureStore, no SERVICE_ROLE
- api.ts attaches Bearer; no ${BASE} on audio URLs
- `make typecheck` green for apps/mobile
</verification>

<success_criteria>
1. Encrypted-session Supabase client exists
2. API requests carry Bearer token
3. Audio URLs used as absolute (no double-prefix)
</success_criteria>

<output>
Create `.planning/phases/06-auth-cloud-sync/06-07-SUMMARY.md` when done. Export the supabase singleton
import path for plan 08 auth screens.
</output>
