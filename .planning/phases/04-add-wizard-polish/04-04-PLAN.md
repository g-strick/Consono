---
phase: 04-add-wizard-polish
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - apps/api/src/routes/home.ts
  - apps/mobile/src/lib/api.ts
  - apps/mobile/app/(tabs)/add/index.tsx
autonomous: true
requirements:
  - ADD-06

must_haves:
  truths:
    - "The 'recent' chips in InputStep (when no input) show actual headwords from the user's most-recently-added cards, not hardcoded ['saudade', 'ficar', 'já', 'quase']"
    - 'Tapping a recent chip fills the input field with that headword'
    - 'Frequency-list chips remain as UI scaffolding (no backend for these yet — static is acceptable since ADD-06 only requires recent words)'
  artifacts:
    - path: 'apps/api/src/routes/home.ts'
      provides: 'recentCards includes headword field for word cards (already present in schema)'
      contains: 'recentCards'
    - path: 'apps/mobile/src/lib/api.ts'
      provides: 'HomeSummary.recentCards.headword field is typed and returned'
      contains: 'headword'
    - path: 'apps/mobile/app/(tabs)/add/index.tsx'
      provides: "InputStep recent chips driven by useQuery(['home', 'summary']) recentCards"
      contains: 'recentHeadwords'
  key_links:
    - from: 'InputStep recent chips'
      to: 'HomeSummary.recentCards'
      via: 'useQuery home/summary — data already fetched by home screen; stale ok'
      pattern: 'recentHeadwords'
---

<objective>
Replace the hardcoded 'recent' word chips in InputStep with real data from the existing /home/summary endpoint's recentCards array.

The cards table already stores headword for word cards. The /home/summary response already includes recentCards (id, headword, sentence_pt, gender, state, created_at). No new endpoint is needed — InputStep adds a TanStack Query read of ['home', 'summary'] to extract the word-card headwords and render them as tappable chips.

Purpose: ADD-06 requires recent words on the word input screen. The data already exists in an endpoint the home screen uses — reuse it.

Output: InputStep shows real recent headwords from the user's card library; tapping one fills the input.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/Consono/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/Consono/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/graysonstricker/Programs/Consono/.planning/ROADMAP.md
@/Users/graysonstricker/Programs/Consono/.planning/STATE.md

<interfaces>
<!-- Key contracts from codebase. -->

From apps/mobile/src/lib/api.ts (HomeSummary):
export interface HomeSummary {
totalCards: number;
streak: { count: number; active: boolean; reviewedToday: boolean };
todayStats: { reviewed: number; again: number };
nextDueAt: string | null;
recentCards: RecentCard[];
}
export interface RecentCard {
id: number;
headword: string | null; // null for sentence cards
sentence_pt: string | null;
gender: 'masculine' | 'feminine' | 'common' | null;
state: 'new' | 'learning' | 'review' | 'relearning';
created_at: string;
}

From apps/mobile/app/(tabs)/add/index.tsx (InputStep, current hardcoded chips ~line 433):
{['saudade', 'ficar', 'já', 'quase'].map((w) => (
<TouchableOpacity key={w} onPress={() => onInputChange(w)}>
<Chip label={w} variant="default" />
</TouchableOpacity>
))}

From apps/api/src/routes/home.ts (recentCards shape returned):
const recentCards = recentCardRows.map((card) => ({
id: card.id,
headword: card.headword, // already included
sentence_pt: card.sentence_pt,
gender: card.gender,
state: card.state,
created_at: card.created_at.toISOString(),
}));
// Limit: 3 most-recently-created rows

TanStack Query key used in review/index.tsx for home summary:
queryKey: ['home', 'summary']
queryFn: () => api.getHomeSummary()
staleTime: 30_000
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire InputStep recent chips to real recentCards data</name>
  <files>apps/mobile/app/(tabs)/add/index.tsx, apps/mobile/src/lib/api.ts</files>
  <read_first>
    - apps/mobile/app/(tabs)/add/index.tsx (read in full — InputStep component, its props, the static recent chips block, AddScreen state)
    - apps/mobile/src/lib/api.ts (HomeSummary interface and RecentCard interface)
    - apps/api/src/routes/home.ts (confirm headword is returned in recentCards response)
  </read_first>
  <action>
    api.ts: No changes needed. HomeSummary and RecentCard interfaces already include headword: string | null.

    add/index.tsx — AddScreen:
    Add a useQuery call inside AddScreen (alongside the existing generateMutation):
    const { data: homeSummary } = useQuery({
      queryKey: ['home', 'summary'],
      queryFn: () => api.getHomeSummary(),
      staleTime: 60_000,
    });

    Derive a recentHeadwords array from the result:
    const recentHeadwords = (homeSummary?.recentCards ?? [])
      .filter((c) => c.headword != null)
      .map((c) => c.headword as string)
      .slice(0, 4);

    Add recentHeadwords: string[] prop to InputStep. Pass recentHeadwords from AddScreen.

    InputStep: Replace the hardcoded ['saudade', 'ficar', 'já', 'quase'] recent chips with:
    - If recentHeadwords.length > 0: render each as a TouchableOpacity wrapping Chip (same pattern as before — onPress calls onInputChange(word)), variant="default"
    - If recentHeadwords.length === 0 (no cards yet): render a Body size={11} tone="muted" text "no recent words yet" in place of the chips

    Keep the 'from frequency list' section below as-is (static ['→ ainda', '→ porque', '→ embora'] chips — ADD-06 only requires recent words; frequency list is UI scaffolding).

    Import useQuery at the top of add/index.tsx if not already imported (it is already imported via useMutation).

  </action>
  <verify>
    <automated>cd /Users/graysonstricker/Programs/Consono && corepack pnpm --filter @portuguese-app/mobile exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - add/index.tsx has useQuery({ queryKey: ['home', 'summary'] }) call in AddScreen (grep)
    - recentHeadwords is derived from homeSummary?.recentCards (grep for 'recentHeadwords')
    - InputStep prop list includes recentHeadwords: string[]
    - The hardcoded ['saudade', 'ficar', 'já', 'quase'] array is gone from InputStep (grep must return 0 matches for 'saudade')
    - Mobile TypeScript compiles clean (exit 0)
  </acceptance_criteria>
  <done>InputStep 'recent' section shows the user's 3–4 most-recently-added word card headwords as tappable chips, or a "no recent words yet" placeholder when the deck is empty.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                 |
| ---------------------------------------- | ----------------------------------------------------------- |
| /home/summary response → InputStep chips | Headwords from user's own card data rendered as chip labels |

## STRIDE Threat Register

| Threat ID | Category               | Component                                       | Disposition | Mitigation Plan                                                                      |
| --------- | ---------------------- | ----------------------------------------------- | ----------- | ------------------------------------------------------------------------------------ |
| T-04-11   | Information Disclosure | recentCards headwords displayed in chip labels  | accept      | Single-user app (V0_USER_ID hardcoded); no cross-user data exposure risk             |
| T-04-12   | Tampering              | headword chip tap injects text into input field | accept      | Same trust level as user typing manually; no elevated privilege or injection surface |
| T-04-SC   | Tampering              | No new npm installs this plan                   | accept      | No packages installed; no slopcheck needed                                           |

</threat_model>

<verification>
After task completes:
- Mobile TypeScript compiles clean
- Manually: open Add wizard with cards in the library — recent chips show real headwords from DB
- Open Add wizard with empty deck — recent chips show "no recent words yet" placeholder
- Tapping a recent chip fills the word input field
</verification>

<success_criteria>

- Recent chips display real headwords from the user's cards (not 'saudade', 'ficar', 'já', 'quase')
- Empty state shows "no recent words yet" text
- Tapping a chip populates the input and advances to word detection state
- Frequency list chips remain (static scaffolding unchanged)
- TypeScript clean
  </success_criteria>

<output>
Create `.planning/phases/04-add-wizard-polish/04-04-SUMMARY.md` when done.
</output>
