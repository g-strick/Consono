---
phase: 04-add-wizard-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - apps/mobile/app/(tabs)/add/index.tsx
autonomous: true
requirements:
  - ADD-03

must_haves:
  truths:
    - 'User can tap the ✎ edit button in ReviewStep and get an inline editable TextInput for the sentence'
    - 'User can type a new sentence value, tap Undo to revert to previous, or tap Save to keep the edit'
    - 'Audio is regenerated at approval time using the final edited sentence (existing behavior — no regression)'
  artifacts:
    - path: 'apps/mobile/app/(tabs)/add/index.tsx'
      provides: 'ReviewStep with sentence inline editing mode and undo history'
      contains: 'editingSentence'
  key_links:
    - from: 'ReviewStep ✎ button'
      to: 'editingSentence state toggle'
      via: 'local component state in ReviewStep'
      pattern: 'editingSentence'
    - from: 'ReviewStep save (Undo / Confirm edit buttons)'
      to: 'selectedSentence in AddScreen'
      via: 'onSentenceEdit(newSentence) callback prop'
      pattern: 'onSentenceEdit'
---

<objective>
Wire the ✎ edit button in ReviewStep to an inline editing mode: the sentence body swaps to a TextInput with the current sentence pre-filled, and Undo/Confirm buttons appear. Undo reverts to the previous sentence value. Confirm edit updates the sentence used at save time.

Purpose: The requirement (ADD-03) calls for sentence inline editing with undo and the assurance that audio will be re-rendered on save — audio re-rendering already happens at approval time (cards.ts synthesize call), so this plan focuses entirely on the UI editing loop.

Output: ReviewStep with a working edit/undo/confirm flow. No new API endpoints.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/graysonstricker/Programs/LingoCards/.planning/ROADMAP.md
@/Users/graysonstricker/Programs/LingoCards/.planning/STATE.md

<interfaces>
<!-- Key contracts from codebase. -->

From apps/mobile/app/(tabs)/add/index.tsx ReviewStep (current):
Props: { draft, selectedImage, selectedSentence, kind, isSaving, error, onSave, tabBarHeight }

- Bottom row has a ✎ TouchableOpacity (width 56, height 48) that currently does nothing
- Save CTA calls onSave(selectedSentence)
- selectedSentence is passed down from AddScreen state

From apps/mobile/src/lib/theme.ts:
colors.paper, colors.paperSoft, colors.brand, colors.brandTint, colors.grayRule
fonts.display (InstrumentSerif — used for sentence body in card)

From apps/mobile/src/components/Type.tsx:
Body, Heading, Action — used throughout ReviewStep already
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inline sentence editing to ReviewStep with undo</name>
  <files>apps/mobile/app/(tabs)/add/index.tsx</files>
  <read_first>
    - apps/mobile/app/(tabs)/add/index.tsx (read in full — focus on ReviewStep and AddScreen state; see selectedSentence, onSave, approveMutation)
    - apps/mobile/src/lib/theme.ts (colors and fonts used in the card body)
    - apps/mobile/src/components/Type.tsx (Body, Action primitives)
  </read_first>
  <action>
    Modify ReviewStep only — no changes to other steps or AddScreen logic except a new callback prop.

    New AddScreen prop passed to ReviewStep:
    - onSentenceEdit: (newSentence: string) => void

    In AddScreen, implement onSentenceEdit: calls setSelectedSentence(newSentence). The approveMutation already uses selectedSentence, so no further change needed.

    Inside ReviewStep — add local state:
    - editingSentence: boolean (false by default)
    - editDraft: string (initialized from selectedSentence when editing begins)
    - previousSentence: string (stores selectedSentence value at the moment editing begins, used for undo)

    Behavior of the ✎ button (currently no-op):
    - On press: sets editingSentence = true, editDraft = selectedSentence, previousSentence = selectedSentence

    When editingSentence is true, replace the static sentence Body text with:
    - A TextInput displaying editDraft, using fontFamily: fonts.display, fontSize 13, lineHeight 20, flex 1
    - autoFocus, autoCorrect false, multiline, style matching the existing sentence area padding
    - onChange updates editDraft

    When editingSentence is true, replace the ✎ button + Save button row with a 3-button row:
    - "Undo" button (secondary style: borderWidth 0.5, borderColor rgba(0,0,0,0.15), backgroundColor colors.paperSoft, width 80): calls onSentenceEdit(previousSentence) then sets editingSentence = false and resets editDraft
    - "Save edit" button (primary: backgroundColor colors.brand, flex 1): calls onSentenceEdit(editDraft) then sets editingSentence = false

    When editingSentence is false (normal mode):
    - Sentence displays as Body (existing behavior)
    - Bottom row shows the ✎ button + Save CTA (existing layout)

    Note on "regenerate audio on save" (ADD-03): Audio is synthesized in cards.ts POST /cards handler using the selected_sentence value passed at approval. Since onSentenceEdit updates selectedSentence in AddScreen, the final onSave(selectedSentence) call will use the edited text. This is sufficient — the requirement is met by the existing approval flow.

  </action>
  <verify>
    <automated>cd /Users/graysonstricker/Programs/LingoCards && corepack pnpm --filter @portuguese-app/mobile exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - ReviewStep prop list includes onSentenceEdit: (newSentence: string) => void
    - editingSentence local state variable exists inside ReviewStep (grep for 'editingSentence')
    - previousSentence local state variable exists inside ReviewStep (grep for 'previousSentence')
    - The ✎ TouchableOpacity onPress sets editingSentence = true (not empty as before)
    - A TextInput appears conditionally when editingSentence is true (grep for 'editDraft')
    - An "Undo" button appears when editingSentence is true and calls onSentenceEdit(previousSentence)
    - A "Save edit" button appears when editingSentence is true and calls onSentenceEdit(editDraft)
    - Mobile TypeScript compiles clean (exit 0)
  </acceptance_criteria>
  <done>User can tap ✎, edit the sentence inline, undo to the original, or confirm the edit — and the confirmed sentence is used at approval time.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                                      |
| ------------------------------------------ | -------------------------------------------------------------------------------- |
| User text → selectedSentence → POST /cards | User edits sentence text which becomes selected_sentence in the approval payload |

## STRIDE Threat Register

| Threat ID | Category          | Component                                | Disposition | Mitigation Plan                                                                                                                                      |
| --------- | ----------------- | ---------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-05   | Tampering         | Edited sentence text in approval payload | accept      | Server-side: selected_sentence is passed to synthesize() and stored as sentence_pt — same trust level as original sentence; no SQL injection surface |
| T-04-06   | Denial of Service | Audio re-synthesis on edited sentence    | accept      | One synthesis call per approval; user can only save once; no retry amplification                                                                     |
| T-04-SC   | Tampering         | No new npm installs this plan            | accept      | No packages installed; no slopcheck needed                                                                                                           |

</threat_model>

<verification>
After task completes:
- Mobile TypeScript compiles clean
- Manually: open Add wizard, enter a word, go through to ReviewStep, tap ✎ — sentence becomes editable TextInput; tap Undo returns to original; tap Save edit uses the new text; final Save card submits the edited sentence
</verification>

<success_criteria>

- ReviewStep ✎ button opens inline editor
- Undo reverts to the sentence value before editing began (not just a text clear)
- Save edit propagates the new text to AddScreen's selectedSentence
- Audio is generated from the final sentence at approval (existing behavior confirmed)
- TypeScript clean
  </success_criteria>

<output>
Create `.planning/phases/04-add-wizard-polish/04-02-SUMMARY.md` when done.
</output>
