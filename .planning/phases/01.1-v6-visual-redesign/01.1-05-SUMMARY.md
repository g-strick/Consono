---
phase: 01.1-v6-visual-redesign
plan: '05'
subsystem: mobile/home
tags: [home, tab-bar, v6, surface-driven, streak-chip, three-states]
dependency_graph:
  requires:
    - '01.1-01 (v6 tokens in theme.ts, Type.tsx primitives, Surface type)'
    - '01.1-02 (StreakChip, StatTile, Chip, Surface/Card components)'
  provides:
    - v6 Home screen (light surface, 3 states, surface-driven greeting, single "+ Add")
    - v6 tab bar (brand active tint, faint inactive, paper bg, Geist label font)
  affects:
    - apps/mobile/app/(tabs)/index.tsx
    - apps/mobile/app/(tabs)/_layout.tsx
tech_stack:
  added: []
  patterns:
    - surface-driven text (black greeting/labels, cobalt reserved for due number + key actions)
    - three home states (first-run / daily pickup / all-done)
    - single "+ Add" entry (collapsed from two add affordances)
    - StreakChip in the home header corner
key_files:
  created: []
  modified:
    - apps/mobile/app/(tabs)/index.tsx
    - apps/mobile/app/(tabs)/_layout.tsx
requirements: [V6-05, DSGN-02]
---

# 01.1-05 — Home Reskin (v6)

## What was built

- **Tasks 1 + 2 — `app/(tabs)/index.tsx` (commit `574f1f3`):** converted Home to the v6 light
  surface with the v6 header/greeting/due tile and the three home states (first-run, daily
  pickup, all-done). Text follows the surface rule (DSGN-02): black greeting, subtitle, and
  labels; cobalt reserved for the big due number and key action labels. Collapsed the two add
  affordances into a single "+ Add", and placed the `StreakChip` in the header corner. Existing
  data fetching, the real user-name (from Phase 1 `/users/me`), and due-count behavior preserved.
- **Task 3 — `app/(tabs)/_layout.tsx` (commit `367b407`):** aligned the tab bar to v6 — brand
  active tint, faint inactive, paper background, Geist label font.

## Verification

- `npx tsc --noEmit -p tsconfig.json` → exit 0.
- Home renders via the Wave 1/2 v6 primitives (`Display`, `Num`, `Action`, `StreakChip`,
  surface props) — no hardcoded body hex.

## Deviations

- **Closeout completed by the orchestrator.** The gsd-executor subagent committed both feature
  commits (`574f1f3`, `367b407`) but the session limit interrupted it before writing this SUMMARY
  and marking the roadmap. The orchestrator verified the committed work (tsc=0) and finished the
  closeout — no code changes were needed.
- **Streak chip count is a placeholder** (consistent with 01.1-04) — real streak aggregation is
  Phase 2 (STRK-01/02); only the chip state/styling matters this phase.

## Self-Check: PASSED
