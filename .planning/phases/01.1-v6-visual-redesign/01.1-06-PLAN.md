---
phase: 01.1-v6-visual-redesign
plan: 06
type: execute
wave: 3
depends_on: ['01.1-01', '01.1-02']
files_modified:
  - apps/mobile/app/streak/index.tsx
  - apps/mobile/src/components/Heatmap.tsx
  - apps/mobile/src/components/RatingDistribution.tsx
autonomous: true
requirements: [V6-06]
must_haves:
  truths:
    - 'Streak detail shows a period toggle (month / year / lifetime, default year) that re-scopes the screen'
    - 'Compact hero shows flame + cobalt streak number + mono caps line; a 2×2 stat grid follows'
    - 'The 53w×7d year heatmap renders with the 4-stop ramp and today outlined (not filled)'
    - 'A rating distribution stacked bar shows again/hard/good/easy with semantic colors and percentages'
  artifacts:
    - path: 'apps/mobile/app/streak/index.tsx'
      provides: 'v6 streak detail: nav, compact hero, period toggle, stat grid, heatmap, rating distribution, personal bests'
      contains: 'lifetime'
    - path: 'apps/mobile/src/components/Heatmap.tsx'
      provides: 'year (53×7) + month grid heatmaps with 4-stop ramp and today outlined'
      exports: ['YearHeatmap']
    - path: 'apps/mobile/src/components/RatingDistribution.tsx'
      provides: 'stacked horizontal rating distribution bar + legend'
      exports: ['RatingDistribution']
  key_links:
    - from: 'apps/mobile/app/streak/index.tsx'
      to: 'apps/mobile/src/components/Heatmap.tsx'
      via: 'selected period renders the matching heatmap variant'
      pattern: 'YearHeatmap|Heatmap'
    - from: 'apps/mobile/src/components/Heatmap.tsx'
      to: 'apps/mobile/src/lib/theme.ts'
      via: '4-stop heat ramp tokens heat0..heat3 + brand outline for today'
      pattern: 'heat0|heat-3|heatmap'
---

<objective>
Reskin the Streak detail screen to v6 §4: a nav row, the compact hero (flame + cobalt number + mono caps line), a period toggle (month / year / lifetime, default year), a 2×2 stat grid, the period-scoped heatmap (53w×7d GitHub-style for year with the 4-stop ramp and today outlined), a reviews bar chart, a rating distribution stacked bar, and the personal-bests list. Real streak aggregation queries are Phase 3 — this plan delivers the v6 layout/visuals driven by the data available now (with clearly-marked static placeholders where a real query does not yet exist).

Purpose: Bring the streak detail screen to full v6 visual fidelity.
Output: Reskinned `streak/index.tsx` + `Heatmap.tsx` + `RatingDistribution.tsx`.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/Consono/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/Consono/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

<interfaces>
<!-- From Plans 01 & 02 -->
From apps/mobile/src/components/Type.tsx: Display, Heading, Body, Mono, Num, Action
From apps/mobile/src/components/StatTile.tsx: StatTile({ value, label, sub })
From apps/mobile/src/components/Chip.tsx: Chip; Surface.tsx: Card
From apps/mobile/src/lib/theme.ts: colors (heat0..heat3, brand, accent, again/hard/good/easy, brandTint), fonts, useTheme

<!-- Existing streak screen — currently V0-stubbed static data; this plan replaces it -->

From apps/mobile/app/streak/index.tsx: StreakScreen (static hero "1", StatTile placeholders). Registered in \_layout.tsx as streak/index with title 'Your streak'.
</interfaces>

<period_scoping>
From wireframes/Wireframes v6.html lines 1995-2002 + 2174: the period toggle (default year) re-scopes every section. Year → 53×7 grid; Month → calendar grid; Lifetime → monthly bars. Stats grid, reviews chart, and rating distribution all show period-scoped values. This phase wires the toggle + per-period layouts with placeholder values where no real query exists; Phase 3 supplies real aggregation.
</period_scoping>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Year/month heatmap component (4-stop ramp, today outlined)</name>
  <read_first>
    - wireframes/Wireframes v6.html lines 466-497 (.heatmap-year: 53-col × 7-row grid, grid-auto-flow column, gap 2, cells radius 1.5, heat0..heat3 via l1/l2/l3, today = transparent + inset 1px brand outline; months row labels)
    - wireframes/Wireframes v6.html lines 440-464 (.heatmap-month: 7-col day grid, dow header M T W T F S S, today = transparent + 1.5px brand border, empty = transparent)
    - wireframes/Wireframes v6.html lines 524-532 (.ramp legend: less → heat0 heat1 heat2 heat3 → more)
    - apps/mobile/src/lib/theme.ts (heat0..heat3 tokens, brand)
  </read_first>
  <action>
    Create `src/components/Heatmap.tsx` exporting `YearHeatmap({ levels, todayIndex, monthLabels })` and `MonthHeatmap({ days, todayIndex, monthLabel })` where `levels`/`days` are arrays of intensity 0–3. YearHeatmap: render a 53×7 grid that fills column-by-column (week columns, day rows) using React Native `View`s in a flex layout — 53 week columns each with 7 day cells, cell intensity mapped to `colors.heat0..heat3`, cells radius 1.5, gap 2, inside a paper `Card`. The `todayIndex` cell renders transparent with a 1px brand inset outline (border, not fill). Include the "less ▢▢▢▢ more" ramp legend in the section header using the four heat tokens. MonthHeatmap: a 7-column calendar grid with a M T W T F S S `Mono` header, day cells mapped to heat tokens, empty leading days transparent, today outlined 1.5px brand. Pull all colors from theme (no inline heat hex). For year, accept up to 371 (53×7) level entries and a `monthLabels` string array for the bottom axis.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "export function YearHeatmap" src/components/Heatmap.tsx && grep -q "export function MonthHeatmap" src/components/Heatmap.tsx && grep -qE "heat0|heat" src/components/Heatmap.tsx && grep -q "todayIndex" src/components/Heatmap.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>YearHeatmap renders a 53×7 grid with the 4-stop ramp and today outlined; MonthHeatmap renders a calendar grid with today outlined; both use theme heat tokens; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rating distribution stacked bar</name>
  <read_first>
    - wireframes/Wireframes v6.html lines 556-587 (.rate-dist: 14px stacked bar radius 4, segment widths by %, legend 4-col with swatch + uppercase mono name + bold pct)
    - wireframes/Wireframes v6.html lines 2110-2142 (usage: again 8% / hard 18% / good 56% / easy 18% with semantic swatches)
    - apps/mobile/src/lib/theme.ts (again/hard/good/easy semantic tokens), Type.tsx (Mono)
  </read_first>
  <action>
    Create `src/components/RatingDistribution.tsx` exporting `RatingDistribution({ counts | percents, total })` where input gives the four rating proportions. Render a 14px-tall stacked horizontal bar (radius 4, overflow hidden) with four flex segments sized by percentage and colored `colors.again` / `colors.hard` / `colors.good` / `colors.easy`. Below it render a 4-column legend: each item has a swatch in the matching semantic color, an uppercase `Mono` name (again/hard/good/easy), and a bold percentage value. Wrap in a paper `Card` with a `det-section` header (title + "{total} reviews" meta). Read all colors from theme. Accept zero-state gracefully (empty bar, 0% legend).
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "export function RatingDistribution" src/components/RatingDistribution.tsx && grep -qE "again|good|easy" src/components/RatingDistribution.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>RatingDistribution renders a semantic stacked bar + 4-item legend with percentages from theme tokens; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Reskin streak/index to v6 (nav, hero, period toggle, grid, charts, bests)</name>
  <read_first>
    - apps/mobile/app/streak/index.tsx (current static stub — replace)
    - wireframes/Wireframes v6.html lines 2004-2175 (year detail: detail-nav ← / "Your streak" / ↗ share; compact-hero flame + cobalt num + mono caps line DAY STREAK · LONGEST · RETENTION; period-toggle month/year/lifetime with year active; 2×2 stat-grid longest/retention/reviews/days-active; year heatmap; reviews bar chart; rating distribution; personal bests list with current highlighted)
    - wireframes/Wireframes v6.html lines 2177-2303, 2305-+ (month + lifetime variants: month calendar + daily bars; lifetime monthly bars)
    - wireframes/Wireframes v6.html lines 335-422, 589-617 (.detail-nav, .compact-hero, .period-toggle, .stat-grid, .best-list/.best-row styles)
    - apps/mobile/src/components/Heatmap.tsx, RatingDistribution.tsx, StatTile.tsx, Chip.tsx, Type.tsx
  </read_first>
  <action>
    Replace the stubbed `StreakScreen` with the v6 layout inside a scrollable light surface. Build: a `detail-nav` row (← back via `router.back()`, centered "Your streak" `Body` semibold, ↗ share placeholder); a `compact-hero` (Ionicons `flame` in accent gold + `Num` cobalt streak number + a `Mono` caps line "DAY STREAK · LONGEST {n} · RETENTION {n}%"); a `period-toggle` segmented control (month / year / lifetime) backed by local `period` state defaulting to `'year'`, active segment = paper pill with shadow, inactive = muted; a 2×2 `StatTile` grid (longest / retention / reviews / days active, each with a sub line). For the activity section, render `YearHeatmap` when period==='year', `MonthHeatmap` when 'month', and a monthly-bars layout when 'lifetime'. Add a reviews bar chart (simple flex bars in brand-fill, today/current highlighted) and the `RatingDistribution` component. Add a personal-bests `Card` list (.best-row: rank, serif day count, dates; current row = brand-tint background). Drive values from any available data; where no real query exists yet, use clearly-named placeholder constants with a `// Phase 3: real aggregation` comment — do NOT claim the numbers are live. Re-scope the visible stat/heatmap/chart values when `period` changes (placeholder maps per period are acceptable this phase).
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "YearHeatmap" app/streak/index.tsx && grep -q "RatingDistribution" app/streak/index.tsx && grep -qE "month|year|lifetime" app/streak/index.tsx && grep -q "period" app/streak/index.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Streak detail renders nav, compact hero, working period toggle (default year), 2×2 stat grid, period-scoped heatmap, reviews bars, rating distribution, and personal bests — all in v6 tokens; tsc passes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                                               |
| ---------- | ------------------------------------------------------------------------------------------------------------------------- |
| (none new) | Visual reskin of a read-only detail screen; no new network calls in this phase (real aggregation queries land in Phase 3) |

## STRIDE Threat Register

| Threat ID  | Category | Component              | Disposition | Mitigation Plan                                                                                               |
| ---------- | -------- | ---------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- |
| (none new) | —        | streak detail + charts | accept      | Presentational charts over local/placeholder data; no input parsing, network, storage, or auth surface added. |

</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- Period toggle defaults to year and switches the heatmap/stat layout.
- Year heatmap is 53×7 with the 4-stop ramp and today outlined (not filled).
- Rating distribution shows again/hard/good/easy with semantic colors + percentages.
</verification>

<success_criteria>

- Streak detail matches the v6 layout and palette (SC #3) with period toggle, heatmap, and rating distribution.
  </success_criteria>

<output>
Create `.planning/phases/01.1-v6-visual-redesign/01.1-06-SUMMARY.md` when done.
</output>
