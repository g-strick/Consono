---
phase: 01.1-v6-visual-redesign
plan: 03
type: execute
wave: 3
depends_on: ['01.1-01', '01.1-02']
files_modified:
  - apps/mobile/app/(tabs)/add/index.tsx
  - apps/mobile/src/lib/detectKind.ts
autonomous: true
requirements: [V6-03]
must_haves:
  truths:
    - "A single '+ Add' entry opens one wizard with no mode toggle"
    - 'The field live-detects word vs sentence and shows a detector chip the user can override'
    - 'Both paths share the image/review/save shell; word path adds the i+1 sentence step; step counter reflects the detected path (word=4, sentence=3)'
  artifacts:
    - path: 'apps/mobile/src/lib/detectKind.ts'
      provides: "pure detectKind(text) → 'word' | 'sentence' implementing the v6 detector contract"
      exports: ['detectKind']
    - path: 'apps/mobile/app/(tabs)/add/index.tsx'
      provides: 'merged type-detecting wizard reskinned to v6'
      contains: 'detectKind'
  key_links:
    - from: 'apps/mobile/app/(tabs)/add/index.tsx'
      to: 'apps/mobile/src/lib/detectKind.ts'
      via: 'live kind detection on input change, overridable'
      pattern: 'detectKind'
    - from: 'apps/mobile/app/(tabs)/add/index.tsx'
      to: 'apps/mobile/src/lib/api.ts'
      via: 'existing api.generate(text, kind) + api.approveCard — unchanged backend contract'
      pattern: 'api.generate|api.approveCard'
---

<objective>
Merge the two Add flows into one type-detecting wizard per v6 §A: a single "+ Add" entry, one input field that live-detects word vs sentence (overridable via the detector chip), a shared image/review/save shell, and a word-only i+1 sentence step. Reskin every frame to the v6 token system. This is a client-only structural + visual change — the existing `api.generate(text, kind)` and `api.approveCard` contracts and the Phase 1 audio/approval behavior are untouched.

Purpose: Collapse two near-duplicate wizards into one branch; deliver the v6 detector UX.
Output: `detectKind.ts` (pure detector) + reskinned `add/index.tsx`.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

<interfaces>
<!-- From Plans 01 & 02 -->
From apps/mobile/src/components/Type.tsx: Display, Heading, Body, Mono, Num, Action
From apps/mobile/src/components/Chip.tsx: Chip({ label, variant, color })
From apps/mobile/src/components/Surface.tsx: Card({ children, gender, padded })
From apps/mobile/src/lib/theme.ts: colors, fonts, useTheme

<!-- Existing add wizard contracts to preserve -->

From apps/mobile/src/lib/api.ts:
export type CardKind = 'word' | 'sentence';
api.generate(text, kind) → GenerateDraft { pending_card_id, draft: { images, fields: { lemma, gender, gendered_form, stress_marker, usage_context, register_tag, sentence_candidates, sounds_like } } }
api.approveCard({ pending_card_id, selected_image_url, selected_image_attribution, selected_sentence }) → { card_id }
Current add/index.tsx state machine: Step = 'input'|'loading'|'pick-image'|'pick-sentence'|'review'|'saved'; Mode = 'word'|'sentence'; generateMutation/approveMutation already wired; sentence mode skips pick-sentence and pre-populates selectedSentence.
</interfaces>

<detector_contract>
From wireframes/Wireframes v6.html lines 1440-1441 and 1431:

- ≤1 token AND no terminal punctuation → word
- anything else (spaces / punctuation / any paste) → sentence
- user can override via the detector chip; mid-entry the kind switches silently as they type/delete (no confirm dialog)
  </detector_contract>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure detectKind() implementing the v6 detector contract</name>
  <read_first>
    - wireframes/Wireframes v6.html lines 1431, 1440-1441 (detector contract: ≤1 token + no terminal punctuation → word; else sentence; overridable)
    - apps/mobile/src/lib/api.ts (CardKind type to reuse)
  </read_first>
  <behavior>
    - detectKind('feijoada') → 'word'
    - detectKind('  saudade ') → 'word' (trim, single token)
    - detectKind('') → 'word' (empty defaults to word path)
    - detectKind('Que saudade de você!') → 'sentence' (multiple tokens)
    - detectKind('Olá.') → 'sentence' (single token but terminal punctuation . ! ?)
    - detectKind('bem-vindo') → 'word' (hyphen is not a token separator nor terminal punctuation)
  </behavior>
  <action>
    Create `src/lib/detectKind.ts` exporting `detectKind(text: string): CardKind`. Trim, then: if the trimmed text is empty → return `'word'`. Split on whitespace; if more than one non-empty token → `'sentence'`. If exactly one token but it ends with terminal punctuation (`.`, `!`, `?`, `…`) → `'sentence'`. Otherwise → `'word'`. Import `CardKind` from `@/src/lib/api`. Keep it a pure function with no React. Add a co-located test file `src/lib/detectKind.test.ts` covering the behavior cases above (the repo has no test runner configured yet — if `npm test` is unavailable, the automated verify falls back to tsc + a node smoke import; create the test file regardless for future use).
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "export function detectKind" src/lib/detectKind.ts && npx tsc --noEmit -p tsconfig.json && node -e "const {register}=require('ts-node')||{};" 2>/dev/null; node --input-type=module -e "import('./src/lib/detectKind.ts').then(()=>process.exit(0)).catch(()=>process.exit(0))" 2>/dev/null; grep -q "sentence" src/lib/detectKind.ts</automated>
  </verify>
  <done>detectKind.ts is a pure function matching the contract; test file exists; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire live detection + reskin input/branch shell to v6</name>
  <read_first>
    - apps/mobile/app/(tabs)/add/index.tsx (full current wizard — remove the explicit mode toggle, drive kind from detectKind, keep mutations)
    - apps/mobile/src/lib/detectKind.ts (from Task 1)
    - wireframes/Wireframes v6.html lines 1096-1190 (A.1 input empty: one field, recent chips, frequency chips, "Propose card →"; A.2 word detected: brand dot + "word · noun · we'll handle gender", "building a word card · 4 steps"; A.3 sentence detected: serif sentence card, accentDeep dot + "sentence · N words · no gender step", source chips)
    - wireframes/Wireframes v6.html lines 853-885 (.wiz-top cancel=brand, step=mono muted; .progress bar 3px, brand fill / gold variant; kind-badge shared/word/sentence colors)
    - apps/mobile/src/components/Type.tsx, Chip.tsx, theme.ts (Plan 01/02 primitives)
  </read_first>
  <action>
    Replace `Mode` toggle logic with derived kind: keep a state `kindOverride: CardKind | null`; compute `kind = kindOverride ?? detectKind(inputText)` and recompute on every keystroke. Remove the `onModeToggle` button and the `InputStep` mode-toggle Text; instead render the v6 detector chip under the field — for word kind a brand dot + `word · we'll handle gender`, for sentence kind an accentDeep dot + `sentence · {N} words · no gender step` (N = whitespace token count). Make the chip tappable to flip `kindOverride` to the other kind (override per contract; clear override when the field is cleared). When kind === 'sentence', render the input value inside a serif `Card` (Instrument Serif ~17px) and show the `source (optional)` chip row (whatsapp/instagram/netflix/+ tag) as display-only chips — source is not persisted this phase (defer real tagging to Phase 4); when kind === 'word' keep the large `Display`/`h-disp` field. Reskin `wiz-top` (Cancel in brand `Action`, step label in `Mono`), the 3px progress bar (brand fill; gold variant during loading), recent + frequency chips using `Chip` (frequency = brand variant with a `→` prefix). On Propose, call `generateMutation.mutate({ text, kind })` exactly as today. Keep `stepLabel()` returning `2/4 3/4 4/4` for word and `2/3 3/3` for sentence based on the derived kind.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "detectKind" "app/(tabs)/add/index.tsx" && grep -q "kindOverride" "app/(tabs)/add/index.tsx" && ! grep -q "onModeToggle" "app/(tabs)/add/index.tsx" && grep -q "api.generate" "app/(tabs)/add/index.tsx" && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>One field with no mode toggle; kind is detected live and overridable via a chip; sentence path shows source chips; input/branch shell uses v6 tokens; api.generate still called with the derived kind.</done>
</task>

<task type="auto">
  <name>Task 3: Reskin loading / pick-image / pick-sentence / review / saved to v6</name>
  <read_first>
    - apps/mobile/app/(tabs)/add/index.tsx (LoadingStep, PickImageStep, PickSentenceStep, ReviewStep, SavedStep)
    - wireframes/Wireframes v6.html lines 1194-1221, 1371-1395 (A.4 AI thinks: pipeline rows ✓ lemma/gender/stress, ○ images N/4, queued i+1; sentence variant: ✓ parsed / ✓ image query / ○ images / ○ gloss; gold progress)
    - wireframes/Wireframes v6.html lines 1224-1286 (A.5 pick image: 2×2 grid, selected = 2px brand border + ✓ badge, more/upload/refine chips; A.6 pick sentence: i+1 chip, .opt rows radio, selected = brand border + brand-tint bg)
    - wireframes/Wireframes v6.html lines 1289-1359, 1397-1425 (A.7 review&save: gender Card, serif headword, stress mono, image, sentence + audio btn, chips, ✎ + Save row; sentence variant drops headword/gender/stress; A.8 saved: brand-tint check circle, "In your queue.", card chip, +Another / ▶Review)
    - apps/mobile/src/components/* + theme.ts (Card, Chip, RatingButtons not needed here; StatTile no; use Display/Body/Mono/Num/Action + Card + Chip)
  </read_first>
  <action>
    Reskin each step to v6 tokens while preserving the existing data wiring (draft fields, selectedImage, selectedSentence, approveMutation). LoadingStep: replace the generic spinner list with the v6 pipeline-row layout — `ucard`-style rows showing ✓ lemma / ✓ gender (gender chip) / ✓ stress (mono) / ○ images / ○ i+1 for word kind, and ✓ parsed / ✓ image query / ○ images / ○ gloss for sentence kind; gold progress bar. (Real live pipeline status is Phase 4 ADD-01 — keep these as static/animated placeholders, do NOT claim live status, but match the visual layout.) PickImageStep: 2×2 grid, selected image = 2px brand border + brand ✓ badge, display-only `↻ more / ↑ upload / ⌕ refine` chips, gender chip in the header for word kind. PickSentenceStep: `i+1` brand chip header, `.opt` rows with a radio dot, selected row = brand border + brand-tint bg, display-only play-all/more/write-own chips. ReviewStep: wrap in gender `Card`; word kind shows serif headword + gender chip + stress mono + image + sentence + audio button + register chips; sentence kind drops headword/gender/stress and leads with the image then the serif sentence; bottom `✎` + `Save` row using `Action`. SavedStep: brand-tint circle with cobalt ✓, `Display` "In your queue.", `card #{id} · due now`, a gender `Card` chip, and `+ Another` / `▶ Review` buttons. Keep all `onSelect`/`onNext`/`onSave` handlers and the `mode === 'sentence'` branch conditions (now driven by the derived `kind`).
  </action>
  <verify>
    <automated>cd apps/mobile && grep -qE "In your queue" "app/(tabs)/add/index.tsx" && grep -q "Card" "app/(tabs)/add/index.tsx" && grep -q "approveMutation" "app/(tabs)/add/index.tsx" && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>All wizard steps render in v6 tokens (pipeline rows, image grid, opt rows, gender Card review, brand-tint saved); data wiring and approve/generate calls unchanged; tsc passes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                                                     |
| ---------------------- | --------------------------------------------------------------------------------------------------------------- |
| user text → detectKind | Untrusted free-text from the Add field is classified client-side; no eval, no network from the detector itself  |
| wizard → existing API  | Already-trusted Phase 1 endpoints (api.generate / api.approveCard); this plan changes no request/response shape |

## STRIDE Threat Register

| Threat ID    | Category               | Component        | Disposition | Mitigation Plan                                                                                                                                                            |
| ------------ | ---------------------- | ---------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-01.1-03-01 | Tampering (input)      | detectKind(text) | mitigate    | Pure string classification only — split/trim/regex on terminal punctuation; no eval, no dynamic require, no injection sink. Output is constrained to the `CardKind` union. |
| T-01.1-03-02 | Information disclosure | source chips     | accept      | Source tags are display-only this phase and not persisted; nothing new is sent to the server.                                                                              |

</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- Single "+ Add" entry, no mode toggle; typing a word vs a sentence flips the detector chip live; tapping the chip overrides.
- Word path shows 4 steps incl. pick-sentence; sentence path shows 3 steps and skips it.
- api.generate / api.approveCard request shapes unchanged from Phase 1.
</verification>

<success_criteria>

- A single "+ Add" entry detects word vs sentence and routes through one shared wizard (SC #2).
- Every wizard frame matches v6 layout/palette.
  </success_criteria>

<output>
Create `.planning/phases/01.1-v6-visual-redesign/01.1-03-SUMMARY.md` when done.
</output>
