---
phase: 01.1-v6-visual-redesign
plan: 02
type: execute
wave: 2
depends_on: ['01.1-01']
files_modified:
  - apps/mobile/src/components/StreakChip.tsx
  - apps/mobile/src/components/StatTile.tsx
  - apps/mobile/src/components/RatingButtons.tsx
  - apps/mobile/src/components/Surface.tsx
  - apps/mobile/src/components/Chip.tsx
autonomous: true
requirements: [V6-02]
must_haves:
  truths:
    - 'Streak chip renders 3 states at 52×26 and animates at-risk→continued over 280ms with no scale/bounce'
    - 'Reusable stat tiles, rating buttons, chips, and card surfaces match v6 specs and are consumed by later screens'
  artifacts:
    - path: 'apps/mobile/src/components/StreakChip.tsx'
      provides: '3-state streak chip with 280ms fill transition using v6 tokens'
      contains: '280'
    - path: 'apps/mobile/src/components/StatTile.tsx'
      provides: 'brand-tint stat tile (serif cobalt number, mono label, sub line)'
      exports: ['StatTile']
    - path: 'apps/mobile/src/components/RatingButtons.tsx'
      provides: '4 FSRS rating buttons with semantic colors + interval labels'
      exports: ['RatingButtons']
    - path: 'apps/mobile/src/components/Chip.tsx'
      provides: 'base chip + brand-tint chip variants'
      exports: ['Chip']
    - path: 'apps/mobile/src/components/Surface.tsx'
      provides: 'card surface (ucard/due-tile) + gender bar wrapper'
      exports: ['Card']
  key_links:
    - from: 'apps/mobile/src/components/StreakChip.tsx'
      to: 'apps/mobile/src/lib/theme.ts'
      via: 'imports v6 colors (brandFill, grayRule)'
      pattern: "brandFill|colors\\."
    - from: 'apps/mobile/src/components/RatingButtons.tsx'
      to: 'apps/mobile/src/lib/theme.ts'
      via: 'semantic again/hard/good/easy colors from theme'
      pattern: 'again|good|easy'
---

<objective>
Build the shared v6 component kit that every screen reskin (Plans 03–06) consumes: the 52×26 streak corner chip with its 3 states and 280ms fill transition, brand-tint stat tiles, semantic FSRS rating buttons, base chips, and the card/gender-bar surfaces.

Purpose: One implementation of each repeated v6 component so Review, Home, and Streak detail stay pixel-consistent.
Output: `StreakChip.tsx` (reskinned), `StatTile.tsx`, `RatingButtons.tsx`, `Chip.tsx`, `Surface.tsx`.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

<interfaces>
<!-- From Plan 01 (must exist before this plan runs) -->
From apps/mobile/src/lib/theme.ts:
  colors with brand/brandFill/brandTint/brandSoft/accent/accentDeep/again/heat*/paperSoft/grayRule/gender* keys
  fonts = { display, displayItalic, ui, uiMedium, uiSemibold, uiBold, mono, monoMedium }
  textForSurface(surface, kind), useTheme()
From apps/mobile/src/components/Type.tsx:
  Display, Heading, Body, Mono, Num, Action

<!-- Current StreakChip contract — keep these exports stable, only the visuals/sizing change -->

From apps/mobile/src/components/StreakChip.tsx:
export type StreakState = 'inactive' | 'at-risk' | 'continued';
export function StreakChip({ count: number, state: StreakState }) // taps → router.push('/streak')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin StreakChip to v6 (52×26, 3 states, 280ms fill)</name>
  <read_first>
    - apps/mobile/src/components/StreakChip.tsx (current animated chip — reuse the Animated.timing pattern)
    - wireframes/Wireframes v6.html lines 189-217 (.streak-chip: height 26, border-radius 13, padding 0 9px, font 12px/500, transition 280ms ease-out on background/border/color)
    - wireframes/Wireframes v6.html lines 202-217 (continued: brandFill bg, #FFF text, no border, padding 0 10px · at-risk: transparent bg, brand text, 1.5px brand border · inactive: transparent, 60% black text, 1px grayRule border)
    - wireframes/Wireframes v6.html lines 1775-1791 (transition: interior fills cobalt, border fades out, text crossfades cobalt→white, NO bounce/scale/overlay, number ticks with no animation)
    - apps/mobile/src/lib/theme.ts (v6 colors from Plan 01)
  </read_first>
  <action>
    Update `StreakChip.tsx` to exact v6 spec. Fix the width: the chip auto-sizes but the section header calls it 52×26 — keep `height: 26`, `borderRadius: 13`, `paddingHorizontal` 9 (10 in continued state), and a flame icon (Ionicons `flame`, size 13) + count Text at fontSize 12, fontFamily from `fonts.uiMedium`. Replace the hardcoded `#1F3494` border/fill with theme tokens: continued fill = `colors.brandFill` (`#2E5BC8`), at-risk border = `colors.brand` 1.5px, inactive border = `colors.grayRule` (`#D5D5D5`) 1px and text `rgba(0,0,0,0.60)`. Animate background, borderColor, and text/icon color together over exactly 280ms with `Animated.timing` (`useNativeDriver: false`, easing ease-out) when `state` becomes `continued`; reverse on leaving. Interpolate text + icon color cobalt→white across the same 280ms. No scale/translate/opacity bounce. The count Text updates instantly (no number animation). Keep `StreakState` and the `{ count, state }` prop contract and the `router.push('/streak')` tap.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "duration: 280" src/components/StreakChip.tsx && grep -q "brandFill" src/components/StreakChip.tsx && grep -qE "height: 26" src/components/StreakChip.tsx && ! grep -q "scale" src/components/StreakChip.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>StreakChip is 26px tall, 13 radius, uses brandFill/brand/grayRule from theme, animates fill+border+text color over 280ms with no scale/bounce, keeps its existing props and tap target.</done>
</task>

<task type="auto">
  <name>Task 2: Build StatTile, Chip, and Card/gender-bar surfaces</name>
  <read_first>
    - wireframes/Wireframes v6.html lines 399-422 (.stat: brand-tint bg, radius 12, padding 12 · .num Instrument Serif 28px cobalt · .label Geist Mono 9px uppercase 0.10em muted · .sub 10px faint)
    - wireframes/Wireframes v6.html lines 268-277 (.chip: paper-soft bg, radius 999, 11px/500, 0.5px divider border · .chip.brand: rgba(31,52,148,0.08) bg, brand text)
    - wireframes/Wireframes v6.html lines 246-266, 902-916 (.due-tile + .ucard card surfaces, radius 14/10, 0.5px divider · .gbar gender bar: 3px left rail, fem #B43A6C / masc brand / common accentDeep)
    - apps/mobile/src/components/Type.tsx + theme.ts (use Num/Mono/Body + tokens)
  </read_first>
  <action>
    Create three component files. `StatTile.tsx` → `StatTile({ value, label, sub? })`: brand-tint background (`colors.brandTint`), radius 12, padding 12; value via `Num` (Instrument Serif, cobalt, ~28px); label via `Mono` (9px uppercase 0.10em); optional `sub` via faint Body 10px. `Chip.tsx` → `Chip({ label, variant?: 'default' | 'brand', color? })`: pill (radius 999) with paperSoft bg + 0.5px divider border (default) or `rgba(31,52,148,0.08)` bg + brand text (brand variant); allow a `color` override for the gender chips (fem `#B43A6C` over `rgba(180,58,108,0.06)`). `Surface.tsx` → export `Card({ children, gender?, padded? })`: paper bg, 0.5px divider border, radius (default 14, allow override), and when `gender` is set render a 3px left rail in the gender color (fem `colors.genderFem`, masc `colors.brand`, common `colors.accentDeep`) per the `.gbar` rule. Pull all colors/fonts from theme + Type primitives; no inline hex except the gender-fem rgba tints which may reference `colors.genderFem`.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "export function StatTile" src/components/StatTile.tsx && grep -q "export function Chip" src/components/Chip.tsx && grep -q "export function Card" src/components/Surface.tsx && grep -q "brandTint" src/components/StatTile.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>StatTile (brand-tint, serif cobalt num), Chip (default + brand + color override), and Card (gender-bar rail) exist and use theme tokens + Type primitives; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Build RatingButtons (semantic FSRS row)</name>
  <read_first>
    - wireframes/Wireframes v6.html lines 969-989 (.rate-row 4-col grid gap 6 · .rate radius 10, 0.5px divider, paper bg · again border/text = again red · hard border + accentDeep text · good = good cobalt border + brand-tint bg + good text · easy = easy soft-blue border + brandSoft text · .rint Geist Mono 9.5px interval)
    - wireframes/Wireframes v6.html lines 1547-1553 (usage: Again <1m / Hard 6m / Good 10m / Easy 4d, label + interval per button)
    - apps/mobile/src/lib/api.ts (Rating type: 'again' | 'hard' | 'good' | 'easy')
    - apps/mobile/src/lib/theme.ts (again/hard/good/easy semantic tokens)
  </read_first>
  <action>
    Create `RatingButtons.tsx` → `RatingButtons({ intervals?, onRate })` where `onRate: (r: Rating) => void` and `intervals?: Record<Rating,string>` (defaults like `<1m / 6m / 10m / 4d` for visual fidelity when no real intervals provided). Render a 4-column grid (gap 6) of `Pressable` tiles, radius 10, 0.5px divider border, paper background. Apply semantic styling per the wireframe: again → border `colors.again` + label `colors.again`; hard → border `colors.hard` + label `colors.accentDeep`; good → border `colors.good` (brandFill) + `colors.brandTint` background + label `colors.good`; easy → border `colors.easy` (brandSoft) + label `colors.brandSoft`. Label uses `fonts.uiSemibold` 12px; interval line uses `Mono` 9.5px muted. Import the `Rating` type from `@/src/lib/api`. No inline semantic hex — read from theme.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "export function RatingButtons" src/components/RatingButtons.tsx && grep -qE "again|good|easy" src/components/RatingButtons.tsx && grep -q "from '@/src/lib/api'" src/components/RatingButtons.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>RatingButtons renders 4 semantic-colored FSRS tiles with interval labels, calls onRate with the Rating type, uses theme semantic tokens; tsc passes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                          |
| ---------- | -------------------------------------------------------------------- |
| (none new) | Presentational components only — no network, input, storage, or auth |

## STRIDE Threat Register

| Threat ID | Category | Component            | Disposition | Mitigation Plan                                                                     |
| --------- | -------- | -------------------- | ----------- | ----------------------------------------------------------------------------------- |
| (none)    | —        | shared UI components | accept      | No new attack surface; props are render-only values from already-trusted app state. |

</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- StreakChip transition runs at exactly 280ms with no scale/bounce; 3 states match v6 colors.
- StatTile/Chip/Card/RatingButtons render with v6 tokens (verified by later-plan screens).
</verification>

<success_criteria>

- Streak chip shows 3 states and animates at-risk→continued over 280ms (SC #4 foundation).
- Reusable component kit (stat tiles, rating buttons, chips, surfaces) ready for screen reskins.
  </success_criteria>

<output>
Create `.planning/phases/01.1-v6-visual-redesign/01.1-02-SUMMARY.md` when done.
</output>
