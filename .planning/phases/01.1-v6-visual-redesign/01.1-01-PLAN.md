---
phase: 01.1-v6-visual-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/package.json
  - apps/mobile/app/_layout.tsx
  - apps/mobile/src/lib/theme.ts
  - apps/mobile/tailwind.config.js
  - apps/mobile/src/components/Type.tsx
autonomous: true
requirements: [V6-01, DSGN-01, DSGN-02]
must_haves:
  truths:
    - 'Geist (UI) and Instrument Serif (display) render across the app once loaded'
    - 'v6 token palette (cobalt, gold, 4-stop heat ramp, OLED) is available as theme constants and Tailwind colors'
    - "A surface-driven text primitive enforces 'text follows surface' so cobalt is never used as body copy"
  artifacts:
    - path: 'apps/mobile/src/lib/theme.ts'
      provides: 'v6 color tokens (brand/brand-fill/accent/heat ramp/OLED) + per-surface text color map + font family constants'
      contains: 'brandFill'
    - path: 'apps/mobile/tailwind.config.js'
      provides: 'v6 token colors + Geist/Geist Mono/Instrument Serif fontFamily entries'
      contains: 'brand-fill'
    - path: 'apps/mobile/src/components/Type.tsx'
      provides: 'surface-aware text primitives (Display, Heading, Body, Mono, Num, Action) that resolve color from surface'
      exports: ['Display', 'Body', 'Mono', 'Num']
    - path: 'apps/mobile/app/_layout.tsx'
      provides: 'font loading via useFonts + splash gate'
      contains: 'useFonts'
  key_links:
    - from: 'apps/mobile/app/_layout.tsx'
      to: '@expo-google-fonts packages'
      via: 'useFonts hook gates render until fonts loaded'
      pattern: 'useFonts'
    - from: 'apps/mobile/src/components/Type.tsx'
      to: 'apps/mobile/src/lib/theme.ts'
      via: 'imports textForSurface() to resolve color'
      pattern: 'textForSurface|surfaceText'
---

<objective>
Establish the v6 design-system foundation: load the three font families, port the full v6 token palette into both `theme.ts` and `tailwind.config.js`, and build the reusable surface-driven text primitives that enforce the "text follows surface, not brand" rule. Every later plan in this phase consumes these tokens and primitives.

Purpose: A single source of truth for color, type, and the surface text rule so the reskin is consistent and cobalt is never misused as body copy.
Output: Loaded fonts, v6 tokens in theme + Tailwind, `src/components/Type.tsx` text primitives.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current theme.ts contract that this plan extends (do not break Surface or useTheme consumers) -->
From apps/mobile/src/lib/theme.ts:
  export type Surface = 'light' | 'color' | 'oled';
  export const colors = { brand, brandLifted, accent, oled, oledElevated, surfaces: { light, color, oled } };
  export const ThemeContext (provides { surface, setSurface });
  export function useTheme(): { surface, setSurface };

app/\_layout.tsx already wraps the app in ThemeContext.Provider with surface state defaulting to 'light'.
global.css holds the three @tailwind directives; metro wires NativeWind via withNativeWind.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install fonts and gate render on font load</name>
  <read_first>
    - apps/mobile/app/_layout.tsx (current root layout, ThemeContext provider, Stack)
    - apps/mobile/package.json (confirm expo-font ~14.0.11 and expo-splash-screen ~31 present)
    - wireframes/Wireframes v6.html lines 8-10 (font import: Geist 400;500;600;700, Geist Mono 400;500, Instrument Serif ital 0;1)
    - wireframes/Wireframes v6.html lines 68-73, 86-97, 239-244 (where each family is applied: body=Geist, h1/greet=Instrument Serif italic, eyebrow/lbl=Geist Mono)
  </read_first>
  <action>
    Add the three Google Font packages via expo: `@expo-google-fonts/geist`, `@expo-google-fonts/geist-mono`, `@expo-google-fonts/instrument-serif` (run `npx expo install` for each so versions match SDK 54). In `app/_layout.tsx` import `useFonts` from `@expo-google-fonts/geist` and the specific weights needed: Geist Regular/Medium/SemiBold/Bold (400/500/600/700), GeistMono Regular/Medium (400/500), InstrumentSerif Regular plus the italic variant from `@expo-google-fonts/instrument-serif`. Call `useFonts({...})` with stable keys matching the Tailwind fontFamily names from Task 2 (e.g. `Geist`, `Geist_500Medium`, `Geist_600SemiBold`, `Geist_700Bold`, `GeistMono`, `GeistMono_500Medium`, `InstrumentSerif`, `InstrumentSerif_Italic`). Use `expo-splash-screen` `preventAutoHideAsync()` before render and `hideAsync()` once `fontsLoaded` is true; return `null` while `!fontsLoaded` so no flash of system font. Keep the existing `Audio.setAudioModeAsync` effect and ThemeContext provider unchanged.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "useFonts" app/_layout.tsx && grep -q "InstrumentSerif" app/_layout.tsx && grep -qE "@expo-google-fonts/(geist|instrument-serif)" package.json && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>The three font packages are in package.json; `_layout.tsx` calls useFonts, returns null until loaded, and hides the splash on load; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Port v6 tokens into theme.ts and tailwind.config.js</name>
  <read_first>
    - apps/mobile/src/lib/theme.ts (current colors object + Surface type — extend, do not remove existing keys still in use)
    - apps/mobile/tailwind.config.js (current colors map)
    - wireframes/Wireframes v6.html lines 15-64 (the :root token block — copy exact hex values)
    - wireframes/Wireframes v6.html lines 1636-1671 (text-follows-surface rule table: white/tint, gold, cobalt-filled, OLED — primary/muted/brand-emphasis per surface)
  </read_first>
  <action>
    In `theme.ts`, extend `colors` with the exact v6 hex values: brand `#1F3494`, brandDeep `#142468`, brandFill `#2E5BC8`, brandTint `#EFF3FB`, brandTint2 `#E8EEF7`, brandLight `#5A8FD4`, brandSoft `#7AA0DD`; heat ramp heat0 `#EFF3FB`, heat1 `#A5BFE8`, heat2 `#5A8FD4`, heat3 `#2E5BC8`; accent `#E8B838`, accentDeep `#C99A1F`; semantic again `#C84A40`, hard `#E8B838`, good = brandFill, easy = brandSoft; surfaces paper `#FFFFFF`, paperSoft `#F7F7F8`, paperSoft2 `#EFEFF1`, grayRule `#D5D5D5`; gender fem `#B43A6C`, masc `#1F3494`, common `#C99A1F`. Update `accent` to `#E8B838` (was `#F0BF38`). Add a `text` map keyed by Surface listing primary/muted/faint for each surface per the rule table: light → `#000000` / `rgba(0,0,0,0.60)` / `rgba(0,0,0,0.42)`; color(cobalt) → `#FFFFFF` / `rgba(255,255,255,0.75)`; oled → `#FFFFFF` / `rgba(255,255,255,0.70)`; plus brand-emphasis per surface (light `#1F3494`, oled `#5A8FD4`, n/a on cobalt/gold). Add a `gold` surface entry for text (`#1F3494` / `rgba(31,52,148,0.65)`). Add `fonts` constants object mapping logical names to the loaded family keys from Task 1 (display=`InstrumentSerif`, displayItalic=`InstrumentSerif_Italic`, ui=`Geist`, uiMedium=`Geist_500Medium`, uiSemibold=`Geist_600SemiBold`, uiBold=`Geist_700Bold`, mono=`GeistMono`, monoMedium=`GeistMono_500Medium`). Export a helper `textForSurface(surface, kind)` returning the right color string. Keep `Surface`, `ThemeContext`, `useTheme` exports intact. Mirror the same color tokens into `tailwind.config.js` `theme.extend.colors` using kebab keys: `brand`, `brand-deep`, `brand-fill`, `brand-tint`, `brand-light`, `brand-soft`, `accent` (`#E8B838`), `accent-deep`, `heat-0..heat-3`, `again`, `gender-fem` (`#B43A6C`), `gender-masc`, `gender-common`, `paper-soft`, `paper-soft-2`, `gray-rule`. Add `theme.extend.fontFamily` entries: `geist: ['Geist']`, `geist-medium`, `geist-semibold`, `geist-bold`, `mono: ['GeistMono']`, `serif: ['InstrumentSerif']`, `serif-italic: ['InstrumentSerif_Italic']`.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "brandFill" src/lib/theme.ts && grep -q "textForSurface" src/lib/theme.ts && grep -q "#E8B838" src/lib/theme.ts && grep -q "brand-fill" tailwind.config.js && grep -q "heat-3" tailwind.config.js && grep -q "fontFamily" tailwind.config.js && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>theme.ts exposes the full v6 token set, a per-surface text map, fonts constants, and textForSurface(); tailwind.config.js mirrors the colors and adds the three font families; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Build surface-driven text primitives</name>
  <read_first>
    - apps/mobile/src/lib/theme.ts (the textForSurface helper + fonts constants added in Task 2; useTheme/Surface)
    - wireframes/Wireframes v6.html lines 1627-1716 (text rule section: what is black, what is cobalt — greeting/subtitle/labels/axis = primary; big numbers/key actions/active toggles/tag chips = brand emphasis)
    - wireframes/Wireframes v6.html lines 234-263, 894-900 (type scale: .greet 30px Instrument Serif, .h-disp 30px, .h-1 22px, .lbl-mono 10px Geist Mono uppercase 0.12em, .due-num 64px serif)
  </read_first>
  <action>
    Create `src/components/Type.tsx` exporting React Native `Text` wrappers that resolve color from the current surface via `useTheme()` + `textForSurface()` (override-able by an optional `surface` prop and `tone` prop of `primary | muted | faint | brand`). Components: `Display` (Instrument Serif, default 30px, optional `italic` prop → uses `InstrumentSerif_Italic`), `Heading` (Instrument Serif 22px), `Body` (Geist 15px, weight prop), `Mono` (Geist Mono 10px, uppercase, letterSpacing 0.12em — the eyebrow/label style), `Num` (Instrument Serif, large, defaults to `tone="brand"` since big numbers are the one brand-emphasis text), and `Action` (Geist 14px semibold, defaults to `tone="brand"` on light surfaces for key action labels). Each must pull `fontFamily` from the `fonts` constants — never rely on a `fontWeight`-only style for Geist/serif since custom fonts need explicit family names. Enforce the rule: `tone="brand"` resolves to cobalt only on `light`/`oled` surfaces and falls back to primary on cobalt/gold (textForSurface handles this). Do NOT inline hex values in the component; read them all from theme.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -qE "export function (Display|Body|Mono|Num)" src/components/Type.tsx && grep -q "textForSurface" src/components/Type.tsx && grep -q "InstrumentSerif" src/components/Type.tsx && ! grep -q "#1F3494" src/components/Type.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Type.tsx exports Display/Heading/Body/Mono/Num/Action; colors resolve from theme via textForSurface (no inline hex); Num/Action default to brand emphasis; tsc passes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                                                  |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------- |
| (none new) | This plan adds fonts, theme tokens, and presentational primitives only — no network, input parsing, storage, or auth surface |

## STRIDE Threat Register

| Threat ID | Category  | Component                            | Disposition | Mitigation Plan                                                                                                                                          |
| --------- | --------- | ------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-01.1-SC | Tampering | npm installs (@expo-google-fonts/\*) | mitigate    | Use `npx expo install` (Expo-curated, version-pinned). @expo-google-fonts is a first-party Expo org package set; no [ASSUMED]/[SUS] checkpoint required. |

</threat_model>

<verification>
- `npx tsc --noEmit` passes for the mobile app.
- App boots without a flash of system font; Instrument Serif + Geist visibly render.
- theme.ts and tailwind.config.js contain the exact v6 hex tokens from the wireframe :root.
</verification>

<success_criteria>

- Geist + Instrument Serif load and render app-wide (foundation for SC #1).
- v6 token palette available in both theme.ts and Tailwind.
- Surface-driven text primitives exist and enforce the cobalt-only-on-emphasis rule.
  </success_criteria>

<output>
Create `.planning/phases/01.1-v6-visual-redesign/01.1-01-SUMMARY.md` when done.
</output>
