---
phase: 01.1-v6-visual-redesign
plan: 05
type: execute
wave: 3
depends_on: ['01.1-01', '01.1-02']
files_modified:
  - apps/mobile/app/(tabs)/index.tsx
  - apps/mobile/app/(tabs)/_layout.tsx
autonomous: true
requirements: [V6-05, DSGN-02]
must_haves:
  truths:
    - 'Home renders on a light surface with black text — cobalt appears only on the due number, key action labels, active tab, and brand chips'
    - 'Home shows its three v6 states (continued today / daily pickup / inactive day-1) with the corner streak chip'
    - "A single '+ Add' entry replaces the two add buttons"
  artifacts:
    - path: 'apps/mobile/app/(tabs)/index.tsx'
      provides: 'v6 home: light surface, surface-driven greeting, due tile, recently-added, three states, streak chip'
      contains: 'due now'
    - path: 'apps/mobile/app/(tabs)/_layout.tsx'
      provides: 'v6 tab bar (active = brand) consistent with home'
      contains: 'tabBarActiveTintColor'
  key_links:
    - from: 'apps/mobile/app/(tabs)/index.tsx'
      to: 'apps/mobile/src/components/Type.tsx'
      via: 'greeting/labels use surface-driven primitives (black, not cobalt)'
      pattern: 'Display|Body|Mono'
    - from: 'apps/mobile/app/(tabs)/index.tsx'
      to: 'apps/mobile/src/components/StreakChip.tsx'
      via: 'corner chip reflects streak state'
      pattern: 'StreakChip'
---

<objective>
Reskin Home to v6 §3 and the text rule §1: a light surface with black text where cobalt is reserved for the big due number, "Start review"/"+ Add" key actions, the active tab, and brand chips. Render the three v6 states — continued today (filled chip), daily pickup (at-risk chip, due tile, recently-added word), and inactive day-1 (gray chip, empty-deck prompt) — and collapse the two add buttons into one "+ Add".

Purpose: Bring Home into the v6 surface-driven system and enforce DSGN-02 app-side.
Output: Reskinned `(tabs)/index.tsx` + `(tabs)/_layout.tsx`.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

<interfaces>
<!-- From Plans 01 & 02 -->
From apps/mobile/src/components/Type.tsx: Display, Heading, Body, Mono, Num, Action
From apps/mobile/src/components/StreakChip.tsx: StreakChip({ count, state }), StreakState
From apps/mobile/src/components/Chip.tsx: Chip; Surface.tsx: Card
From apps/mobile/src/lib/theme.ts: colors, fonts, useTheme

<!-- Existing home contract to preserve -->

From apps/mobile/app/(tabs)/index.tsx:
useQuery(['cards','due']) → DueCard[]; useQuery(['users','me']) → { name }; getGreeting(); getStreakState(streakCount,dueCount); cardCounts();
states: LoadingState / ErrorState / EmptyState(deck empty) / AllDoneState(due=0) / DailyPickupState(due>0).
DueCard fields used: state, gender, headword, sentence_pt, gendered_form.
From apps/mobile/app/(tabs)/\_layout.tsx: Tabs with tabBarActiveTintColor '#1F3494', Ionicons home/albums/add-circle/settings.
</interfaces>

<surface_text_rule>
From wireframes/Wireframes v6.html lines 1627-1716 + 1829-1990:

- Home surface is WHITE/light (not cobalt) — the old bg-brand full-bleed home is replaced.
- Black: greeting, subtitle, stat labels, recently-added headword, "today" labels.
- Cobalt: the due number (Instrument Serif 64px), "Start review"/key-action labels, active tab, brand chips, continued streak chip.
- Three states map to: continued today (H.1, chip continued, due 0, today stats), daily pickup (H.2, chip at-risk, due N tile + breakdown chips + recently-added word card), inactive day-1 (H.3, chip inactive, empty-deck prompt + try-these chips).
  </surface_text_rule>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Home to a light surface + v6 header/greeting/due tile</name>
  <read_first>
    - apps/mobile/app/(tabs)/index.tsx (full file — SafeAreaView bg-brand → light; topbar, greeting, due tile, buttons)
    - wireframes/Wireframes v6.html lines 1838-1940 (H.1 + H.2: topbar icon-btn + streak chip; .greet Instrument Serif w/ italic name; subtitle muted; .due-tile paper card radius 14, .due-label mono, .due-num serif 64px cobalt, breakdown chips; "Start review" btn (brand) vs "+ Add" (alt))
    - wireframes/Wireframes v6.html lines 219-229, 239-266 (.topbar, .icon-btn paper-soft; .greet/.greet em styles; .due-tile spec)
    - apps/mobile/src/components/* (Display, Mono, Num, Chip, StreakChip, Card)
  </read_first>
  <action>
    Change the root `SafeAreaView` from `bg-brand` to a light surface (paper `#FFFFFF`) and apply the local `'light'` surface so Type primitives resolve black text. Replace the white `≡` and white-on-brand text with v6: an `icon-btn` (paper-soft rounded square) `≡` on the left and the `StreakChip` on the right of the topbar. Rebuild the greeting with `Display` (Instrument Serif) and an italic name span via `Display italic` muted (e.g. "Bom dia, *Léo*."), subtitle as muted `Body` 13px. Build the due tile as a paper `Card` (radius 14, 0.5px divider): a `Mono` "due now" label, a `Num` due count (Instrument Serif ~64px, cobalt — the one big-number brand emphasis), and a breakdown `Chip` row (new = brand variant, learning/review = default). Keep the `useQuery` data, `getGreeting`, `cardCounts`, and `withName` helpers. Keep `getStreakState`'s logic and feed the chip from it.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "StreakChip" "app/(tabs)/index.tsx" && grep -q "due now" "app/(tabs)/index.tsx" && grep -qE "Display|Num" "app/(tabs)/index.tsx" && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Home is a light surface with black text; topbar has icon-btn + StreakChip; greeting is serif with italic name; due tile is a paper Card with cobalt serif number and breakdown chips; data wiring unchanged; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin the three home states + collapse to one "+ Add"</name>
  <read_first>
    - apps/mobile/app/(tabs)/index.tsx (EmptyState, AllDoneState, DailyPickupState, CTARow, RecentlyAdded, PrimaryButton)
    - wireframes/Wireframes v6.html lines 1838-1887 (H.1 continued: filled chip, "Tudo pronto", due 0 tile "next batch · 4h" brand chip, "+ Add" alt button, today stats card reviewed/accuracy)
    - wireframes/Wireframes v6.html lines 1889-1940 (H.2 daily: at-risk chip, "Bom dia", due 23 tile, "▶ Start review" brand button, recently-added .word-card with gender rail + fem chip + "added 2h ago · due now")
    - wireframes/Wireframes v6.html lines 1942-1987 (H.3 inactive: gray chip, "Bem-vindo", "start with / A word, or a sentence." serif, "+ Add a word or sentence" brand button, try-these chips)
    - wireframes/Wireframes v6.html lines 307-330 (.word-card: gender rail fem #B43A6C / masc brand / common accentDeep, black headword, muted meta)
  </read_first>
  <action>
    Collapse `CTARow`'s two buttons ("+ Add a word" / "Paste a sentence") into a single "+ Add" action that navigates to `/(tabs)/add` (the merged wizard from Plan 03). Reskin each state to v6: EmptyState (inactive day-1) → gray streak chip context, `Display` "Bem-vindo", empty-deck `Card` with "start with / A word, or a sentence." serif + helper, a brand `Action` "+ Add a word or sentence", and try-these `Chip`s. AllDoneState (continued today) → "Tudo pronto" `Display`, due-0 `Card` with a "next batch · Nh" brand `Chip`, a "+ Add" alt button, and a today-stats `Card` (reviewed / accuracy as `Body` rows with bold values). DailyPickupState (daily) → greeting + due tile (Task 1) + a brand `Action` "▶ Start review" → `router.push('/review')`, then a "recently added" `Mono` label and the recently-added word rendered via the v6 `.word-card` (use `Card` with the gender rail; headword in black `Body` semibold, a gender `Chip`, and a muted meta line). Replace `PrimaryButton`'s white-on-brand styling with the v6 button styles (brand fill for primary actions, alt/paper for secondary). Keep all routing targets and the `recentCards` slice.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -qE "\\+ Add" "app/(tabs)/index.tsx" && ! grep -q "Paste a sentence" "app/(tabs)/index.tsx" && grep -q "Start review" "app/(tabs)/index.tsx" && grep -q "recently added" "app/(tabs)/index.tsx" && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>One "+ Add" entry (no "Paste a sentence"); all three states reskinned to v6 with the corner chip, due tile, and recently-added word card; routing preserved; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Align the tab bar to v6</name>
  <read_first>
    - apps/mobile/app/(tabs)/_layout.tsx (current Tabs config, colors, icons)
    - wireframes/Wireframes v6.html lines 295-305 (.tabbar: 4 cols, 10px labels, .tab.on = brand color + brand icon; inactive = faint)
    - apps/mobile/src/lib/theme.ts (brand token)
  </read_first>
  <action>
    Update `(tabs)/_layout.tsx` to use the v6 tokens from theme: `tabBarActiveTintColor` = `colors.brand` (`#1F3494`), inactive = `rgba(0,0,0,0.42)` (text-on-light-faint), `tabBarStyle` border = `colors.divider`/grayRule 0.5px on a paper background, and a Geist label font where the tab bar supports `tabBarLabelStyle.fontFamily`. Keep the four tabs (home/cards/add/settings) and their Ionicons. Do not change navigation structure.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "tabBarActiveTintColor" "app/(tabs)/_layout.tsx" && grep -qE "colors\\.|#1F3494" "app/(tabs)/_layout.tsx" && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Tab bar uses v6 brand active color + faint inactive + paper/divider styling from theme; four tabs intact; tsc passes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary            | Description                                                             |
| ------------------- | ----------------------------------------------------------------------- |
| home → existing API | Already-trusted api.getDueCards / api.getMe; no request/response change |

## STRIDE Threat Register

| Threat ID  | Category | Component             | Disposition | Mitigation Plan                                                                                  |
| ---------- | -------- | --------------------- | ----------- | ------------------------------------------------------------------------------------------------ |
| (none new) | —        | home screen + tab bar | accept      | Visual reskin only; renders already-trusted query data; no new network, input, storage, or auth. |

</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- Home is a light surface; greeting/labels are black; cobalt appears only on the due number, key actions, active tab, brand chips, and continued chip.
- All three states render with the corner chip; one "+ Add" entry only.
</verification>

<success_criteria>

- Home matches the v6 layout and palette across its three states (SC #3).
- No cobalt-on-white body copy (SC #1 / DSGN-02).
  </success_criteria>

<output>
Create `.planning/phases/01.1-v6-visual-redesign/01.1-05-SUMMARY.md` when done.
</output>
