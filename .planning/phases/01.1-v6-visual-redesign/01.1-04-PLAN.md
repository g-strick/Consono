---
phase: 01.1-v6-visual-redesign
plan: 04
type: execute
wave: 3
depends_on: ['01.1-01', '01.1-02']
files_modified:
  - apps/mobile/app/review/index.tsx
  - apps/mobile/src/lib/useNightSurface.ts
autonomous: true
requirements: [V6-04, DSGN-01]
must_haves:
  truths:
    - "Review front shows no image and no word — only a gold play button, waveform, and 'O que ouves?'"
    - 'Reveal shows the full card and the 4 semantic FSRS rating buttons with intervals'
    - 'Session-done screen uses the filled-cobalt surface and ticks the streak chip'
    - 'Night reviews auto-switch to the OLED surface (lifted-brand text emphasis)'
  artifacts:
    - path: 'apps/mobile/app/review/index.tsx'
      provides: 'v6 review loop (intro → audio-only front → reveal&rate → done) with OLED night theme'
      contains: 'O que ouves'
    - path: 'apps/mobile/src/lib/useNightSurface.ts'
      provides: 'hook deciding OLED vs light surface from system dark mode + time-of-day'
      exports: ['useNightSurface']
  key_links:
    - from: 'apps/mobile/app/review/index.tsx'
      to: 'apps/mobile/src/components/RatingButtons.tsx'
      via: 'reveal screen renders RatingButtons, onRate → handleRate'
      pattern: 'RatingButtons'
    - from: 'apps/mobile/app/review/index.tsx'
      to: 'apps/mobile/src/lib/useNightSurface.ts'
      via: 'surface drives bg/text tokens for OLED night mode'
      pattern: 'useNightSurface'
---

<objective>
Reskin the review loop to v6 §R: session intro (queue size + streak chip), audio-only front (gold play button, waveform, "O que ouves?", speed hint, Don't know / Reveal), reveal & rate (full card + semantic FSRS buttons), and the filled-cobalt session-done screen with the streak tick. Apply the OLED night theme (DSGN-01): when the system is in dark mode during night hours, the review surfaces switch to OLED black with lifted-brand emphasis.

Purpose: Deliver the v6 review experience and the auto night theme.
Output: Reskinned `review/index.tsx` + `useNightSurface.ts`.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/Consono/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/Consono/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

<interfaces>
<!-- From Plans 01 & 02 -->
From apps/mobile/src/components/Type.tsx: Display, Heading, Body, Mono, Num, Action
From apps/mobile/src/components/RatingButtons.tsx: RatingButtons({ intervals?, onRate })
From apps/mobile/src/components/StreakChip.tsx: StreakChip({ count, state }), StreakState
From apps/mobile/src/components/Chip.tsx: Chip; Surface.tsx: Card
From apps/mobile/src/lib/theme.ts: colors, fonts, useTheme, textForSurface, Surface type ('light'|'color'|'oled')

<!-- Existing review contract to preserve -->

From apps/mobile/app/review/index.tsx:
Phase = 'intro'|'front'|'back'|'done'; useQuery(['cards','due']); reviewMutation = api.submitReview(card_id, rating, duration_ms);
handleBegin/handleReveal/handleDontKnow(=again)/handleRate; playAudio(url) via expo-av; sessionStats { reviewed, again, startMs }.
From apps/mobile/src/lib/theme.ts: ThemeContext provider in \_layout.tsx with setSurface — review can set surface for the session.
</interfaces>

<surface_rule>
From wireframes/Wireframes v6.html lines 1636-1671: light → black text, cobalt emphasis; OLED black → white text, lifted-brand (#5A8FD4) emphasis; cobalt-filled → white text.
DSGN-01 (STATE.md): "OLED theme is time-triggered, not user-toggled." Trigger = system dark mode AND night hours.
</surface_rule>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useNightSurface hook (system dark mode + time-of-day → OLED)</name>
  <read_first>
    - apps/mobile/src/lib/theme.ts (Surface type, colors, ThemeContext)
    - .planning/STATE.md lines 44 (OLED theme is time-triggered, not user-toggled)
    - wireframes/Wireframes v6.html lines 1664-1668, 1695-1700 (OLED surface: black bg, white text, lifted-brand emphasis #5A8FD4)
  </read_first>
  <action>
    Create `src/lib/useNightSurface.ts` exporting `useNightSurface(): Surface`. Use React Native `useColorScheme()` for system dark mode and the current hour. Return `'oled'` when `colorScheme === 'dark'` AND the hour is in the night window (≥ 19 or < 6); otherwise `'light'`. Keep the time check simple and deterministic (read `new Date().getHours()` once per render is acceptable for this phase; do not add a timer). Pure hook, no side effects beyond reading color scheme. Do not flip the global app surface here — return the value so the review screen can apply it locally.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "export function useNightSurface" src/lib/useNightSurface.ts && grep -q "useColorScheme" src/lib/useNightSurface.ts && grep -qE "oled" src/lib/useNightSurface.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>useNightSurface returns 'oled' only when system dark mode + night hours, else 'light'; pure hook; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin intro + audio-only front to v6 (gold play, waveform)</name>
  <read_first>
    - apps/mobile/app/review/index.tsx (intro + front phases — keep playAudio, handleBegin, handleReveal, handleDontKnow, the index/phase state)
    - wireframes/Wireframes v6.html lines 1460-1519 (R.1 intro: × Exit + streak chip, "Vamos lá.", "23 cards · ~7 min", new/review chips, ▶ Begin; R.2 front: gold progress, "listen" mono, .audio-btn.xl 76px brand play, waveform svg, italic serif "O que ouves?", speed hint 0.7×/1.0×/1.2×, Don't know / Reveal ↓)
    - wireframes/Wireframes v6.html lines 942-951 (.audio-btn.xl 76×76 round, shadow; note R.2 play uses brand but R.2 marker calls gold — use gold accent per the "gold play button" spec in ROADMAP/REQUIREMENTS RVEW-01)
    - apps/mobile/src/components/* (Display, Heading, Mono, Chip, StreakChip), useNightSurface
  </read_first>
  <action>
    Apply `useNightSurface()` at the top of `ReviewScreen` and use it to pick bg + text tokens for all phases (light → paper bg/black text, oled → `colors.oled` bg/white text + lifted-brand emphasis). Intro phase: × Exit (Action), a `StreakChip` in the wiz-top (derive state/count from session — use the queue/streak you have; streak count may be a placeholder this phase but state should be `at-risk` pre-completion), centered `Display` "Vamos lá.", `Heading muted` "{n} cards · ~{est} min", new/review `Chip`s (new = brand variant), helper mono line, and a full-width Begin button. Front phase: gold 3px progress bar, "listen" `Mono` faint, a 76px round play button in gold accent (`colors.accent`) with the v6 shadow, a waveform — render the v6 SVG path (the `<path d="M 0 15 Q 5 6 ...">` from the wireframe) via `react-native-svg` in brand color (lifted-brand on OLED), an italic serif "O que ouves?" via `Display italic` muted, the speed hint `tap to replay · 0.7× · 1.0× · 1.2×` as a faint mono line (display-only — real speed toggle is Phase 2 RVEW-05), and a Don't know / Reveal ↓ row. Keep the front audio autoplay effect and `playAudio` exactly as-is. Confirm `react-native-svg` is available (it ships with Expo SDK 54 / expo-router); if not, `npx expo install react-native-svg`.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "useNightSurface" app/review/index.tsx && grep -q "O que ouves" app/review/index.tsx && grep -q "StreakChip" app/review/index.tsx && grep -qE "Svg|react-native-svg" app/review/index.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Intro shows streak chip + queue + Begin; front is audio-only (gold play, waveform svg, "O que ouves?", speed hint, Don't know / Reveal); OLED tokens applied via useNightSurface; audio wiring unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Reskin reveal & rate + filled-cobalt session done</name>
  <read_first>
    - apps/mobile/app/review/index.tsx (back + done phases — keep handleRate, reviewMutation, sessionStats, accuracy/time math)
    - wireframes/Wireframes v6.html lines 1522-1556 (R.3 reveal: image, gender Card, serif headword + gender chip, stress mono, sentence + audio btn, EN-reveal chip, "how did it go?" + rate-row)
    - wireframes/Wireframes v6.html lines 1582-1611 (R.5 done: phone bg = brand-fill cobalt, white text, streak chip ticks 12→13 with translucent bg, "today's session" mono, "Acabou." Display, big gold "23/23" serif, translucent stat block new/reviewed/accuracy/time, white Home button → brand text)
    - apps/mobile/src/components/RatingButtons.tsx, StreakChip.tsx, Card.tsx, Type.tsx
  </read_first>
  <action>
    Back (reveal) phase: render the image, then a gender `Card` with serif `Heading` headword + gender `Chip`, stress `Mono`, the sentence `Body`, a ghost audio button (keep the existing `playAudio` for sentence audio), and an "EN reveal" `Chip`; below, a `Mono` "how did it go?" label and `<RatingButtons onRate={handleRate} />` (intervals optional — pass real next-interval strings if available, otherwise the defaults). Apply OLED tokens when `surface==='oled'`. Done phase: set the surface to filled-cobalt — bg `colors.brandFill`, white text — render × complete + a translucent `StreakChip` (continued state, count ticked +1; the chip's 280ms fill is the celebratory moment), "today's session" `Mono` at white/55, `Display` "Acabou." in white, a big serif total `{reviewed}/{total}` in `colors.accent` gold, a translucent stat block (new/reviewed/accuracy/time) reusing the existing accuracy/time computation, and a white "Home" button with brand text that calls `exitReview`. Keep all rating/submit logic and the `next >= cards.length → done` transition intact.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "RatingButtons" app/review/index.tsx && grep -q "Acabou" app/review/index.tsx && grep -q "brandFill" app/review/index.tsx && grep -q "handleRate" app/review/index.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Reveal shows full card + semantic RatingButtons wired to handleRate; done screen is filled-cobalt with gold total, stats, streak-chip tick, and white Home button; submit logic unchanged; tsc passes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                                          |
| --------------------- | ------------------------------------------------------------------------------------ |
| review → existing API | Already-trusted Phase 1 api.submitReview / api.getDueCards; request shapes unchanged |

## STRIDE Threat Register

| Threat ID  | Category | Component                       | Disposition | Mitigation Plan                                                                                                           |
| ---------- | -------- | ------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------- |
| (none new) | —        | review screen + useNightSurface | accept      | Visual reskin + a pure presentational hook reading useColorScheme/clock; no new network, input parsing, storage, or auth. |

</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- Front shows only the play button (no image, no word).
- Done screen renders on the filled-cobalt surface; streak chip animates the tick.
- With system dark mode at night, review surfaces render OLED black with white text.
</verification>

<success_criteria>

- Review front is audio-only; reveal + session-done match v6 (SC #3).
- Streak chip ticks at session complete (SC #4).
- Night review auto-switches to OLED (DSGN-01).
  </success_criteria>

<output>
Create `.planning/phases/01.1-v6-visual-redesign/01.1-04-SUMMARY.md` when done.
</output>
