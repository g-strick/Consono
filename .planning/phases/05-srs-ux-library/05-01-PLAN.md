---
phase: 05-srs-ux-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema.ts
  - packages/db/drizzle/
autonomous: true
requirements: [SRS-01]
deferred_requirements:
  note: 'SRS-02 (filtered review session picker), SRS-03 (study ahead), SRS-04 (5-card sprint), SRS-05 (new-cards/day limit) are explicitly DEFERRED out of Phase 5 per 05-CONTEXT.md <deferred>. They are NOT implemented in any Phase 5 plan and will be addressed in a future phase. This note satisfies coverage acknowledgment; no task implements them.'
  ids: [SRS-02, SRS-03, SRS-04, SRS-05]

must_haves:
  truths:
    - 'The cards table has a nullable suspended_at timestamp column (null = active, timestamp = when suspended)'
    - 'The Drizzle migration for suspended_at is generated and applied to the database'
  artifacts:
    - path: 'packages/db/src/schema.ts'
      provides: 'suspended_at column on cards table'
      contains: "suspended_at: timestamp('suspended_at')"
    - path: 'packages/db/drizzle/'
      provides: 'generated 0002 migration adding suspended_at'
      contains: 'ADD COLUMN "suspended_at"'
  key_links:
    - from: 'packages/db/src/schema.ts'
      to: 'packages/db/drizzle/0002_*.sql'
      via: 'drizzle-kit generate'
      pattern: 'suspended_at'
---

<objective>
Add a nullable `suspended_at: timestamp` column to the `cards` table (D-08) and generate + apply the Drizzle migration. This column is the sole source of truth for card suspension — suspension is NOT a `card_state` enum value (Pitfall 5). All downstream Phase 5 plans depend on this column existing in both the schema and the live database.

Purpose: Enables SRS-01 (suspend/unsuspend). A suspended card has `suspended_at != null` and is excluded from review queues; unsuspending sets it back to null with no FSRS reschedule (D-07).
Output: Modified `schema.ts` + generated `0002_*.sql` migration applied to the DB.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/Consono/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/Consono/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-srs-ux-library/05-CONTEXT.md
@.planning/phases/05-srs-ux-library/05-RESEARCH.md
@.planning/phases/05-srs-ux-library/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add suspended_at column to cards table</name>
  <files>packages/db/src/schema.ts</files>
  <read_first>
    - packages/db/src/schema.ts (the `cards` pgTable definition; `suspended_at` goes after `last_reviewed_at` and before `created_at`)
    - packages/db/drizzle/0001_redundant_inertia.sql (precedent: nullable column added via single ALTER TABLE)
  </read_first>
  <action>
    In `packages/db/src/schema.ts`, inside the `cards = pgTable('cards', {...})` definition, add a new column `suspended_at: timestamp('suspended_at')` placed immediately after the existing `last_reviewed_at: timestamp('last_reviewed_at')` line and before `created_at`. Use the already-imported `timestamp` helper from `drizzle-orm/pg-core` (no new import). Do NOT add `.notNull()` — the column must be nullable (null = active, timestamp = suspended). Do NOT touch `cardStateEnum` — suspension is the `suspended_at` column only, never a `state` enum value.
  </action>
  <verify>
    <automated>grep -q "suspended_at: timestamp('suspended_at')" packages/db/src/schema.ts &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `packages/db/src/schema.ts` contains the exact string `suspended_at: timestamp('suspended_at')`
    - The new column appears inside the `cards` pgTable block (between `last_reviewed_at` and `created_at`)
    - `cardStateEnum` definition is unchanged (still `['new', 'learning', 'review', 'relearning']`)
    - No new imports added (timestamp already imported)
  </acceptance_criteria>
  <done>The cards table schema declares a nullable `suspended_at` timestamp column.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Generate and apply suspended_at migration</name>
  <files>packages/db/drizzle/</files>
  <read_first>
    - packages/db/drizzle/0001_redundant_inertia.sql (expected migration shape: single ALTER TABLE ADD COLUMN)
    - Makefile (targets `db-generate` and `db-migrate`)
  </read_first>
  <action>
    Run `make db-generate` to generate the migration SQL from the schema change (this invokes `cd packages/db && pnpm exec drizzle-kit generate`). Drizzle Kit will emit a new `0002_*.sql` file in `packages/db/drizzle/` containing `ALTER TABLE "cards" ADD COLUMN "suspended_at" timestamp;`. Then run `make db-migrate` to apply it to the live database (`pnpm exec drizzle-kit migrate`). This task is [BLOCKING] — it must run AFTER Task 1 and BEFORE any verification; the rest of the phase cannot pass verification without the column existing in the database. If `make db-generate` reports no schema change detected, confirm Task 1 actually saved the column before re-running. Use the verified Makefile targets `db-generate`/`db-migrate` (NOT `db:generate`/`db:push`).
  </action>
  <verify>
    <automated>ls packages/db/drizzle/0002_*.sql >/dev/null 2>&amp;1 &amp;&amp; grep -lq 'ADD COLUMN "suspended_at"' packages/db/drizzle/0002_*.sql &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - A new file matching `packages/db/drizzle/0002_*.sql` exists
    - That file contains `ALTER TABLE "cards" ADD COLUMN "suspended_at" timestamp;`
    - `make db-migrate` completes without error (migration applied to DB)
  </acceptance_criteria>
  <done>The `suspended_at` migration is generated and applied; the column exists in the live database.</done>
</task>

</tasks>

<verification>
- `schema.ts` declares `suspended_at: timestamp('suspended_at')` in the cards table
- A `0002_*.sql` migration adding the column exists and has been applied via `make db-migrate`
- `corepack pnpm test` still passes (no regressions in pure-lib tests)
</verification>

<success_criteria>
The cards table has a nullable `suspended_at` column in both schema and database, unblocking the API and UI plans that read/write suspension state.
</success_criteria>

<output>
Create `.planning/phases/05-srs-ux-library/05-01-SUMMARY.md` when done.
</output>
