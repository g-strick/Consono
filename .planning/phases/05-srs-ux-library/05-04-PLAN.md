---
phase: 05-srs-ux-library
plan: 04
type: execute
wave: 3
depends_on: ['05-02']
files_modified:
  - apps/mobile/app/_layout.tsx
  - apps/mobile/app/cards/[id].tsx
autonomous: false
requirements: [SRS-01, SRS-06, LIB-02, LIB-03]

must_haves:
  truths:
    - 'GestureHandlerRootView wraps the entire app root (enables swipe gestures app-wide)'
    - 'The cards/[id] route is registered in the Stack navigator'
    - 'Tapping a library row opens a card detail screen showing six SRS stats'
    - 'The detail screen can edit sentence_pt and source_tag, suspend/unsuspend, and delete with confirmation'
  artifacts:
    - path: 'apps/mobile/app/_layout.tsx'
      provides: 'GestureHandlerRootView wrapper + cards/[id] Stack.Screen registration'
      contains: 'GestureHandlerRootView'
    - path: 'apps/mobile/app/cards/[id].tsx'
      provides: 'card detail screen: stats grid, editable fields, suspend toggle, delete'
      min_lines: 120
  key_links:
    - from: 'apps/mobile/app/cards/[id].tsx'
      to: 'api.getCard / updateCard / suspendCard / deleteCard'
      via: 'useQuery + useMutation with cache invalidation'
      pattern: "api\\.(getCard|updateCard|suspendCard|deleteCard)"
    - from: 'apps/mobile/app/_layout.tsx'
      to: 'react-native-gesture-handler'
      via: 'GestureHandlerRootView import + outermost wrapper'
      pattern: 'GestureHandlerRootView'
---

<objective>
Build the unified card detail screen at `app/cards/[id].tsx` (D-04) AND do all shared `app/_layout.tsx` infra wiring: wrap the app root in `GestureHandlerRootView` (required before ANY swipe works — Pitfall 2) and register the `cards/[id]` Stack.Screen.

This plan OWNS `_layout.tsx` so that the wave-4 library plan (05-03) does not touch it (file-ownership / no overlap). The detail route must also exist before 05-03 because `typedRoutes: true` requires `app/cards/[id].tsx` to be registered for the library's `router.push({ pathname: '/cards/[id]', ... })` to typecheck — hence detail (wave 3) precedes library (wave 4).

Purpose: SRS-06 (per-card stats), SRS-01 (suspend toggle), LIB-02 (edit sentence + source_tag), LIB-03 (delete with confirmation).
Output: Modified `_layout.tsx`, new `cards/[id].tsx`.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/LingoCards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-srs-ux-library/05-RESEARCH.md
@.planning/phases/05-srs-ux-library/05-PATTERNS.md
@.planning/phases/05-srs-ux-library/05-UI-SPEC.md

<interfaces>
api methods (from 05-02, apps/mobile/src/lib/api.ts):
  api.getCard(id: number): Promise<{ card: AllCard }>
  api.updateCard(id, { sentence_pt?, source_tag? }): Promise<{ card: AllCard }>
  api.suspendCard(id, suspend: boolean): Promise<void>
  api.deleteCard(id): Promise<void>

AllCard fields used here: headword, gendered_form, gender, image_url, sentence_pt,
source_tag, stability, difficulty, state, due_at, reps, last_reviewed_at, suspended_at.

Stat formatters (from 05-02, apps/mobile/src/lib/cardUtils.ts):
formatDueAt(dueAt: string): string
formatLastReviewed(lastReviewedAt: string | null): string

Reused components (do NOT respecify — Phase 1.1):
<Display>, <Body>, <Num>, <Mono> from src/components/Type.tsx
<StatTile value label sub style> from src/components/StatTile.tsx
<Chip label variant color style> from src/components/Chip.tsx
colors, fonts, textColors from src/lib/theme.ts

TanStack keys to invalidate on every mutation:
['cards'] (clears 'all' + 'due' + per-card), ['home','summary'] (totalCards changes)

Current \_layout.tsx tree (apps/mobile/app/\_layout.tsx) returns:
<QueryClientProvider><ThemeContext.Provider><Stack>...3 screens...</Stack><StatusBar/></ThemeContext.Provider></QueryClientProvider>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build card detail screen app/cards/[id].tsx</name>
  <files>apps/mobile/app/cards/[id].tsx</files>
  <read_first>
    - apps/mobile/app/streak/index.tsx (analog: detail screen with useQuery, back nav, StatTile grid, ScrollView + StyleSheet root)
    - apps/mobile/app/(tabs)/add/index.tsx (inline edit TextInput pattern; source-tag chip row pattern; Alert usage)
    - apps/mobile/src/lib/cardUtils.ts (formatDueAt, formatLastReviewed)
    - apps/mobile/src/lib/api.ts (AllCard, getCard/updateCard/suspendCard/deleteCard)
    - apps/mobile/src/lib/theme.ts (colors, fonts, textColors)
    - .planning/phases/05-srs-ux-library/05-UI-SPEC.md (Card detail screen layout, SRS stat formatting table, copywriting contract, color rules)
    - .planning/phases/05-srs-ux-library/05-PATTERNS.md ("apps/mobile/app/cards/[id].tsx" section — exact imports + param/query/back-nav/StatTile/inline-edit snippets)
  </read_first>
  <action>
    Create `apps/mobile/app/cards/[id].tsx` (default export `CardDetailScreen`). Param + query: `const { id } = useLocalSearchParams<{ id: string }>(); const cardId = parseInt(id, 10); if (isNaN(cardId)) return null;` then `useQuery({ queryKey: ['cards', cardId], queryFn: () => api.getCard(cardId) })`. Layout top-to-bottom on light surface (UI-SPEC "Card detail screen"):
    1. Image thumbnail — full-width, 180px height, `resizeMode: cover`; if `image_url` null show `paperSoft` 180px placeholder with centered muted `<Body>` "No image".
    2. Word header — 20px horizontal / 16px top padding: headword as `<Display>` 30px; gendered_form as muted `<Body>` 15px; 3px gender rail on the section's left edge using gender color (feminine `#B43A6C`, masculine `#1F3494`, common `#C99A1F`).
    3. SRS stats grid — 2 columns × 3 rows of `<StatTile>`, six stats with EXACT labels + formatting (UI-SPEC stat formatting table): STABILITY = `Math.round(stability)` + " days"; DIFFICULTY = `Math.round(difficulty)` integer 1–10 no unit; SRS STATE = state badge, but show "Suspended" (muted gray `rgba(0,0,0,0.40)` text / `rgba(0,0,0,0.06)` bg) when `suspended_at != null` regardless of `state`; DUE = `formatDueAt(due_at)`; REVIEWS = `reps` integer; LAST REVIEWED = `formatLastReviewed(last_reviewed_at)`. SRS stat numbers use `<Num>` tone="brand" (cobalt, per accent reserved list). Stat tiles use the StatTile grid pattern from streak (gap-3/12px, px-5/20px, width '48.5%').
    4. Editable section — `<Mono>EDIT</Mono>` eyebrow; sentence_pt as a `<TextInput>` styled to match `<Body>` 15px (dashed cobalt underline when editing, per add-wizard inline-edit pattern); source_tag chip row (same chip pattern as add wizard). On save call `api.updateCard(cardId, { sentence_pt, source_tag })` via a useMutation; `onSuccess` invalidate `['cards']` AND `['home','summary']` AND `['cards', cardId]`.
    5. Actions — two stacked full-width buttons, 20px horizontal margin: suspend toggle as a muted bordered button (`borderColor: rgba(0,0,0,0.15)`, text `rgba(0,0,0,0.60)`) labeled "Suspend card" when `suspended_at` null else "Unsuspend card" — calls `api.suspendCard(cardId, !card.suspended_at)` via useMutation (no confirmation, reversible); "Delete card" as a destructive text-only button in `colors.again` (`#C84A40`) that opens `Alert.alert('Delete card?', 'This will permanently remove "{headword}" and its review history.', [{text:'Keep card', style:'cancel'},{text:'Delete', style:'destructive', onPress: () => deleteMutation.mutate()}])`; deleteMutation `onSuccess` invalidates `['cards']` + `['home','summary']` then `router.back()`.
    6. Back nav: copy the streak back-arrow pattern (`router.back()`); title "Card detail". Gold `#E8B838` must NOT appear — library/detail are light-surface only (UI-SPEC). All mutations use `useQueryClient()`.
  </action>
  <verify>
    <automated>cd /Users/graysonstricker/Programs/LingoCards &amp;&amp; test -f "apps/mobile/app/cards/[id].tsx" &amp;&amp; grep -q "useLocalSearchParams" "apps/mobile/app/cards/[id].tsx" &amp;&amp; grep -q "api.deleteCard\|deleteCard" "apps/mobile/app/cards/[id].tsx" &amp;&amp; grep -q "Delete card?" "apps/mobile/app/cards/[id].tsx" &amp;&amp; corepack pnpm exec tsc -p apps/mobile/tsconfig.json --noEmit 2>&amp;1 | tail -3; echo DONE</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/app/cards/[id].tsx` exists and default-exports a screen component
    - Uses `useLocalSearchParams<{ id: string }>()` + `parseInt(id,10)` + `isNaN` guard
    - Renders six StatTiles with labels STABILITY, DIFFICULTY, SRS STATE, DUE, REVIEWS, LAST REVIEWED
    - SRS STATE tile shows "Suspended" when `suspended_at != null`
    - Suspend button label toggles between "Suspend card" / "Unsuspend card"
    - Delete uses `Alert.alert` with title "Delete card?" and a destructive "Delete" button
    - Edit calls `api.updateCard`; suspend calls `api.suspendCard`; delete calls `api.deleteCard`
    - Every mutation invalidates `['cards']` and `['home','summary']`
    - `tsc -p apps/mobile/tsconfig.json --noEmit` reports no new errors
  </acceptance_criteria>
  <done>The card detail screen shows stats, edits sentence + source_tag, toggles suspend, and deletes with confirmation.</done>
</task>

<task type="auto">
  <name>Task 2: Wire _layout.tsx — GestureHandlerRootView + cards/[id] route</name>
  <files>apps/mobile/app/_layout.tsx</files>
  <read_first>
    - apps/mobile/app/_layout.tsx (existing imports, the `return (...)` tree with 3 Stack.Screen entries)
    - .planning/phases/05-srs-ux-library/05-RESEARCH.md (Pattern 6 Stack.Screen registration; Pitfall 2 GestureHandlerRootView placement — outermost, outside QueryClientProvider)
    - .planning/phases/05-srs-ux-library/05-PATTERNS.md ("apps/mobile/app/_layout.tsx" section — exact wrapper + Stack.Screen edit)
  </read_first>
  <action>
    Runs AFTER Task 1 so the route file `app/cards/[id].tsx` already exists — under `typedRoutes: true`, expo-router types the `Stack.Screen` `name` prop against the route registry, so registering `cards/[id]` before the file exists would fail tsc. Two edits to `apps/mobile/app/_layout.tsx`: (1) Add `import { GestureHandlerRootView } from 'react-native-gesture-handler';` at the top, and wrap the entire returned tree so `GestureHandlerRootView` with `style={{ flex: 1 }}` is the OUTERMOST element — outside `QueryClientProvider` (Pitfall 2: gestures are silently swallowed without a root provider above the navigator). (2) Inside the existing `<Stack>` block, add `<Stack.Screen name="cards/[id]" options={{ title: '' }} />` after the `streak/index` entry. Do not change the early `if (!fontsLoaded) return null;` guard or any existing screen options.
  </action>
  <verify>
    <automated>cd /Users/graysonstricker/Programs/LingoCards &amp;&amp; grep -q "GestureHandlerRootView" apps/mobile/app/_layout.tsx &amp;&amp; grep -q 'name="cards/\[id\]"' apps/mobile/app/_layout.tsx &amp;&amp; corepack pnpm exec tsc -p apps/mobile/tsconfig.json --noEmit 2>&amp;1 | tail -3; echo DONE</automated>
  </verify>
  <acceptance_criteria>
    - `_layout.tsx` imports `GestureHandlerRootView` from `react-native-gesture-handler`
    - `GestureHandlerRootView` with `style={{ flex: 1 }}` is the outermost wrapper, above `QueryClientProvider`
    - A `<Stack.Screen name="cards/[id]" options={{ title: '' }} />` entry exists inside the `<Stack>` block
    - The 3 pre-existing Stack.Screen entries are unchanged
    - `tsc -p apps/mobile/tsconfig.json --noEmit` reports no new errors
  </acceptance_criteria>
  <done>The app root provides gesture handling and the card detail route is navigable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the card detail screen on device</name>
  <action>Pause for the user to verify the card detail screen on a device/simulator before the phase closes. This is a verification-only checkpoint — all implementation lands in Tasks 1-2.</action>
  <verify><human-check>User confirms the how-to-verify steps below pass.</human-check></verify>
  <done>User types "approved".</done>
  <what-built>Card detail screen at app/cards/[id].tsx with six SRS stats, editable sentence + source tag, suspend/unsuspend toggle, and delete-with-confirmation; GestureHandlerRootView wired into the app root.</what-built>
  <how-to-verify>
    1. Start the app (`corepack pnpm --filter mobile start` or the project's run command) and open it on a device/simulator.
    2. From the Cards tab, tap any card row → the detail screen opens.
    3. Confirm the six stat tiles read sensibly (Stability "N days", Difficulty integer, SRS State badge, Due "Today"/date, Reviews count, Last Reviewed "Never"/relative).
    4. Edit the sentence text, save → it persists; if you open the review screen for that card, audio should match the new sentence (Option A re-synth from 05-02).
    5. Tap "Suspend card" → label flips to "Unsuspend card"; the card should be excluded from review queue (verify on Home due count after refresh).
    6. Tap "Delete card" → confirm the "Delete card?" alert appears; tap Delete → returns to library and the card is gone.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- `tsc -p apps/mobile/tsconfig.json --noEmit` clean
- `corepack pnpm test` still green (no pure-lib regressions)
- Human-verify checkpoint passed
</verification>

<success_criteria>
A user can open any card's detail screen, view its SRS stats, edit the sentence + source tag, suspend/unsuspend it, and delete it with confirmation — and swipe gestures are enabled app-wide for the wave-4 library plan.
</success_criteria>

<output>
Create `.planning/phases/05-srs-ux-library/05-04-SUMMARY.md` when done.
</output>
