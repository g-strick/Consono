---
phase: 05-srs-ux-library
plan: 03
type: execute
wave: 4
depends_on: ['05-02', '05-04']
files_modified:
  - apps/mobile/app/(tabs)/cards/index.tsx
  - apps/mobile/src/components/SwipeableRow.tsx
autonomous: false
requirements: [SRS-01, LIB-01, LIB-03]

must_haves:
  truths:
    - 'The library shows ALL cards (not just due) via getAllCards, newest-first'
    - 'Horizontally scrollable filter chips across gender, source, register, SRS state with AND logic'
    - 'Swipe-left on a row reveals Suspend/Unsuspend and Delete actions'
    - 'Tapping a row opens the card detail screen'
    - 'Pull-to-refresh re-fetches the card list'
    - 'Suspended rows show a Suspended badge instead of the SRS state badge'
  artifacts:
    - path: 'apps/mobile/src/components/SwipeableRow.tsx'
      provides: 'ReanimatedSwipeable wrapper with Suspend + Delete action panels'
      contains: 'react-native-gesture-handler/ReanimatedSwipeable'
    - path: 'apps/mobile/app/(tabs)/cards/index.tsx'
      provides: 'all-cards library: filter chip bar, swipe rows, pull-to-refresh, row→detail nav'
      contains: 'getAllCards'
  key_links:
    - from: 'apps/mobile/app/(tabs)/cards/index.tsx'
      to: 'filterCards + getAllCards'
      via: "useQuery ['cards','all'] + client-side filter"
      pattern: "filterCards\\("
    - from: 'apps/mobile/app/(tabs)/cards/index.tsx row'
      to: '/cards/[id]'
      via: 'router.push object form'
      pattern: "pathname: '/cards/\\[id\\]'"
    - from: 'SwipeableRow'
      to: 'api.suspendCard / api.deleteCard'
      via: 'onSuspend / onDelete callbacks → mutations in the screen'
      pattern: 'onSuspend|onDelete'
---

<objective>
Redesign the library screen (`app/(tabs)/cards/index.tsx`) to show ALL cards (D-01), with a multi-dimensional filter chip bar (D-02, LIB-01), swipe-left row actions for suspend + delete (D-03, SRS-01, LIB-03), pull-to-refresh, and row-tap navigation to the detail screen. Build the new `SwipeableRow` component (no codebase analog — RESEARCH Pattern 3).

Purpose: LIB-01 (filter), SRS-01 (suspend from library swipe), LIB-03 (delete from library swipe). This is the user-facing "library is a real, fully editable view of the deck" deliverable.
Output: New `SwipeableRow.tsx`, rewritten `cards/index.tsx`.

Depends on 05-04 because `_layout.tsx` already provides `GestureHandlerRootView` (swipes no-op without it) AND the `cards/[id]` typed route must already exist for `router.push({ pathname: '/cards/[id]', ... })` to typecheck under `typedRoutes: true`.
</objective>

<execution_context>
@/Users/graysonstricker/Programs/Consono/.claude/get-shit-done/workflows/execute-plan.md
@/Users/graysonstricker/Programs/Consono/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-srs-ux-library/05-RESEARCH.md
@.planning/phases/05-srs-ux-library/05-PATTERNS.md
@.planning/phases/05-srs-ux-library/05-UI-SPEC.md

<interfaces>
From 05-02 (apps/mobile/src/lib/api.ts):
  api.getAllCards(): Promise<{ cards: AllCard[] }>
  api.suspendCard(id: number, suspend: boolean): Promise<void>
  api.deleteCard(id: number): Promise<void>
  AllCard adds: source_tag, suspended_at, created_at, stability, difficulty, reps, lapses, last_reviewed_at

From 05-02 (apps/mobile/src/lib/cardUtils.ts):
filterCards(cards: AllCard[], filters: ActiveFilters): AllCard[]
type ActiveFilters = { gender: GenderValue[]; source_tag: string[]; register: RegisterTag[]; srs_state: SrsStateFilter[] }

ReanimatedSwipeable (RESEARCH Pattern 3 — import EXACTLY this path, NOT the package root):
import Swipeable, { SwipeableMethods } from 'react-native-gesture-handler/ReanimatedSwipeable';
import { SharedValue } from 'react-native-reanimated';
renderRightActions(progress: SharedValue<number>, translation: SharedValue<number>, swipeable: SwipeableMethods)

Existing cards/index.tsx (to be rewritten/extended):
query key ['cards','due'] -> change to ['cards','all'], getDueCards() -> getAllCards()
existing CardRow + gender-bar + chip UI patterns to preserve/extend
stateColor() badge helper — add suspended -> '#5A6995' muted case

Reused components (Phase 1.1, do NOT respecify): <Body>, <Chip>, Ionicons via @expo/vector-icons
typedRoutes navigation: router.push({ pathname: '/cards/[id]', params: { id: card.id } })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build SwipeableRow component</name>
  <files>apps/mobile/src/components/SwipeableRow.tsx</files>
  <read_first>
    - .planning/phases/05-srs-ux-library/05-RESEARCH.md (Pattern 3 — full ReanimatedSwipeable component source; Pitfall 3 deprecated import; Anti-Patterns)
    - .planning/phases/05-srs-ux-library/05-UI-SPEC.md (Library screen — row swipe: 80px panels, Suspend neutral rgba(0,0,0,0.08), Delete destructive #C84A40, single-word labels, Ionicons pause-circle/play-circle/trash)
    - apps/mobile/src/lib/theme.ts (colors.again = #C84A40 for delete)
  </read_first>
  <action>
    Create `apps/mobile/src/components/SwipeableRow.tsx`. Import `Swipeable, { SwipeableMethods }` from `react-native-gesture-handler/ReanimatedSwipeable` (NEVER the package root — Pitfall 3 gives the deprecated Animated component). Props interface: `{ onSuspend: () => void; onDelete: () => void; isSuspended: boolean; children: React.ReactNode }`. Implement `renderRightActions(progress: SharedValue<number>, translation: SharedValue<number>, swipeable: SwipeableMethods)` rendering two 80px-wide full-height TouchableOpacity panels in a `flexDirection: 'row'` view: (1) Suspend/Unsuspend — `backgroundColor: 'rgba(0,0,0,0.08)'`, Ionicons `pause-circle` when `!isSuspended` else `play-circle` (icon color `rgba(0,0,0,0.60)`), label "Suspend"/"Unsuspend" (single word, no "card" noun), `onPress` calls `swipeable.close()` then `onSuspend()`. (2) Delete — `backgroundColor: '#C84A40'`, Ionicons `trash` white, label "Delete" white, `onPress` calls `swipeable.close()` then `onDelete()`. Render `<Swipeable ref={swipeableRef} renderRightActions={renderRightActions} overshootRight={false}>{children}</Swipeable>` with `swipeableRef = useRef<SwipeableMethods>(null)`. Panels span minimum 44pt tap height (full row height). Suspend is the neutral/muted action (NOT accent cobalt, NOT destructive red).
  </action>
  <verify>
    <automated>cd /Users/graysonstricker/Programs/Consono &amp;&amp; grep -q "react-native-gesture-handler/ReanimatedSwipeable" apps/mobile/src/components/SwipeableRow.tsx &amp;&amp; grep -q "onSuspend" apps/mobile/src/components/SwipeableRow.tsx &amp;&amp; grep -q "#C84A40" apps/mobile/src/components/SwipeableRow.tsx &amp;&amp; corepack pnpm exec tsc -p apps/mobile/tsconfig.json --noEmit 2>&amp;1 | tail -3; echo DONE</automated>
  </verify>
  <acceptance_criteria>
    - `SwipeableRow.tsx` imports from `react-native-gesture-handler/ReanimatedSwipeable` (NOT the package root)
    - Props are `onSuspend`, `onDelete`, `isSuspended`, `children`
    - Renders an 80px Suspend panel (`rgba(0,0,0,0.08)`) and an 80px Delete panel (`#C84A40`)
    - Suspend icon toggles `pause-circle`/`play-circle` on `isSuspended`; Delete icon is `trash`
    - Each action calls `swipeable.close()` before its callback
    - `tsc` reports no new errors
  </acceptance_criteria>
  <done>A reusable swipe-left row component exposing Suspend and Delete actions.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite library screen — all cards, filters, swipe, refresh, nav</name>
  <files>apps/mobile/app/(tabs)/cards/index.tsx</files>
  <read_first>
    - apps/mobile/app/(tabs)/cards/index.tsx (current screen: useQuery ['cards','due'], Filter type, filterCards helper, CardRow, chip UI, stateColor)
    - apps/mobile/src/components/SwipeableRow.tsx (the component built in Task 1)
    - apps/mobile/src/lib/cardUtils.ts (filterCards + ActiveFilters — replaces the local single-dimension filter)
    - apps/mobile/src/lib/api.ts (getAllCards, suspendCard, deleteCard, AllCard)
    - .planning/phases/05-srs-ux-library/05-UI-SPEC.md (filter chips active/inactive styling, library row anatomy, suspended badge coexistence rule, empty/error/filtered-to-zero states, pull-to-refresh, copywriting contract)
    - .planning/phases/05-srs-ux-library/05-PATTERNS.md ("apps/mobile/app/(tabs)/cards/index.tsx" section; Mutation + cache invalidation; State badge / gender-bar helpers; Delete confirmation Alert)
  </read_first>
  <action>
    Rewrite `apps/mobile/app/(tabs)/cards/index.tsx`:
    - Data: replace `useQuery({ queryKey: ['cards','due'], queryFn: getDueCards })` with `['cards','all']` + `api.getAllCards()`. Remove the local `Filter` type + `filterCards` helper; import `filterCards`, `ActiveFilters` from `@/src/lib/cardUtils`. Hold filter state as `ActiveFilters` (`{ gender:[], source_tag:[], register:[], srs_state:[] }`); visible list = `filterCards(allCards, filters)`. Cards arrive newest-first from the API; do not re-sort.
    - Filter chip bar: a single horizontally scrollable row (`ScrollView horizontal showsHorizontalScrollIndicator={false}`) with four chip groups (UI-SPEC Interaction Contract): gender (Masculine/Feminine/Common), source tag (canonical WhatsApp/Instagram/Netflix ALWAYS present + any distinct custom `source_tag` values found in the fetched cards), register (Formal/Informal/Slang/Neutral — NOT Vulgar), SRS state (New/Learning/Review/Relearning/Suspended). Tapping a chip toggles its value in the matching `ActiveFilters` dimension array (multi-select, AND across dimensions). Active chip = custom inline pill `backgroundColor:'#1F3494'`, `borderColor:'#142468'`, white `fonts.uiSemiBold` text; inactive chip = `<Chip variant="default">` (paperSoft bg, black text). The `<Chip>` component has no text-color override, so render the active state as a custom inline pill (UI-SPEC + RESEARCH note). 20px horizontal edge padding, 8px chip gap. If FilterChipBar exceeds ~40 lines, extract to `src/components/FilterChipBar.tsx`.
    - Rows: wrap each row in `<SwipeableRow isSuspended={card.suspended_at != null} onSuspend={() => suspendMutation.mutate({id: card.id, suspend: card.suspended_at == null})} onDelete={() => confirmDelete(card)}>`. The row body (existing CardRow) is wrapped in a `TouchableOpacity` whose `onPress` navigates `router.push({ pathname: '/cards/[id]', params: { id: card.id } })` (typedRoutes-safe object form). Badge rule (UI-SPEC suspended coexistence): when `suspended_at != null` show a muted "Suspended" badge (`rgba(0,0,0,0.40)` text / `rgba(0,0,0,0.06)` bg) IN PLACE OF the SRS state badge — never both. Extend `stateColor()` with a suspended case if used.
    - Mutations: `suspendMutation` -> `api.suspendCard(id, suspend)`; `deleteMutation` -> `api.deleteCard(id)`. Both `onSuccess` invalidate `['cards']` AND `['home','summary']`. `confirmDelete(card)` opens `Alert.alert('Delete card?', 'This will permanently remove "{headword ?? this card}" and its review history.', [{text:'Keep card', style:'cancel'},{text:'Delete', style:'destructive', onPress: () => deleteMutation.mutate(card.id)}])`. Suspend executes immediately, no confirmation (reversible).
    - FlatList: add `<RefreshControl>` (pull-to-refresh) whose `onRefresh` invalidates/refetches `['cards','all']`; `refreshing` bound to the query's `isRefetching`.
    - States/copy (UI-SPEC copywriting): subtitle "{N} cards"; empty deck -> "No cards yet." / "Add a word to get started."; filtered-to-zero -> "No cards match." / "Try removing some filters." + "Clear filters" cobalt text button that resets `filters` to all-empty; error -> "Could not load cards." / "Pull to refresh." (no separate Retry button). Title "Cards" kept. Gold `#E8B838` must NOT appear (light surface only).
  </action>
  <verify>
    <automated>cd /Users/graysonstricker/Programs/Consono &amp;&amp; F='apps/mobile/app/(tabs)/cards/index.tsx'; grep -q "getAllCards" "$F" &amp;&amp; grep -Eq "'cards', ?'all'" "$F" &amp;&amp; grep -q "SwipeableRow" "$F" &amp;&amp; grep -q "RefreshControl" "$F" &amp;&amp; grep -q "pathname: '/cards/\[id\]'" "$F" &amp;&amp; corepack pnpm exec tsc -p apps/mobile/tsconfig.json --noEmit 2>&amp;1 | tail -3; echo DONE</automated>
  </verify>
  <acceptance_criteria>
    - `cards/index.tsx` uses `useQuery({ queryKey: ['cards','all'], queryFn: () => api.getAllCards() })`
    - Imports `filterCards` + `ActiveFilters` from `@/src/lib/cardUtils`; no local single-dimension Filter type remains
    - Filter chip bar is a horizontal ScrollView with gender/source/register/SRS-state groups; active chip is solid `#1F3494` with white text
    - Each row is wrapped in `<SwipeableRow>` with `onSuspend`/`onDelete`/`isSuspended`
    - Row tap navigates via `router.push({ pathname: '/cards/[id]', params: { id: card.id } })`
    - Suspended rows show the "Suspended" badge in place of the SRS state badge
    - FlatList has a `RefreshControl`; delete uses `Alert.alert` with "Delete card?"; suspend/delete mutations invalidate `['cards']` + `['home','summary']`
    - Filtered-to-zero shows "No cards match." + a "Clear filters" button that resets filters
    - `tsc -p apps/mobile/tsconfig.json --noEmit` reports no new errors
  </acceptance_criteria>
  <done>The library shows all cards with multi-dimensional filters, swipe suspend/delete, pull-to-refresh, and row-tap navigation to detail.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the library redesign on device</name>
  <action>Pause for the user to verify the redesigned library on a device/simulator before the phase closes. This is a verification-only checkpoint — all implementation lands in Tasks 1-2.</action>
  <verify><human-check>User confirms the how-to-verify steps below pass.</human-check></verify>
  <done>User types "approved".</done>
  <what-built>Redesigned Cards library: all cards (newest-first), horizontally scrollable multi-dimensional filter chips (AND logic), swipe-left Suspend/Unsuspend + Delete actions, pull-to-refresh, row-tap to detail; new SwipeableRow component.</what-built>
  <how-to-verify>
    1. Open the Cards tab. Confirm it now lists ALL cards (including not-yet-due), newest first, subtitle "{N} cards".
    2. Tap filter chips across multiple groups (e.g. Masculine + Informal) — the list narrows with AND logic; tapping again deselects.
    3. Select filters that match nothing → "No cards match." + "Clear filters"; tap Clear filters → all cards return.
    4. Swipe a row left → Suspend (muted) and Delete (red) panels appear. Tap Suspend → row shows "Suspended" badge immediately; swipe again shows "Unsuspend".
    5. Swipe left → Delete → confirm the "Delete card?" alert; tap Delete → row disappears.
    6. Tap a row (no swipe) → the card detail screen opens.
    7. Pull down to refresh → the list re-fetches.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- `tsc -p apps/mobile/tsconfig.json --noEmit` clean
- `corepack pnpm test` still green
- Human-verify checkpoint passed
</verification>

<success_criteria>
The library is a filterable, swipe-actionable, refreshable view of the entire deck, and every row opens the detail screen — satisfying Phase 5 success criteria 1 (suspend disappears from queue) and 4 (filter + edit + delete without navigation issues).
</success_criteria>

<output>
Create `.planning/phases/05-srs-ux-library/05-03-SUMMARY.md` when done.
</output>
