---
context: default
phase: 1-core-flow-fixed
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-06-08
---

## Current State

Phase 1 of 7 — "Core Flow Fixed". Two of three plans complete. Paused before Plan 1.3.

<current_state>
Plans 1.1 and 1.2 are committed to main. Plan 1.3 is the only remaining item to close Phase 1.

Plan 1.3: Add GET /users/me endpoint and wire the real user name into the Home screen, replacing the hardcoded "Léo".
</current_state>

<completed_work>

- Plan 1.1: Tab bar / flex layout fix in Add wizard (commit 9fc062d)
- Plan 1.2: Sentence TTS moved to POST /cards at approval time; word TTS removed from /generate entirely (commit fa89fef)
  - `apps/api/src/routes/generate.ts` — TTS stripped, /sentence-audio sub-route removed
  - `apps/api/src/routes/cards.ts` — synthesize(selected_sentence) at approval, sentence_audio_clip_hash stored
  - `apps/mobile/app/(tabs)/add/index.tsx` — sentenceAudioMutation removed, onSave(sentence) API added, sentence mode pre-populates selectedSentence
  - `apps/mobile/app/review/index.tsx` — front uses sentence_audio_url
  - `apps/mobile/src/lib/api.ts` — generateSentenceAudio removed, types updated
  - doc files updated: REQUIREMENTS.md, PROJECT.md, STATE.md (untracked, committed via handoff)
    </completed_work>

<remaining_work>

- Plan 1.3: GET /users/me endpoint + Home screen real user name
  - Backend: new route in apps/api/src/routes/ returning { name: string }
  - Mobile: useQuery hook calling /users/me, wire into Home screen to replace "Léo"
  - Check users table schema in packages/db/src/schema.ts for the name/display_name field
    </remaining_work>

<decisions_made>

- Sentence is the card's only audio asset — plays on both review front (auto-play) and reveal (manual button)
- onSave(sentence: string) threads final text to TTS so ADD-03 inline edits work correctly when implemented
- Sentence mode pre-populates selectedSentence from candidates[0] after generation — removes || fallback from approval path
- z.string().min(1) guard on selected_sentence in ApproveInput (cards.ts)
- useSafeAreaInsets() + useBottomTabBarHeight() combined for scroll padding in Add wizard steps
  </decisions_made>

<blockers>
- None blocking. Optional: device test to verify sentence audio plays on review front/reveal before marking Phase 1 complete.
</blockers>

## Required Reading (in order)

1. `.planning/STATE.md` — current phase status and decisions
2. `apps/api/src/routes/cards.ts` — understand the new TTS-at-approval pattern before modifying
3. `packages/db/src/schema.ts` — check users table for name field before implementing /users/me

## Infrastructure State

- API: Hono on port 3000, `apps/api/src/routes/` — add new route file and register in main index
- Mobile: Expo Router, `apps/mobile/app/(tabs)/index.tsx` is the Home screen
- DB: Drizzle + Supabase Postgres, schema at `packages/db/src/schema.ts`
- V0_USER_ID constant used everywhere (single-user phase) — /users/me should use it

<context>
Clean state. No uncommitted changes. Planning docs (PROJECT.md, REQUIREMENTS.md, ROADMAP.md, STATE.md) are untracked — they'll be committed with this handoff. Plan 1.3 is small: one new API route and a query hook in the Home screen.
</context>

<next_action>
Start with: read packages/db/src/schema.ts to find the users table name field, then add GET /users/me to the API, then wire into the Home screen.
</next_action>
